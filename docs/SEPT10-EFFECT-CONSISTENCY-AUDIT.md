# 2026-09-10 至今效果一致性复核

## 2026-09-14 增量：响应费用/效果说明与最小化墓地查看

- 响应窗口不再依赖整段卡文让对手自行判断冒号前后。权威支付成功后才记录结构化Cost摘要，弹框固定分行显示`Cost（已支付）`与`效果`；不显示未提交费用，也不额外解释无效后的退款规则。单次响应承载多段时只展开该响应实际覆盖的全部已启用效果，不泄露尚未确定的分支。
- 嬴政支付成功会公开实际弃置的当前8费军团及击杀、返还士气、追加士气限制；无8费时只走展示手牌分支且没有虚假Cost。天照第二能力公开实际弃牌并同时包含士气转活跃与前排【高天原】+1000。共享记录器同步覆盖复合手牌打出和其他现有预支付效果。
- 所有阻塞Prompt最小化后都可打开双方公开墓地并查看卡牌详情；墓地覆盖层处于只读检查模式，不能发动墓地能力或奥西里斯特殊胜利，原Prompt与选择状态不丢失。
- 提交`fb50b52374ca5fe3bd82443b785f3cc2d0eed2d5`通过专项27/27、Release规则3228/3228、平台116/116、UI323、连接23、卡图324+42、原子审计110程序/96卡及前后端生产构建；证据位于`D:\GPT\Legion12\artifacts\deploy\fb50b52374ca5fe3bd82443b785f3cc2d0eed2d5\l12-release-fb50b52374ca5fe3bd82443b785f3cc2d0eed2d5.json`，未部署。

## 2026-09-14 增量：对方单张弃牌与贯穿

- 全池扫描把纳芙蒂蒂、血斧艾瑞克、海伦、鲍斯、佐佐木小次郎5项单结果归并为同一结构化协议；条件在触发时锁定，对方在结算期私密选择，唯一手牌仍须点击。原始无目标静默，响应后手牌变空=`failed`，无效不泄露候选，V2恢复及重复提交安全。〈毒药〉因包含先无效再弃牌的复合语义而排除。
- 鲍斯此前存在卡文和局部审计结论，但缺少权威阵亡运行入口；本批通过按能力形态的全池扫描发现并补齐，说明“卡号存在某个入口”不能替代逐能力验收。
- 贯穿权威事件新增“来源是否为进攻军团”事实。赵云、帕西瓦尔作为防守方反杀不再触发贯穿；进攻方正例仍按剩余兵力进攻主宰且不触发“进攻时”。其他防守方击杀能力不受这一关键词专属条件影响。
- 功能提交`995b04d98da7f3c3b2f9037b5825a2f5f7fde653`最终通过规则3226/3226、平台116/116、UI323、连接23、卡图324+42、原子110程序/96卡和前后端生产构建；未部署。

## 范围

- 复核时间范围：2026-09-10 00:00（Asia/Shanghai）至本批开始时的远端 `main` `038c911`。
- Git 一方主线共 48 个提交，其中 27 个产品/规则提交、19 个同步或部署回执、2 个排位治理提交，涉及 157 个去重路径。
- 不以“某张卡的专项绿测”代替验收。当前态必须同时通过运行入口、支付与候选、响应逆序重验、结算结果、权威快照/重连、按钮/弹框、动效/日志/回放、卡牌目录/图鉴以及生产构建。

## 本次发现并修复

### 始皇帝 嬴政

- 根因不是原支付函数本身，而是效果登场、GM/权威置入和重复登场等通用入口绕过了登场前支付网关，直接把击杀段放入堆叠。
- 所有登场入口现统一在 `QueueOrPushTriggeredEffect` 进入冒号前 Cost 网关。只有实际支付当前费用恰为 8 的军团后，才允许击杀、返还士气和追加士气限制入栈；没有合法支付对象只展示手牌。
- 候选和提交均读取当前费用而不是印刷费用。已选对象失效但仍有其他合法对象时重新计算候选并生成新 Prompt；旧 Prompt 作废，重复提交不能二次弃牌。来源离场或候选全部消失会安全关闭或进入未满足条件分支，不会卡死。

### 天照大神

- 第二能力的弃牌 Cost、最多两张休整士气选择、一次响应边界和前排【高天原】持续 +1000 保持为同一能力，但数值状态、展示状态和实际事件此前不是同一份权威投影。
- 现在结算建立按 `TurnSerial` 限定的持续状态，立即重算兵力，并发布准确的实际效果事件；V2 快照为受影响军团提供“本回合兵力+1000 / 天照大神”状态。
- 回归固定结算当刻、之后进入前排、前后排移动、检查点恢复/重连、整项被无效及回合结束清除，避免把一次性加值误当持续状态或只改界面不改实际数值。

### 孙悟空

- 服务端卡牌目录已是批准后的新文本，但图鉴使用的 `cards.lookup.json` 仍保留旧文本，这是此前只核对一个来源导致的遗漏。
- 图鉴 `effectText`、搜索文本和服务端 S2 目录现逐字一致。卡图架构门禁新增孙悟空专门契约，任一来源漏改都会使生产构建失败。

## 当前证据

- 定向：嬴政通用入口、当前费用、无支付对象、支付成功、候选失效重选、来源离场、旧 Prompt 重复提交；天照 V2 实际兵力/状态/日志/重连/到期，共 8/8 通过。
- Focused：2979/2979 规则测试，零失败、零跳过；S1/S2/ST 分区效果声明门禁全部通过。
- 卡池：248 张卡 / 578 个效果段逐能力审计，189 项直接通过、59 项已有修复证据、0 项待人工判断。
- 原子运行：324 张卡；旧卡号 `case` 为 0、效果文本推断为 0、无运行入口为 0。
- Batch：Release 配置 2979/2979 规则测试；UI 契约 320 项；连接恢复 23/23；Prompt、效果呈现、录像、排位广播、构筑同步与卡图 40 项/324 张门禁通过；Vue/TypeScript 与 Vite 生产构建通过。

## 后续强制标准

1. 冒号前 Cost 必须经共享预支付事务；候选与提交使用同一当前状态谓词。
2. 冒号后的效果对象不反向成为发动费用；必发效果无对象时 `skipped`，有对象即使唯一也保留玩家选择。
3. 入栈时声明对象，逆序结算时用同一谓词重验；失效为 `failed`，不改选、不退已支付 Cost。
4. 支付或选择失败必须可取消、可安全重选或进入规则定义的替代分支；旧 Prompt 和重复命令不得再次改变状态。
5. 持续效果必须进入权威状态、快照和恢复数据，而不只是结算当刻修改一个实体或只显示文字。
6. 每次卡文变更必须同时校验服务端目录、图鉴、搜索、按钮/Prompt、实际结果、动效、日志和回放；生产构建必须执行交叉来源契约。
7. 后台卡效工作台仍排在效果一致性之后；届时结构化能力段将成为卡文、勘误、产品收录、Prompt/日志/动效文案及呈现样式的唯一来源，样式选项必须带图例或可播放范例，并遵循现有布局与设计系统。

本文件记录的是当前代码与本批验证结果，不代表本批已部署。正式发布仍须使用干净提交运行 Release 门禁并核对远端身份；生产部署须另有当次授权。
