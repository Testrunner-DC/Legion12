# 十二军团当前任务进度与排期

更新时间：2026-09-24。此表只列当前主线、仍有独有提交的工作树和现有专项任务的可核验状态；历史过程记录不重复列为待办。

## 一、已完成并进入主线

| 工作项 | 当前状态 | 验收/上线状态 | 后续动作 |
| --- | --- | --- | --- |
| 架构P0—P4 | 已完成 | P0卡效出口、P1内核边界、P2外围投影、P3持久化、P4规则中心与移动结构均退出；架构锁随本批接入本地与GitHub门禁 | 后续功能在架构锁内扩展；不自动开启P5 |
| 效果一致性与后台卡效工作台 | 已完成 | 324张卡、686能力段、78/78生命周期档案；工作台已进入主线和测试服 | 按现有能力身份与结构哈希继续维护，不另建旁路 |
| 低卡顿性能治理 | 已完成 | 主线最终提交`6953e44`已部署测试服；性能样本、HTTP和WebSocket通过，正式服未因该批切换 | 新功能继续受轮询、请求扇出、重试、限流和动作门闩约束 |
| 移动对战互动区与比例 | 已完成 | `79463be`已进入主线并包含在测试服`6953e44`；127组状态、11档视口、弹框/安全区及桌面隔离通过 | 后续仅按具体Bug或明确新需求迭代 |
| 对局记录专项修正 | 已完成 | Main以`a91b0ce`接收并推送，随后包含在测试服`6953e44` | 新的记录需求继续走统一事件事实与投影，不拼卡名文案 |
| 公开牌库、牌库存储与详情内容 | 已完成并进入主线 | 存储规范化、公开浏览、详情/指南/版本、准确空态及请求可靠性已合并，并包含在测试服`6953e44` | 不再把旧交接中的“实施中/待接收”当作当前状态 |

## 二、当前仍需完成的工作

| 优先级 | 工作项 | 可核验状态 | 下一出口 |
| --- | --- | --- | --- |
| P0 | 架构退出锁与状态归并 | 本批已完成实现与批次验证；只新增总锁、持续集成接线和状态文档，不改业务逻辑 | 以本记录所在远端提交与Release回执为完成依据；不部署 |
| P1 | `L12-牌库界面优化` | 独立工作树干净，存在2个主线尚未包含的提交；核心实现为`b2bbe27`，合并记录为`d6e0842`，当前落后主线9个提交 | 由该专项基于最新main重放/解决冲突，Main验收移动/宽屏、规则校验和完整Release后统一推送 |
| P2 | 正式服Bug关闭闭环 | 工程清单记录为25项已修、3项需移动真机复测、3项无需代码、1项已转性能治理；后台报告仍不能仅因代码合并而关闭 | 形成正式候选后，经明确部署授权上线；按原场景复测并逐项关闭/关联重复项 |
| P3 | `L12-后台优化` | 方案已审阅，未开始编码 | 牌库批次收口后拆成独立最小批，先做信息架构与高频操作，不与规则/赛事后台混写 |
| P4 | `L12-规则中心优化` | A—D方案已审阅，等待独立实施；当前规则中心基础结构已满足架构退出 | 从批次A开始；先锁字段迁移、`effectiveAt`原子切换、深链接和搜索不泄漏，再进入实现 |
| P5 | `L12-赛事中心优化` | 方案已交Main审阅，尚未进入实现 | 在规则内容版本契约稳定后再排实施，避免重复建设赛事规则快照 |

## 三、发布队列

1. **本批**：架构锁与任务状态归并，完成Release、推送；不部署。
2. **下一功能批**：优先接收`L12-牌库界面优化`的独有提交。它已经有实际代码，且落后主线，继续等待会增加冲突成本。
3. **其后实施批**：后台优化、规则中心优化、赛事中心优化按P3→P4→P5逐批实施；共享后台文件不安排并行写入，每批分别验证和推送。
4. **测试服候选**：牌库批次或用户指定的下一批完成后，再把最新`origin/main`作为单一候选部署测试服并做版本、HTTP、WebSocket、移动/桌面和相关功能验收。
5. **正式服候选**：只在用户明确授权当批正式部署后执行；包含玩家更新日志、维护策略、备份/回滚、版本精确核验和正式服Bug逐项复测。正式上线前不把后台报告标记为已解决。

## 四、调度原则

- 同一时间最多一个共享代码写入批；方案评审和只读验收可以并行。
- 独立任务只交付干净提交和证据，Main负责重放、冲突处理、最终Release、推送和部署。
- 已进入主线的历史工作树不再代表待办；确认无独有提交、无未跟踪文件且任务结束后，才按Git正规流程清理。
- 排期按依赖顺序而非虚构日期；真实规则裁定、外部权限或生产部署授权是唯一允许暂停既定队列的输入门。
