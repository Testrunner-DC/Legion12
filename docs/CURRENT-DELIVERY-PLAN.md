# 十二军团当前任务进度与排期

更新时间：2026-09-25。此表只列当前主线、仍有独有提交的工作树和现有专项任务的可核验状态；历史过程记录不重复列为待办。

## 一、已完成并进入主线

| 工作项 | 当前状态 | 验收/上线状态 | 后续动作 |
| --- | --- | --- | --- |
| 架构P0—P4 | 已完成 | P0卡效出口、P1内核边界、P2外围投影、P3持久化、P4规则中心与移动结构均退出；架构锁随本批接入本地与GitHub门禁 | 后续功能在架构锁内扩展；不自动开启P5 |
| 效果一致性与后台卡效工作台 | 已完成 | 324张卡、686能力段、78/78生命周期档案；工作台已进入主线和测试服 | 按现有能力身份与结构哈希继续维护，不另建旁路 |
| 低卡顿性能治理 | 已完成 | 主线最终提交`6953e44`已部署测试服；性能样本、HTTP和WebSocket通过，正式服未因该批切换 | 新功能继续受轮询、请求扇出、重试、限流和动作门闩约束 |
| 移动对战互动区与比例 | 已完成 | `79463be`已进入主线并包含在测试服`6953e44`；127组状态、11档视口、弹框/安全区及桌面隔离通过 | 后续仅按具体Bug或明确新需求迭代 |
| 对局记录专项修正 | 已完成 | Main以`a91b0ce`接收并推送，随后包含在测试服`6953e44` | 新的记录需求继续走统一事件事实与投影，不拼卡名文案 |
| 公开牌库、牌库存储与详情内容 | 已完成并进入主线 | 存储规范化、公开浏览、详情/指南/版本、准确空态及请求可靠性已合并，并包含在测试服`6953e44` | 不再把旧交接中的“实施中/待接收”当作当前状态 |

## 二、当前仍需完成的工作

| 优先级 | 工作项 | 可核验状态 | 下一出口 |
| --- | --- | --- | --- |
| P0 | 牌库界面第二轮 | 已由 Main 接收并合入统一候选；删除、公开内容编辑、同页锚点、横卡/详情统一、侧栏/卡池/筛选及分享二维码均通过合同、扫码和六档浏览器验收 | 随本批完整 Release 后统一推送并部署测试服 |
| P1 | 移动对战与沙盒入口第二轮 | 已由 Main 接收并合入统一候选；士气遗存、玩家信息、圆形槽、沙盒入口全功能及桌面排名回归通过127组交互、连续尺寸和宽屏隔离验收 | 随本批完整 Release 后统一推送并部署测试服 |
| P2 | 测试服验收数据 | 严格环境隔离的验收场景包已进入主线；只为测试服账号幂等补齐牌库/公开内容场景，不创建比赛结果和统计 | 本批部署后核验版本、健康、WebSocket与验收牌库可见性 |
| P3 | 正式服Bug关闭闭环 | 工程清单记录为25项已修、3项需移动真机复测、3项无需代码、1项已转性能治理；后台报告仍不能仅因代码合并而关闭 | 形成正式候选后，经明确部署授权上线；按原场景复测并逐项关闭/关联重复项 |
| P4 | `L12-后台优化` | 已以 `7ea1e25` 完成 Main 验收并推送；危险操作、凭据单次显示、权限与审计边界已收口 | 纳入最终发布候选，不再单独实施或部署 |
| P5 | `L12-规则中心优化` | 已以 `af1eb8a` 完成审核、定时生效、公开投影与资源同步收口；`03c67f2`另完成首屏并发最小修复 | 纳入最终发布候选；后续内容仍按逐项审核发布契约扩展 |
| P6 | `L12-赛事中心优化` | Main 对抗复核已通过；已补强接任资格复验和公开信息最小化，赛事30/30、平台204/204、规则4759/4759、UI342与双构建378模块通过；Batch退出码0 | 由 Main 统一提交推送，然后进入最终 Release 和双环境部署 |

## 三、发布队列

1. **当前候选**：牌库第二轮、移动对战/沙盒入口第二轮和测试服验收场景包已由 Main 逐一接收并合入同一候选；专项工作树没有互相合并，也没有自行推送或部署。
2. **统一验收**：牌库合同、二维码扫码、六档详情/编辑器浏览器矩阵、移动127组交互、沙盒32组和身份28组已通过；整合后继续执行完整 Release。
3. **统一发布门槛**：牌库、移动对战／沙盒、后台、规则中心、赛事中心、回放遮挡、对局动效和公开牌库版本统计必须全部实施、Main验收、合入、门禁全绿才能进入部署。
4. **测试服**：在干净 HEAD 上通过完整 Release 后部署 `/testrun/`，验收公网 HTTP、健康、WebSocket、运行提交、测试场景和资源复用。
5. **正式服与维护**：测试服验收成功后部署同一提交至正式服，验收版本、健康、HTTP 和 WebSocket；全部成功后解除正式服维护。玩家更新日志要覆盖上次正式版本以来的全部可见变化，不出现后台、存储或安全实现细节。

## 四、调度原则

- 同一时间最多一个共享代码写入批；方案评审和只读验收可以并行。
- 独立任务只交付干净提交和证据，Main负责重放、冲突处理、最终Release、推送和部署。
- 已进入主线的历史工作树不再代表待办；确认无独有提交、无未跟踪文件且任务结束后，才按Git正规流程清理。
- 排期按依赖顺序而非虚构日期；真实规则裁定、外部权限或生产部署授权是唯一允许暂停既定队列的输入门。
