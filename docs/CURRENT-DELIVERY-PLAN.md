# 十二军团当前任务进度与排期

更新时间：2026-09-26。此表只列当前主线、仍有独有提交的工作树和现有专项任务的可核验状态；历史过程记录不重复列为待办。

正式服已于 2026-09-26 原子部署 `08cd7af644106ce8c723d24af5230c05c4db437a`：公网健康、首页、卡牌页、WebSocket 与玩家更新日志注入均通过，维护状态按用户最新指令保持 `true`，未调用解除接口。测试服仍运行已验收的 `b7d9f6306b9b6b518ce948325e7ed7ee01b2c485`。规范工作区及 GitHub 在部署时精确对齐 `08cd7af`，原独有分支 `codex/log-essential-details@76a4662` 保留。其后的部署回执若形成仅文档提交，只表示台账前移，不代表正式服业务版本落后或需要重复部署。

## 一、已完成并进入主线

| 工作项 | 当前状态 | 验收/上线状态 | 后续动作 |
| --- | --- | --- | --- |
| 架构P0—P4 | 已完成 | P0卡效出口、P1内核边界、P2外围投影、P3持久化、P4规则中心与移动结构均退出；架构锁随本批接入本地与GitHub门禁 | 后续功能在架构锁内扩展；不自动开启P5 |
| 效果一致性与后台卡效工作台 | 已完成 | 324张卡、686能力段、78/78生命周期档案；工作台已进入主线和测试服 | 按现有能力身份与结构哈希继续维护，不另建旁路 |
| 低卡顿性能治理 | 已完成 | 全部治理批次已进入 `b7d9f63` 测试服；性能架构、请求可靠性、资源同步和动作门闩门禁通过 | 新功能继续受轮询、请求扇出、重试、限流和动作门闩约束 |
| 移动对战互动区与比例 | 已完成 | `b7d9f63` 已进入测试服；候选新增双方同构外置计时轨、弹框同级尺寸合同，计时对抗 844/844（36图）、弹框 203/203、旧移动弹层 64 组（412图）通过 | 待本候选 Release、远端同步和正式发布 |
| 对局记录专项修正 | 已完成 | 统一事件事实与玩家投影已进入 `b7d9f63` 测试服 | 新的记录需求继续走统一事件事实与投影，不拼卡名文案 |
| 公开牌库、牌库存储与详情内容 | 已完成并进入主线 | 存储、浏览、详情、指南、版本、匿名统计、短码和二维码均已进入 `b7d9f63` 测试服 | 不再把旧交接中的“实施中/待接收”当作当前状态 |
| 后台、规则中心与赛事中心完整方案 | 已完成 | 独立路由/工作台、规则审核发布、赛事生命周期和恢复边界已进入 `b7d9f63` 测试服；Release 平台测试 205/205 | 正式部署后按原场景验收，不再以历史方案缺口重复立项 |
| 对战展示与选择流程 | 已完成 | 军团离场、动画顺序、比约恩加权多选/登场已进入 `b7d9f63` 测试服；候选又根治计时器侵入战垫及平行选项尺寸漂移 | 保留具名回归、全弹框合同与跨比例对抗矩阵 |

## 二、当前仍需完成的工作

| 优先级 | 工作项 | 可核验状态 | 下一出口 |
| --- | --- | --- | --- |
| P0 | 权威状态文档与台账收口 | 正式服 `08cd7af`、维护保留及更新日志注入已读回，正在记录部署后事实 | 提交并同步部署回执文档；不因文档提交重复部署业务版本 |
| P1 | 正式服候选发布 | 已完成；首次尝试在上传前由真实生产日志注入合同拦截，修复合同后重新完整 Release 并成功原子部署 | 保持维护，不执行启服／结束即时维护命令 |
| P2 | 正式服 Bug 关闭闭环 | 25 项工程修复已随 `08cd7af` 上线，但维护期无法完成全部原场景；3 项仍需真机；3 项产品裁定和1项性能归档结论明确。正式后台账号 `Aimin` 在生产认证失败，未越权改状态 | 取得有效生产后台授权并完成原场景/真机证据后再关闭；当前保持未关闭并逐项记录部署事实 |
| P3 | 旧工作树与分支退出 | 尚未执行；必须在正式发布与 Bug 状态落盘后先审计独有提交、未提交和未跟踪文件 | 先归档差异，再仅删除已结束、干净且无独有内容的工作树和分支 |

## 三、发布队列

1. **已完成的测试服出口**：`b7d9f63` 的 Release 规则 4766/4766、平台 205/205、UI 契约 348 项及正式/测试双构建通过；`/testrun/` 健康、HTTP、WebSocket 与提交一致。
2. **候选收口提交**：在 `f5b655e` 文档基线上加入 `1ea1b51` 对战修正并同步更新权威状态；不复用 `b7d9f63` 的旧制品冒充新提交制品。
3. **正式发布门槛（已完成）**：`08cd7af` 完整 Release、工作树干净、`origin/main` 精确读回；生产制品绑定 `fdb73f8 → 08cd7af`，更新日志已实际注入线上 bundle。
4. **正式服（已完成）**：健康、首页、卡牌页、WebSocket、版本与更新日志均通过；维护继续保留。
5. **部署后**：25+3+3+1 Bug 不以版本上线替代原场景/真机证据；后台认证失败时保持未关闭。旧工作树先做差异归档，只清理已结束、干净且无独有内容的目录和分支。

## 四、调度原则

- 同一时间最多一个共享代码写入批；方案评审和只读验收可以并行。
- 独立任务只交付干净提交和证据，Main负责重放、冲突处理、最终Release、推送和部署。
- 已进入主线的历史工作树不再代表待办；确认无独有提交、无未跟踪文件且任务结束后，才按Git正规流程清理。
- 排期按依赖顺序而非虚构日期；真实规则裁定、外部权限或生产部署授权是唯一允许暂停既定队列的输入门。
