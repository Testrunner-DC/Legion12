# 十二军团当前任务进度与排期

更新时间：2026-09-26。此表只列当前主线、仍有独有提交的工作树和现有专项任务的可核验状态；历史过程记录不重复列为待办。

当前唯一候选为 `b7d9f6306b9b6b518ce948325e7ed7ee01b2c485`。它已同步 `origin/main`，工作树干净，并于 2026-09-26 部署到 `https://legion-12.com/testrun/`；公网健康、HTTP 与 WebSocket 均精确匹配该提交。正式服仍运行 `fdb73f84d058a415842d071a7a4cb92776352556`，尚未切换。

## 一、已完成并进入主线

| 工作项 | 当前状态 | 验收/上线状态 | 后续动作 |
| --- | --- | --- | --- |
| 架构P0—P4 | 已完成 | P0卡效出口、P1内核边界、P2外围投影、P3持久化、P4规则中心与移动结构均退出；架构锁随本批接入本地与GitHub门禁 | 后续功能在架构锁内扩展；不自动开启P5 |
| 效果一致性与后台卡效工作台 | 已完成 | 324张卡、686能力段、78/78生命周期档案；工作台已进入主线和测试服 | 按现有能力身份与结构哈希继续维护，不另建旁路 |
| 低卡顿性能治理 | 已完成 | 全部治理批次已进入 `b7d9f63` 测试服；性能架构、请求可靠性、资源同步和动作门闩门禁通过 | 新功能继续受轮询、请求扇出、重试、限流和动作门闩约束 |
| 移动对战互动区与比例 | 已完成 | 全部修正已进入 `b7d9f63` 测试服；最新计时对抗 419 项断言、28 张截图通过 | 后续仅按具体 Bug 或明确新需求迭代 |
| 对局记录专项修正 | 已完成 | 统一事件事实与玩家投影已进入 `b7d9f63` 测试服 | 新的记录需求继续走统一事件事实与投影，不拼卡名文案 |
| 公开牌库、牌库存储与详情内容 | 已完成并进入主线 | 存储、浏览、详情、指南、版本、匿名统计、短码和二维码均已进入 `b7d9f63` 测试服 | 不再把旧交接中的“实施中/待接收”当作当前状态 |
| 后台、规则中心与赛事中心完整方案 | 已完成 | 独立路由/工作台、规则审核发布、赛事生命周期和恢复边界已进入 `b7d9f63` 测试服；Release 平台测试 205/205 | 正式部署后按原场景验收，不再以历史方案缺口重复立项 |
| 对战展示与选择流程 | 已完成 | 军团离场、动画顺序、比约恩加权多选/登场、移动选择弹框与计时布局已进入 `b7d9f63` 测试服 | 保留具名回归与对抗矩阵 |

## 二、当前仍需完成的工作

| 优先级 | 工作项 | 可核验状态 | 下一出口 |
| --- | --- | --- | --- |
| P0 | 权威状态文档与台账收口 | 测试服版本和正式服版本已读回；本文件、`HANDOFF.md`、任务台账及 Bug 收口表正在按唯一候选更新 | 提交并同步文档后，以新提交重跑 Batch/Release |
| P1 | 正式服候选发布 | 用户已明确授权；只能部署文档收口后通过完整 Batch/Release、工作树干净且远端精确读回的唯一提交 | 原子部署正式服并核验版本、HTTP、WebSocket、维护状态和更新日志 |
| P2 | 正式服 Bug 关闭闭环 | 25 项有工程修复、3 项需移动真机、3 项无需代码、1 项归入性能治理；当前正式版本仍为 `fdb73f8`，因此尚不能关闭已修项 | 正式部署后按原场景和具名回归逐项复测；证据不足项保持待复测 |
| P3 | 旧工作树与分支退出 | 尚未执行；必须在正式发布与 Bug 状态落盘后先审计独有提交、未提交和未跟踪文件 | 先归档差异，再仅删除已结束、干净且无独有内容的工作树和分支 |

## 三、发布队列

1. **已完成的测试服出口**：`b7d9f63` 的 Release 规则 4766/4766、平台 205/205、UI 契约 348 项及正式/测试双构建通过；`/testrun/` 健康、HTTP、WebSocket 与提交一致。
2. **文档收口提交**：仅更新权威状态和任务台账；完成后重新形成唯一候选，不复用 `b7d9f63` 的旧制品冒充新提交制品。
3. **正式发布门槛**：新提交完整 Batch/Release、工作树干净、`origin/main` 精确读回、玩家更新日志覆盖上次正式版本以来全部可见变化。
4. **正式服**：按本批明确授权部署新提交；验收健康、首页、卡牌页、WebSocket、服务状态和维护策略。部署失败由脚本自动回滚，不以测试服成功替代正式验收。
5. **部署后**：逐项处理 25+3+3+1 Bug；无线上或真机证据的报告不得关闭。最后归档并清理旧工作树、制品缓存和已结束分支。

## 四、调度原则

- 同一时间最多一个共享代码写入批；方案评审和只读验收可以并行。
- 独立任务只交付干净提交和证据，Main负责重放、冲突处理、最终Release、推送和部署。
- 已进入主线的历史工作树不再代表待办；确认无独有提交、无未跟踪文件且任务结束后，才按Git正规流程清理。
- 排期按依赖顺序而非虚构日期；真实规则裁定、外部权限或生产部署授权是唯一允许暂停既定队列的输入门。
