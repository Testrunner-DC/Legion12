# 效果一致性实施计划

## 当前验收状态（2026-09-17）

当前状态：主动次数显式登记0f4c192已干净Release并同步main，未部署。当前推进触发型主宰次数重置：8名主宰9段限次使用同一持久化键定义，神农鼎资格、选项、支付复验与结算同源。新增57例与相邻195/195通过，当前族Batch/提交级Release待执行。整体P0—P6仍未完成，待归属344项；次数覆盖不等于整能力生命周期完成。

本文件是执行进度的唯一入口；[完成定义](EFFECT-CONSISTENCY-COMPLETION-PLAN.md)负责固定P0—P6的验收条件。以下历史批次中的“下一批/待同步”是当时状态，当前工作以此节及最新交接为准。

### 2026-09-17 触发型主宰次数重置（执行中）

最终开发验证：Batch3769/3769、零失败/跳过、退出0，全池静态门禁通过。提交级Release与精确main同步待执行；不部署。下一族暂待信仰狂热者发动时机裁定：是否先询问本体发动，拒绝不占次数；确认后仍保留本体结算后选择免费主宰效果、分别响应的既有规则。当前自动本体入栈计次且结算后允许skip，既有测试保留次数；未直接修改。

- 扫描全池主宰明示限次触发与UsedAbilities读写：梅杰德2、奈芙蒂斯3、月读1、阿尔忒弥斯1、莫瑞甘1、安格斯2/3、嫦娥1、迦具土1，共9段。仅登记效果段和原持久化键，不增加限次或新原子；触发产生、计次、结算复验和重置同读L12MasterTriggeredUsageRules。
- 根因有两层：重置候选只枚举主动按钮；旧active-target把能力ID当卡牌对象，新的触发ID会在提交时被通用目标复验拒绝。改用现有option类型及ChoiceLabels，不放宽其他卡牌对象校验，文本直接取结构段Text，沿用现有弹框组件和布局。
- 初9项均复现“没有已使用效果次数”；补候选后8项又复现声明无法入栈，类型化选项修复后一并通过。新增57项覆盖9段正例/无已用次数、取消、费用前失效、结算失效、无效、Prompt和堆叠V2恢复、重复提交、预留键保护、安格斯独立两段、支付误选恢复取消及莫瑞甘真实触发候选重新开放。新增候选反射测试曾误写方法名，修正夹具后相邻195/195零跳过退出0。
- 已用次数与pending预留、临时持续状态分离；重置不会自动执行已过去的触发。试炼、阵营、随从自己的次数不因主宰卡展示引用而纳入重置。全族Batch与提交级Release待执行，不部署。

前批最终回执：0f4c19282510a9b719806b53d885b9cd8805fb31干净Release退出0，规则3712/3712、平台116/116、UI323/连接23、卡图324+42及前后端构建通过；main精确读回一致，未部署。证据artifacts/deploy/0f4c19282510a9b719806b53d885b9cd8805fb31/l12-release-0f4c19282510a9b719806b53d885b9cd8805fb31.json。下方主动批次“待Release”是过程记录，由此覆盖。

### 2026-09-17 主动次数统一（执行中）

最终开发门禁：追加四组共享键回归后新增130例，完整Batch3712/3712零失败/跳过、退出0，所有卡池静态守卫通过。首次Batch停在十字军旧文件位置断言；改为核对新登记的三分支共用crusade-choice及实际调用，不豁免共享次数规则。下列155为追加前定向结果。提交级Release和main同步待执行。

继续审计发现下一有界缺口：神农鼎旧实现只枚举主动按钮，未覆盖有明示限次的触发型主宰效果（月读、梅杰德等）；该问题独立于本批主动默认限次，下一批统一次数重置对象清单及异常矩阵。信仰狂热者按物理实例计次有BQ-20260830-01正式裁定，保留，不按名称擅改。

- 根因：通用门禁原默认所有非主动休整效果受到一次键限制；卡片各自写入。现仅显式登记产生限次键，按钮、Begin/Commit和重置使用同一规则；旧检查点残留的无限次键不再阻挡。四组共享分支维持共享，须佐之男两段不串限次，主动休整仍须支付休整。
- 全池扫描实际GetAbilities排除TriggerOnly后的111项；负例守卫找到12项会被旧键错误阻挡，但不声称12项都具有真实重复路径。真实索尔正常/无效后再次发动均先红后绿；格拉墨、明示限次须佐之男、孟婆/洛基共享重置及V2恢复、重复Prompt拒绝覆盖边界。
- 验证过程：初始3项可执行红测均失败；新增夹具曾错误使用Text/Options字段，另5项失败来自缺少合法高天原目标及天廷零士气额外触发，修正夹具而非产品规则，最终定向155/155零跳过退出0。神农鼎保留来源休整和返还Cost，已重置目标结算失败，旧无限次键不提供虚假候选。
- 本批不增加原子，不改变临时持续标记、触发预留键及网络去重。触发路径中的李牧、哮天犬、莫瑞甘、奈芙蒂斯、梅杰德、信仰狂热者已核对均有明示限次；全触发族次数归属继续按独立清单核验，不混称已全部验收。

| 验收项 | 当前结论 | 退出条件/剩余工作 |
| --- | --- | --- |
| P0 卡池台账 | 基线已建立，未完成 | 运行目录324张/692能力段，7张无能力卡单列。87项细原子定义、209项复合定义匹配、52项共用规则归属（5骑兵+47持续远程）；344项入口待核对。59项能力挂接100组精确具名引用；不据此自动标记整能力完成。 |
| P1 共用生命周期 | 部分完成 | 继续消除资格、费用和结果的独立解释；旧事件兼容映射已把`effect-cancelled`视为失败，不能把仅改事件名算成新增规则保障。 |
| P2 对象复验 | 进行中 | ST本批对象与公共转活跃原区域绑定已通过提交级Release并同步79dfdce。仍有来源状态、私区登场及其他旧入口待逐项盘点。 |
| P3 费用条件 | 部分完成 | 已有冒号Cost、支付回执、取消兜底；仍需能力级覆盖证据确认全部费用路径。 |
| P4 触发分支 | 部分完成 | 已有统一触发族；组合顺序、前段成功依赖、响应范围须覆盖全部能力。 |
| P5 展示同源 | 部分完成 | 核对各能力的图鉴、按钮、弹框、动效、日志、回放及公开等级；后台编辑工作台留在一致性验收之后。 |
| P6 恢复与发布 | 部分完成 | 已有V2恢复/重复提交回归；仍须逐类补齐缺口及最终提交级门禁。 |

提交规则：同族相关子项集中验证、代码/测试/记录同批提交；不为单个入口或补记回执重复提交与完整发布验证。无人并行也保留已有未纳入本批的工作区修改。只有最终退出码和测试汇总均已核实才登记通过。

原子规则：“抽牌后强制弃牌”复用抽牌、选择、弃牌基础操作，属于同一能力内的顺序组合。`L12AtomicAbility`为能力容器；组合计划和展示子段不是新基础原子，也不因此自动增加响应窗口。

## 2026-09-17 印刷远程持续能力归属（已验证、已同步，未部署）

最终回执：a884a9a3c3bd6ce080321017bda781610051a48e同提交完整重跑Release退出0，规则3582/3582、平台116/116、UI323、连接23、卡图324+42及前后端构建通过；origin/main已精确读回同一完整SHA。证据D:\GPT\Legion12\artifacts\deploy\a884a9a3c3bd6ce080321017bda781610051a48e\l12-release-a884a9a3c3bd6ce080321017bda781610051a48e.json。未部署；以下未同步/重跑中为过程状态，由本回执覆盖。回执留本地随下批归档，不独立提交或重复验证。

提交a884a9a3c3bd6ce080321017bda781610051a48e首轮干净Release：规则3582/3582、平台116/116及UI323/连接23通过，但前端npm在排名刷新检查附近以-1073741819原生异常退出，Release退出1，未推送。Node v22.18.0；同项独立3次均退出0，近期应用日志查询未返回可用Node崩溃模块证据。未改代码或豁免门禁，正在同提交完整重跑；与8112a9c过程中的同位置异常重复出现，根因仍未定位，不记为已修复。此回执随下一批归档，不另造文档提交。

范围为47个已复核的精确能力身份，不含相邻主动付费段、临时授予或未知卡。共同入口覆盖GetCombatRuleAbilities、CombatProfile、合法进攻候选、进攻提交与伤害结算。档案显式说明持续能力无独立入栈/费用/响应；不能把这项不适用解释成进攻事件不可无效或不用检查重复请求。

首批47卡前/后排条件、实际权威候选、无军团候选、重复读取无副作用、V2恢复全部通过；四种条件的真实战斗伤害代表例另补，不把预览检查冒充结算期恢复。台账按协议登记额外检查，消除原先所有档案硬套骑兵目的地检查的问题。47段归属从待核对转为共用入口不等于47段全部验收；能力总数692不变，待归属391→344。

代表战斗第一轮50通过/1失败，原因是测试忽略源义经卡文明示后排进攻时视为2000；修正独立期望参数、保留双方伤害断言，未改运行规则。最终本族51新例及相邻156/156、台账9/9退出0；四例均实际进攻、响应通过、远程无反伤且重复进攻拒绝。完整Batch3582/3582零失败/跳过、退出0，全池静态门禁通过；提交级Release待完成。本批没有修改生产代码，测试/审计范围的Batch不要求重复前端构建，提交级Release仍执行完整门禁。

待裁定：扫描相邻主动次数规则，雷神索尔卡文没有回合1次，但thorCharge写入一次键。正式规则未找到主宰统一次数上限；已询问是否将“除卡文/规则明示限次外，满足条件和费用可重新发动，包括被无效后”推广全池。当前不擅改索尔或其他未裁定次数。

## 2026-09-17 后排付费扩展与支付取消（已验证、已同步）

最终回执：5c228ea185e28b8343dfe5f19624f5855d8439fe干净Release退出0，规则3530/3530、平台116/116、UI323、连接23、卡图324+42及前后端构建通过；origin/main精确读回一致。证据D:\GPT\Legion12\artifacts\deploy\5c228ea185e28b8343dfe5f19624f5855d8439fe\l12-release-5c228ea185e28b8343dfe5f19624f5855d8439fe.json；未部署。以下待验证状态由本回执覆盖，回执随下批归档，不为回执单独提交/重跑Release。

次数冲突已获用户明确裁定：攻城投石车与养由基均按卡文、能支付即可重新发动；旧检查点留下的一次键也不得阻止新合法发动。按钮与两次提交检查共用次数判断。两卡共享参数记录同时提供完整主动段、Cost、按钮、支付数值与结算权限；单段动效/结果使用既有single-active，不新增操作原子。

养由基原本与投石车共用“可进攻后排和主宰”字段，真实进攻回归确认会错误进攻主宰。现新增独立仅后排权限；军团候选、正式进攻和天灾强制目标均读取同一权限函数。权限覆盖V2恢复、克隆、晋升继承、回合结束和离场清理；未使用的新字段不序列化，避免无关旧状态形状变化。结算来源不在我方战场/已非军团则通过共同失败结果结束，已付Cost不退。

支付误选回归进一步复现主动返还士气、主动消耗资源两条公共预支付Prompt缺少取消。现在两条在入栈前均可取消；效果阶段强制返还不能取消。沿用现有底部按钮与确认按钮相邻，文案读取服务端的“不发动”，打出流程仍为“取消打出”，不改布局。

初始12例11失败/1通过；追加返还取消、消耗取消各复现1次失败后修复，最终21个新例及相邻287/287、台账8/8退出0。16组引用精确绑定两项能力；重复取消/Prompt拒绝与主动重新发动分别测试，不把合法再次发动等同网络重复请求。非军团源为最小状态夹具，离场清理/晋升继承另属内核单测，不冒充完整变身/晋升链。原远程合成反例在宿主迁入人工结构后改为直接验证分段/合并函数；47张真实卡集成守卫保留。

验证过程：首轮Batch3529通过/1失败，唯一失败是单段主动呈现精确清单未登记本次两项；补入两项并保留全量精确断言后定向83/83。旧UI检查仍硬编码“取消打出”，改为检查原位置读取服务端文案，保留取消对象排除/底部位置断言。最终Batch3530/3530、零失败/跳过、退出0，全池静态门禁、UI323和前端构建通过；提交级Release及同步待执行。

## 2026-09-17 远程基础能力身份与费用隔离（已验证、已同步）

最终回执：1158bbe5072ec7f9fc54d724c2551f09e3ff1c29干净Release退出0，规则3509/3509、平台116/116、UI323、连接23、卡图324+42及前后端构建通过，main已精确读回同一SHA。证据D:\GPT\Legion12\artifacts\deploy\1158bbe5072ec7f9fc54d724c2551f09e3ff1c29\l12-release-1158bbe5072ec7f9fc54d724c2551f09e3ff1c29.json；未部署。以下待Release和待裁定为过程状态，由本回执与上方用户裁定覆盖。

扫描运行目录所有包含“进攻距离+1，远程进攻无损”的47张卡。自动分段删掉句号后，与战斗覆盖表的精确文本比较失败，导致重复；攻城投石车/养由基/秦良玉还混入后段费用或条件，锡瓦的卡巴的“对方”被留在前段。珀涅罗珀人工段使用continuous时点，又被static覆盖表重复追加。

仅对已登记且卡文开头完整匹配的远程基础句先建立原有结构段，再拆剩余卡文；不从嵌入文案、临时授予或未知卡推导新战斗规则。目录和CombatProfile使用同一远程覆盖匹配，人工结构优先，保留前后排差别及连续能力原身份。22张受影响、19个重复段移除，费用/条件剩余段完整保留；本批没有改变实际支付、进攻权限、次数或日志布局。

新51例初始26失败/25通过；修复及4个不误合并反例后相邻140/140、台账8/8通过。两例合成反例曾选用必须有登场动效的卡号导致夹具失败，改用无该依赖的远程宿主，不改产品逻辑。最终Batch3509/3509、零失败/跳过、退出0，全池静态门禁通过；提交级Release待完成。

下一族待裁定：攻城投石车现行卡文没有“回合1次”，但20260831-170记录与TryCommitS1ExtendedActiveAbility均明确保留一次键，Begin/Commit会拒绝重复。询问按卡文移除限制，还是保留并补卡文后再推进其主动结构归属。同时排队核验养由基共用CanAttackBackAndMasterUntilTurn是否错误开放主宰目标、以及本族结算来源离场/前后排变化；这些尚未计入本批通过范围。用户已明确先不部署，不再为本批追问部署。

## 2026-09-17 试炼推进公共记录隐私（已验证、已同步，未部署）

最终回执：8112a9c228e7630e1a468b2e3d82bc0e2d9a0a51干净Release退出0，规则3454/3454、平台116/116、UI323、连接23、卡图324+42和前后端生产构建通过；已推送origin/main并精确读回一致，包含父提交b79870f骑兵归属批次。证据：D:\GPT\Legion12\artifacts\deploy\8112a9c228e7630e1a468b2e3d82bc0e2d9a0a51\l12-release-8112a9c228e7630e1a468b2e3d82bc0e2d9a0a51.json。未部署，仍需本批上线授权。以下待验证/同步为过程记录，由本回执覆盖；回执留本地随下一批归档，不独立提交或重复Release。

发布验证过程保留：首轮Release前端npm进程原生异常退出（-1073741819），Release退出1，当时未推送。排名刷新检查随后独立3次通过，未改业务代码或豁免门禁，同一提交完整重跑最终退出0。异常未复现，不声称已定位环境根因。

用户插入的隐藏信息问题已优先处理：所有试炼推进共用AdvanceTrial，旧记录在文字中写明被推进试炼、source为空时还附带试炼实体。现由共享输出过滤器同时保护实时双方/观战RecentEvents与LastAction、玩家回放Events/LastAction/Log；保持简洁的“试炼进度 0 → 2”。正式完成后的揭示保留，达到8但尚未完成仍不公开。

不改写原始事件/存档，不改原命令重放结果与哈希；内部管理审计仍保留完整记录。普通8种试炼来源逐一遍历目录全部试炼，另补无来源、旧事件恢复、旧版回放及V2实际命令重放；专项/相邻49/49通过，退出0。回放夹具先前错误共享可变卡对象，现按真实事件克隆冻结，不把夹具错误当作产品哈希问题。最终Batch/Release及同步待完成，未部署。

最终Batch已取得3454/3454、零失败/跳过、退出0及全卡静态门禁通过。骑兵归属已单独提交b79870f；试炼隐私提交仅纳入输出过滤器、两个调用点、具名回归和本次记录。接下来对最终集成提交一次干净Release，未获本批部署授权，不切换线上。

## 2026-09-17 非堆叠规则动作族归属（已验证、已同步）

最终回执：b79870f已随8112a9c完成干净Release及main精确同步，验证明细见上节。P0仅5项入口归属完成，整体效果一致性仍未完成；未部署。

插入优先项：用户报告试炼推进日志泄露未完成试炼身份。已定位共用AdvanceTrial事件包含试炼名称、无来源时附试炼卡；先修对外事件/快照/玩家回放边界，再恢复本批归档。两项分别记录，不互相冒充完成。

范围与退出条件：全池原生骑兵位移精确为齐格鲁德、源义经、珀尔修斯·晋升、赵云、莫德雷德5段；将各段绑定现有共用规则入口和适用性档案，补齐逐卡按钮/命令、唯一空位、无空位、来源/位置过期、V2前后恢复及重复提交。不是新增原子，也不重写已正确的位移结算；勾玉等效果赋予位移及位移后的触发属于不同档案，不借用原生动作证据。

最初新增测试夹具有两处init-only属性赋值编译错误，已改为明确的最小状态替身；此编译错误不计规则缺陷。可执行红测29项中24通过、5失败，实际失败均为按钮与提交各自维护时点拒绝文案；现已提取共同原因。类型/职介替身仅验证过期权威状态拒绝，不冒充真实变身连锁。

逐能力台账升级加入精确结构哈希绑定的共用入口和不适用理由；原生规则动作不进入响应堆叠，因此被无效及入栈后对象失效不适用，改验证提交时来源/位置复验。5项从owner-unreviewed转为shared-rule-owner，未核对415→410，总能力711不变。台账8/8、相邻58/58通过，退出0；具名测试引用仍不等于提交级执行回执。最终Batch/Release与同步待完成。

集成验证回执：骑兵与插入的试炼隐私两项完成后，隔离副本Batch3454/3454、零失败/跳过、退出0，全卡静态门禁通过。两项分别提交，最终集成版本仅做一次干净Release与同步；不为文档回执另跑发布验证。

## 2026-09-17 抽牌/弃牌逐张结算（已验证、已同步）

最终回执：`8554994cb31d8dcd34b1ee4587656f4f4199fa72`干净Release退出0，规则3419/3419、平台116/116、UI323、连接23、卡图324+42与前后端生产构建通过；main精确读回一致，未部署。证据`D:\GPT\Legion12\artifacts\deploy\8554994cb31d8dcd34b1ee4587656f4f4199fa72\l12-release-8554994cb31d8dcd34b1ee4587656f4f4199fa72.json`。以下“待Release”为当时过程状态，由本项覆盖；回执留本地随下批集中归档，不单独提交或重跑Release。

按用户新裁定更换旧“数量不足则整次不移动”的规则基线：共用Draw/Mill逐张移区并逐张通知；遇空库保留已完成卡牌，结束当前抽弃并判败。零次操作不产生记录或败北，负数请求不移动。Cost预先检查完整可支付的规则继续保留。

用户随后明确逐张原则用于处理特殊边界，不要求逐张写日志。因此玩家日志仍按本次实际完成数量合并；内部逐张事实/触发与日志展示分开。新增单张回调观测确保第二张移动前已处理第一张，不用日志条数冒充逐张结算。最初按逐条日志编写的预期已按此补充修正。

旧基础抽/弃牌不足测试与奥拉夫二世抽2后弃1场景已按新裁定更新：剩1先入手，再因下一次抽牌遇空库判败，不能打开弃牌Prompt。补齐夺命诗人/神剑格拉墨0或1张牌不足支付Cost的4场景和部分完成恢复的2场景后，最终相邻146/146、Batch3419/3419，零失败/跳过、退出0，全池静态门禁通过；提交级Release与同步待完成，不沿用前版完整测试数。

全池共用调用扫描：`L12LibraryOps.Draw/Mill`只有权威抽牌、磨牌门面和海拉单张Cost三处；磨牌覆盖登场费用、贝奥武夫、格拉墨、夺命诗人、英灵殿、瓦尔基里、凯恩、玛格丽特、密米尔之泉及GM。手牌移除/弃置按单张或选择顺序循环处理，没有整批Hand.RemoveAll/Clear弃牌实现需要替换；换区、重洗和打出不是弃牌，不混改。主引擎已有异画修改保持独立，本批仅改变Draw/判胜防重复片段。该批不声称完成所有牌库查看/检索/费用和P0—P6剩余能力验收。

## 2026-09-17 反击条件抽牌与所有者区域边界（已验证、已同步）

最终回执：`d2134b917e727722c64d7f97c0d39991e9deb601`干净Release退出0，规则3391/3391、平台116/116、UI323、连接23、卡图324+42及前后端构建通过。已推送main并精确读回一致，未部署。证据`D:\GPT\Legion12\artifacts\deploy\d2134b917e727722c64d7f97c0d39991e9deb601\l12-release-d2134b917e727722c64d7f97c0d39991e9deb601.json`；回执随本批归档。

目标：承接上一批的同族缺口，不新增基础原子；将条件失效、抽牌失败、没有处理对象、选择不执行及实际所有者区别清楚。完成条件为本节具名矩阵、全池匹配守卫、隔离Batch和提交级Release全部取得最终结果，记录并同步。

全S1/S2/ST扫描现行18张反击的`若.*可抽取1张牌`，只有战斗至黎明、空城计两项，已由`ResolveConditionalResponseDraw`共同处理。声明后条件失效记failed，未选择抽牌根本不建立该段，军团增益无对象记skipped。空城计结算时必须仍有原进攻事件；本段失败不影响原本独立的条件抽牌段，不擅自改成“随后”依赖。正式规则`officialRules.ts`规定需要操作空牌库时败北，真正执行抽牌且牌库为空时记失败并判败；未选择/条件失效不操作牌库。

扫描所有`PutOnTop`和返回所有者牌库顶部入口：桂小五郎/草薙剑已用权威区域事务；墓地回库和牌库整理有既有路径。粮草掠夺存在手牌持有者代替实际所有者的错误，现复用基础区域移动的来源/目的玩家分离参数，并保留`CardOwner`的旧存档回退。反击弹框、场景和日志均写明所有者，匿名返回日志不公开卡名或实例。未改公开离场触发协议，也不声称完成所有私区移动/空牌库操作审计。

新增17个响应恢复场景，首轮10失败，其中2项是不发动时不应生成结算记录的测试预期错误，另外8项复现实际错误；修复后连同新增全池守卫、区域同源/跨所有者与重复输入保护，相邻92/92通过，台账6/6通过。下个缺口仍包括公共无条件抽牌的空牌库统一判败、反击链嵌套时点与全部类型的能力归属；当前Batch/Release待完成。没有改UI布局，没有部署。

继续核对嵌套响应发现已保存的`authorityTarget`未被消费：空城计直接查中间反击，S2沿中间层再寻根，导致中间层消失就错误失败。统一`DeclaredResponseTimingTarget`优先读取已声明原事件；原事件消失不重选，新存档中间层消失不影响原绑定，旧存档缺字段时保留链查找。空城计、破败仪式、毒药发作新增9场景，补齐实际堆叠目标选择后4失败/5通过；修复并加入3个旧存档案例后相邻149/149、台账6/6通过。前版Batch3379/3379退出0，新增绑定修复后仍须最终重验，不能沿用前版通过。

最终隔离Batch 3391/3391、零失败、零跳过、退出0，全池静态门禁通过；149项相邻回归和6项台账检查通过。提交级Release和同步待执行，不部署。

下一阶段规则已由用户裁定（2026-09-17）：所有抽牌、弃牌动作本质上逐一进行；牌库剩1张而要求抽2张时，先抽剩余1张，再因下一次抽牌遇空库判败。此裁定覆盖旧`DrawIsAtomicWhenLibraryIsTooSmall`等“整批不足则不移动”的实现基线。下一批按共用抽牌/弃牌操作、逐张事件和部分完成结果推进；Cost仍先校验全部可支付，再逐张支付，不能把费用不足改成先付一部分。零次操作不触发空库败北，不为每张牌额外创建响应窗口。

## 2026-09-17 反击身份与具名证据归属（已验证、已同步）

最终回执：`efccebdb52645f8c2e52147b7e03356d9f7940bc`隔离Batch与干净Release均退出0，规则3359/3359、平台116/116、UI323、连接恢复23、卡图324+42及前后端生产构建通过。main已精确读回该提交，未部署。证据`D:\GPT\Legion12\artifacts\deploy\efccebdb52645f8c2e52147b7e03356d9f7940bc\l12-release-efccebdb52645f8c2e52147b7e03356d9f7940bc.json`；本回执随下一批归档，不单独提交。

通过逐能力清单发现S1反击卡被按自身进攻/登场/离场时点切段，出现只有“对方”“我方军团”等前缀的假能力。全池扫描现行18张反击身份，S1的11张均纠正为完整反击能力：绝对防御、拼死反抗、落穴陷阱、伏击、战斗至黎明、摄政皇权、空城计、不朽之礼、智慧法典、复仇血鹰、切腹仪式。已有人工分段（S2/ST）仍优先；不改变实际响应窗口、Cost或分支顺序。

`L12CounterTacticRules`现在为放置/响应识别与目录兜底提供同一反击身份表，避免又维护一份卡号清单。全池能力分母711=原719去除8个假段，已匹配定义并不等于生命周期通过。修复前12项新回归全部失败；修复后相邻125/125及台账6/6通过。旧智慧法典用例的play时点改成真实reaction，原Cost反例断言保留。

四种公共反击（战斗至黎明、空城计、破败仪式、粮草掠夺）的声明提交/重连/重复提交/结构化响应事件已通过具名用例绑定精确能力ID和结构哈希。引用方法必须存在、启用且包含对应参数卡牌；ID过期或同卡其他段不能继承证据。声明期恢复不冒充结算期恢复，代码引用不冒充执行回执；整能力无效、结算时对象失效等矩阵仍待逐项挂接/补测。本批Batch与干净Release待执行，不部署。

随后继续核对同族结算，发现破败仪式匿名手牌/军团失效静默成功，粮草掠夺返牌失败仍抽牌，毒药发作无效转活跃失败仍要求弃牌。三项现复用共享failed结果；后两项复用`RequiresPreviousSuccess`，前段失败/无效时后段skipped且不生成错误Prompt。新增13个真实响应、状态改变、V2恢复场景，修复前7失败/6通过，修复后13/13和相邻86/86通过；已关联准确具名结算证据（包括实际区域/兵力、无效状态和重复提交）。非军团反例为最小状态夹具，不冒充完整变身卡连锁测试。

最终隔离Batch 3359/3359，零失败、零跳过，退出码0；全池静态门禁通过。开发工作区的最终Batch曾因本批之外的账号代码引用不存在的AccountRow.UpdatedAt而编译失败，未覆盖或修复该独立改动；以e17b46a加本批15文件的隔离副本重新验证通过。提交级干净Release及同步仍待执行。后续待核对：S1条件抽牌在条件逆结算失效后的结果状态、空城计原进攻离栈、回库的实际所有者边界及完整同类扫描；这些未纳入当前通过范围。

## 2026-09-17 P0 逐能力台账基线（本批已验证、已同步）

[逐能力清单](l12/EFFECT-ABILITY-INVENTORY.md)直接读取实际`L12Catalog.AtomicEffects`，不复制一份卡牌规则，也不从卡级测试数推断能力覆盖。每行保存稳定能力ID、当前正文、Cost、原子顺序、运行定义匹配和场景数；完整JSON产物保存所有参数、场景、路由候选和逐项待核对维度，位于治理缓存的`effect-lifecycle-inventory/effect-ability-inventory.json`。

当前目录为324张/719段（包括ST，不能直接与旧S1/S2的577段分母比较）；7张无能力卡单列。87项与细原子程序的顺序/参数匹配，200项存在本能力显式Flow匹配；其余432项仅表示入口归属待核对，不表示未实现。静态、替代、规则动作与主宰主动入口不能靠同卡同触发候选自动判为完成。目录的时点/分段标签也须审查，当前台账不据此改游戏裁定。

最终回执：`e17b46a9eb033b96071ee1d09e66ec2027c86e91`已通过干净Release（退出码0）：规则3333/3333、平台116/116、UI323、连接23、卡图324+42及前后端生产构建；main已推送并精确读回。证据`D:\GPT\Legion12\artifacts\deploy\e17b46a9eb033b96071ee1d09e66ec2027c86e91\l12-release-e17b46a9eb033b96071ee1d09e66ec2027c86e91.json`。未部署。本地回执随下一批归档，不另建回执提交。

验收边界：本批只交付可再生基线及防串段/漂移检查，不新增运行时原子、不改变卡效或UI。正例、无目标、无效、目标失效、重复提交、重连与展示消费者目前均为“待核对”；有费用/对象选择的能力另列取消兜底及唯一候选等适用性。下一步按同族一次性补具名用例引用与生命周期归属，不能将已有测试一律抹掉或将未核对项当通过。定向5/5、最终Batch 3333/3333及全卡静态门禁通过，均退出码0；按本批统一提交后执行一次干净Release，回执随下一批归档。未部署。

已补齐此前遗漏的路线图记录：`e621756`/`5a3b9b8`统一敌方目标全失效及部分结算；`040fc80`/`bffb72a`统一胡夫、乔泽、弗蕾迪斯、伊丽莎白、莫德雷德、布狄卡目标结果与唯一目标点击。此前交接证明它们已同步，当前本地HEAD及缓存远端为`bffb72a`；本任务没有据此宣称上线。

## 2026-09-17 P2 启动卡组剩余目标/来源结算复验（本批已验证、已同步）

当前有界批次覆盖`L12StarterRemainingEffects`的7处旧取消分支、雅典娜前排增益及〈侵略如火〉独立来源失效场景。涉及ST06-03、ST06-M1、ST03-10、ST04-10、ST05-10、ST05-M1。扫描还发现赵云/诸葛连弩兵来源缺失、光之剑主动目标、沙漠送葬私区事务等其他入口，故不能宣称整个处理器或P2已完成。

统一规则：已声明场上对象在结算时必须仍处于权威战场、仍是军团、仍满足阵营/前排/远程等当前条件；士气必须仍在士气区；叠放效果还必须确认来源仍在结算区。无声明对象仍按原规则静默跳过；已声明对象或必需来源失效统一记为`failed`，并且不退款。多目标雅典娜保留“部分失效、其余继续”，全失效才为失败。

同批加入公共`effect-ready`区域绑定：创建时记录field/relic/morale，响应后仅在原区域查找。旧存档缺少区域字段时保留原兼容查询；此限制已明确，不能声称旧存档也具有完整区域绑定。三类对象覆盖正常、换区、提前活跃、被无效，每项经过V2恢复及重复提交检验；雅典娜追加真实支付、响应、重连后的全/部分失效和无对象跳过。

定向64/64、Focused 3327/3327已取得退出码0；补入无对象回归后的最终Batch 3328/3328、全卡原子审计和规范门禁通过。前一轮缺少最终退出结果的通过说法已由这些实测证据替代。代码/回归/记录合为一个提交，随后执行一次提交级Release；该提交的发布验证结果保存在`artifacts/deploy/<完整提交>/l12-release-<完整提交>.json`，同步结果补入本地交接并随下一批集中归档。未部署。

最终回执：提交`79dfdcee4eb450c67c14a4e6b15ffa3e52008eca`在干净副本通过Release（退出码0）：规则3328/3328、平台116/116、UI323、连接23、卡图324+42及前后端生产构建。证据`D:\GPT\Legion12\artifacts\deploy\79dfdcee4eb450c67c14a4e6b15ffa3e52008eca\l12-release-79dfdcee4eb450c67c14a4e6b15ffa3e52008eca.json`；已推送main并精确读回。未部署。下一批先建立逐能力的可重复导出台账，给剩余工作确定分母。

## 2026-09-17 抽牌后强制弃牌全卡池生命周期防回归

按完整句型`抽取1/2张牌，并弃置1张手牌`复核S1/S2/ST卡池，精确命中黑胡子蒂奇、传奇的拉格纳、奥拉夫二世、洛基、雷神之锤和赫拉克勒斯6项。它们已共用`draw-discard-draw-1/2 → draw-discard-discard`组合计划：整项只开放一次响应，第二段只在第一段`resolved`后建立私有手牌选择，即使唯一也必须点击；抽牌被无效或牌库不足时，第二段记为`skipped`且不留下阻塞Prompt。

本批新增全池回归守卫，不再靠逐张人工清点。它读取印刷句型，要求每张卡绑定两个共享Flow、第二段显式依赖前段成功、两个展示段属于同一原子能力，且不存在`Legacy`回退；未来新增同形态卡未迁移会直接失败。洛基的同卡另一主动能力使目录汇总状态不能代表本能力，因此守卫验证实际段身份与运行路径，而不误用卡级标签。定向18/18、Focused、Batch与提交级Release均通过；提交级Release证据`D:\GPT\Legion12\artifacts\deploy\ef5dd2ad4c3c75a776ee9ceadd6d287a49fa8991\l12-release-ef5dd2ad4c3c75a776ee9ceadd6d287a49fa8991.json`，待同步，未部署。

## 2026-09-17 天灾开场触发与主动触发来源分离

天灾“开场触发”现只指回合阶段自然增长导致的天灾触发；“主动触发”现只指打出带天灾等级军团或使用卡牌导致的触发。PendingDisasterTriggerSource 保存 turn-phase、card-effect 或 gm 来源，延后排程、Stack、日志、回放和V2恢复均读取该同一事实；GM调试单列，不冒充卡牌效果。

〈诸神黄昏〉不再以“是否第1回合”猜测分支：任何回合阶段自然达到阈值，所有玩家各抽2；军团/卡效主动跨阈值才令其余玩家各抽2、结束当前回合并追加回合。后续新增天灾若需要按来源分支，必须读取该字段而不能解析文本或回合号。

## 2026-09-17 支援阵亡与击杀事实分离

支援军团阵亡建立其自身【阵亡时】触发；但支援不是进攻军团以战斗伤害击杀被进攻对象，进攻军团的【击杀时】不得因此触发。支援结算显式使用`Defeat`离场，仍走通用阵亡候选；它不写入`Defeated*InstanceId`，所以战斗时间线不创建`CombatDamage`或`kill-source`。武则天支援、源义经进攻的V2恢复样例固定“入墓→抽牌→治疗、没有击杀来源”。

## 2026-09-14 进攻时自身单段兵力增益统一

贝奥武夫、古斯塔夫一世、奥德修斯、彭忒西勒亚、斯巴达勇士和帕西瓦尔已经归并到`L12SimpleSelfTroopBuffTriggerEffects`。同一规格供应能力序号、真实时点、公开声明计划、冒号前Cost类型、增益数值、按钮、响应效果、结算日志和`single-trigger`场景；六个旧逐卡增益分支删除。

统一生命周期为可支付检查→明确发动→费用对象选择/支付→响应→来源实体复核→本回合兵力修正→结果。错误费用选择不销毁Prompt并可“取消整次发动”；费用预付后被无效仍保留支付但不增益；来源在响应中离开战场则`failed`且不修改冻结快照；V2恢复和重复提交只能结算一次。细原子Cost带`prepaid=true`，既供后台、响应和回放读取，也不会在结算期重复收费。功能提交`d0034f66236cd5875cf02440adfb9e91268078c1`通过专项161、Focused/Batch规则3300和全池原子审计，未部署。下一批按新增裁定扫描天灾触发来源。

## 2026-09-14 单目标军团状态触发统一

尼托克丽丝登场令〈陵墓守卫〉活跃、神箭奥德尔阵亡令对方军团休整、疯狂的爱丽丝击杀后令自身活跃、安提诺乌斯登场令【奥林匹斯】军团活跃，已经归并到`L12SimpleCardStateTriggerEffects`。规格同时供应能力段、真实时点、可/必发、条件、目标范围、回合次数、按钮/Prompt、响应正文、实际结算日志和`single-trigger`场景；旧登场/6I-B/ST逐卡分支已删除。

战场对象即使只有一个合法目标也必须点击；“此军团”固定对象不增加多余目标步骤。候选时没有对象则静默，选发拒绝不入栈，入栈后在逆序结算与独立`effect-ready`响应结束时分别重验同一实体的区域、类型/阵营和活跃/休整状态。目标失效记`failed`且不改选；被无效不改状态；V2恢复和重复提交只完成一次。功能提交`122f458aac8d9057d930d1825a98477abe1e712d`通过Release规则3280、原子审计122程序/108卡、平台116、UI323、连接23、卡图324+42和生产构建，未部署。下一批继续从“完整结算形态”扫描单段状态修改，不把带Cost、多结果、士气对象或临时状态的能力粗合并。

## 2026-09-14 登场获得符文统一

梅林、帕西瓦尔、高文、阿麦金、伊丽莎白·都铎、加雷斯与纯白的灵鹿已经从4份手写必发程序和旧6I-A选发解释器归并到单段资源规格。4项无目标必发效果直接进入响应堆叠；3项选发效果先明确声明是否发动，拒绝不生成空堆叠。统一规格同时供应卡牌/能力段、可/必发、原子程序、Prompt、实际结算文案、日志和`single-trigger`场景。

声明器新增零步骤保护：必发且没有目标、费用或模式时，不建立空`PendingActivation`，从结构上消除支付/选择流程无法继续和恢复后卡死的风险。功能提交`bab5457f1b8d9630997f38911caaf8a445cd346f`通过相关359、完整规则3268、全池248张/577项审计和完整Release；原子真实覆盖更新为119程序/105卡，未进入两类原子路由为91卡且无权威入口仍为0。证据`D:\GPT\Legion12\artifacts\deploy\bab5457f1b8d9630997f38911caaf8a445cd346f\l12-release-bab5457f1b8d9630997f38911caaf8a445cd346f.json`，未部署。下一批继续收敛单结果“转为活跃/休整”，带Cost、目标或后续增益的复合段分别建模，不按关键词粗合并。

## 2026-09-14 单段资源触发统一

哮天犬·稚、阿塔兰忒、阿尔忒弥斯、莫瑞甘、李牧、〈寻找圣杯之旅〉、安格斯·麦·奥格、孙悟空和天廷士气共用`L12SimpleResourceTriggerEffects`。同一规格生成能力段、真实时点、可/必发、次数预占、候选/结算条件、士气目标、资源操作、按钮/Prompt文本、堆叠文案、日志及`single-trigger`场景；运行时不再进入6I-B、6G-A、6J-B或逐卡资源分支。

统一协议先过滤公开候选，再完成玩家选择与次数提交，随后开放响应；逆序结算时重新验证当前条件及声明目标。无士气牌库或无合法士气时静默不入栈；必发阿塔兰忒即使只有一个士气也要求点击；目标被响应改变为神力时结果为`failed`；被无效不改变资源；V2恢复只结算一次。孙悟空的资源段是“作为军团离场→返回主宰区”能力的后续子段，后台不增加虚假第三效果。

能力目录同步纠正莫瑞甘2项、安格斯3项、〈寻找圣杯之旅〉2项，并新增可执行翻转士气原子。功能提交`85f770e9b5d437c3f61ab92ce9716a4b7bb80258`通过专项、相邻400、Focused/Batch/Release规则3268、平台116、UI323、连接23、卡图324+42及生产构建，未部署。下一批继续按“单一结算结果”扫描转活跃/休整、恢复资源与状态写入，不按资源关键词合并复合效果。

## 2026-09-14 触发效果实际Cost回执与无冒号效果链

公开触发声明已接入与主动效果相同的支付快照协议：只有声明流程真实改变的权威状态才会生成`Cost（已支付）`。回执保留实际卡名、区域、数量与资源类型；作为费用被弃置的叠放卡递归纳入，同名卡按数量归并。对手响应不再读取候选的简写路由名，而是读取当前实际能力段；已经生成回执的冒号前Cost会从效果栏移除，费用与效果不重复。

全池复核按用户确认的语义边界执行：只有冒号前支付才是卡效Cost。吕布的返还4士气、高文的消耗X符文、芬尼亚传奇的消耗X符文都没有该边界，因此均属于效果，不能在响应前支付。它们被无效时不改变资源；正常结算时重验当前对象和支付能力。吕布的返还与转活跃、芬尼亚的符文消耗与全部目标减兵力分别作为不可部分成功的一条效果链处理。

古斯塔夫、理查德等真实冒号Cost继续在声明提交后、响应前支付，并由公共状态差投影实际墓地回库卡或叠放侍从数量。功能提交`5346f70deeee82034c6ed330ccb44119fcc9a1aa`通过专项、Focused、Batch与Release规则3263/3263；全池原子审计覆盖248张卡、577项能力且疑点为0，平台116/116、UI323、连接23、卡图324+42及生产构建通过。证据为`D:\GPT\Legion12\artifacts\deploy\d18a18f83bbbb7ad2be7d318a639b0309baf35d3\l12-release-d18a18f83bbbb7ad2be7d318a639b0309baf35d3.json`，本批未部署。

## 2026-09-14 主动Cost手写摘要归并

全量检索主动效果的`paidCostSummary`写入点，清除天照两能力、洛基、英灵殿、花魁与努阿达共6处单卡手写摘要。这些入口现全部由统一支付前后状态差生成回执：英灵殿显示实际返回牌库的两张卡名与来源休整，天照显示实际弃牌，洛基显示真实消耗士气，努阿达显示真实消耗符文，花魁显示实际休整来源。

主动路径中只保留阿麦金的显式回执，因其“展示牌顶”是已支付但不造成区域/数值差的公开Cost；它由已锁定的真实牌实体生成，不是手写模糊摘要。子动费用回执在V2检查点中冻结，重连后继续展示原支付事实。

功能提交`00de3c6fbcefa683bfc3be1de8f5193ffd33c807`通过相关与重连回归85/85、Focused/Release规则3260/3260、平台116/116、UI323、连接23、卡图324+42及前后端生产构建；证据为`D:\GPT\Legion12\artifacts\deploy\00de3c6fbcefa683bfc3be1de8f5193ffd33c807\l12-release-00de3c6fbcefa683bfc3be1de8f5193ffd33c807.json`，未部署。

## 2026-09-14 全时点印刷冒号Cost边界守卫

原子目录新增跨时点的印刷Cost边界规则：只有冒号前最后一个完整子句包含真实支付动作时，才拆分为Cost/效果。“进攻时：”、“进攻后：”、“1~2：”、“选择一项：”及其他时点/条件/分支标点不再因冒号后出现“弃置”字样而生成假Cost。

全目录当前155个语义上的冒号费用段均有Cost原子。本批从非主动时点补齐18项遗漏，覆盖阿斯加德军团的自伤/墓地回库、奥德修斯展示手牌、理查德弃置侍从骑士、王者之剑移除、战场/牌库弃置以及彼界符文效果；同时清除3项已有目录误报。新增的18个正例、5个反例与全目录不变量会阻止后续回归。

功能提交`daf0b14b1e1714ac3c5c2c0f6a84eab19e193c36`通过专项97/97、Focused/Release规则3260/3260、平台116/116、UI323、连接23、卡图324+42、原子旧分支0及前后端生产构建；证据为`D:\GPT\Legion12\artifacts\deploy\daf0b14b1e1714ac3c5c2c0f6a84eab19e193c36\l12-release-daf0b14b1e1714ac3c5c2c0f6a84eab19e193c36.json`，未部署。

## 2026-09-14 主动效果已支付Cost权威回执

主动能力从提交前权威状态生成支付快照，在实际进入响应堆叠时按状态差生成回执：主宰伤害、返还/消耗士气、临时士气、神力、符文、主动休整、手牌/战场/牌库弃置及墓地回库均显示实际发生的数量和卡名。被减免为0的费用不伪造Cost行；既有显式支付摘要优先，避免重复。多段复合效果的普通响应只展示当前段，明确单次响应包裹全部段的计划则继续展示本次完整效果。

阿麦金的“主动休整 展示牌库顶部1张牌：……”已按冒号规则修正：休整与展示牌顶均在响应前支付，并锁定同一张已展示卡；被无效时仍保留这两项已支付Cost，结算只处理已锁定的牌。结构化原子目录不再被“已验证复合路由”整体覆盖；雷神之锤、荷鲁斯、希芙、雅典娜、努阿达的冒号前Cost恢复为后台、响应和实战可共用的结构段。新增全目录守卫：任何含冒号的主动效果缺少Cost段时直接测试失败。

功能提交`999133a073b90c36a23e51039b66d239691d02ac`通过相关回归386/386、Focused/Release规则3231/3231、平台116/116、UI323、连接23、卡图324+42、原子旧分支0与前后端生产构建；证据为`D:\GPT\Legion12\artifacts\deploy\999133a073b90c36a23e51039b66d239691d02ac\l12-release-999133a073b90c36a23e51039b66d239691d02ac.json`，未部署。

## 2026-09-14 响应费用/效果同源与最小化墓地查看

响应窗口新增统一的已支付费用投影：只有服务端已实际提交的Cost才写入堆叠元数据，并在对手弹框中按`Cost（已支付）：…`与`效果：…`分行展示；不从卡文冒号或关键词临时猜测，也不额外解释无效后的退款规则。单次响应覆盖多个效果段时，效果区展示该响应实际覆盖的全部已启用段；尚未支付或尚未确定的后续分支不提前公开。

嬴政成功弃置当前费用为8的军团后，对手可看到被弃置卡名及击杀、返还士气、追加士气限制的完整效果；没有8费军团时只进入展示手牌分支，不出现虚假Cost。天照第二能力显示实际弃置卡牌，并同时列出最多2张士气转活跃和前排【高天原】本回合兵力+1000。共享记录器同步覆盖手牌打出复合Cost及赫拉、洛基、瓦尔哈拉、花魁、努阿达等既有预支付入口。

任意阻塞弹框最小化后，双方公开墓地都可打开并查看全部卡牌及详情；原弹框、选择状态和计时保持不变。查看态禁止发动墓地能力及奥西里斯特殊胜利，关闭墓地后可继续原流程，避免“查看信息”成为绕过待处理Prompt的操作入口。

功能提交`fb50b52374ca5fe3bd82443b785f3cc2d0eed2d5`通过专项27/27、Release规则3228/3228、平台116/116、UI323、连接23、卡图324+42、原子审计110程序/96卡和前后端生产构建；证据为`D:\GPT\Legion12\artifacts\deploy\fb50b52374ca5fe3bd82443b785f3cc2d0eed2d5\l12-release-fb50b52374ca5fe3bd82443b785f3cc2d0eed2d5.json`，未部署。

## 2026-09-14 对方选择弃置单张手牌与贯穿角色统一

纳芙蒂蒂、血斧艾瑞克、海伦、鲍斯、佐佐木小次郎共用`L12OpponentHandDiscardTriggerEffects`。同一规格生成能力序号、真实时点、触发条件、受影响方、私密Prompt、结算文本、原子程序和`single-trigger`场景。条件在候选建立时锁定；无对方手牌时静默不入栈；响应后手牌变空记为`failed`；有对象即使唯一也必须由对方点击选择；V2恢复及重复提交不能二次弃牌。〈毒药〉的“无效随后弃牌”是复合响应效果，明确排除，不按关键词误并。

【贯穿】的权威击杀事件新增“来源是否为本次进攻军团”事实。只有进攻军团以战斗击杀被进攻军团时才可发动印刷或临时赋予的贯穿；防守方反杀不触发，因此被进攻的赵云和帕西瓦尔均不会贯穿。衍生进攻继续读取该军团结算时剩余兵力，并统一设置`SuppressAttackTriggers`，不会触发“进攻时”。其他防守方击杀效果及“下一次击杀后转活跃”不受这一贯穿专属限制。

功能提交`995b04d98da7f3c3b2f9037b5825a2f5f7fde653`通过专项22/22、Release规则3226/3226、平台116/116、UI323、连接23、卡图324+42、原子审计110程序/96卡与前后端生产构建，未部署。

## 2026-09-14 阵亡时单一试炼推进统一

侍从骑士与费奥纳的骑士已归并到`L12SimpleTrialAdvanceTriggerEffects`。同一规格拥有能力序号、时点、推进值和结算文本，并生成细粒度原子程序与`single-trigger`场景；声明、动效、日志、结果和恢复不再分别登记+1/+2。

两项均为必发：运行时推进当前控制者的第一张未完成试炼，最多至8；无可推进试炼时静默完成，被无效时不推进。多试炼、跨所有者控制、V2响应中恢复和旧Prompt拒绝均有守卫。全池形态扫描排除了不同触发时点与所有带费用、选择或额外结果的试炼能力。功能提交`9ae755eadcee90c025e86afebf3c5de0ebf96224`通过专项/相邻136、Focused/Batch/Release规则3210，未部署。

## 2026-09-14 阵亡时单一主宰恢复统一

金发哈拉尔与圣女贞德已归并到`L12SimpleMasterHealTriggerEffects`，同一规格拥有能力序号、时点、恢复对象、数值、结算文本和日志原因，并生成细粒度原子程序与`single-trigger`场景。前者恢复控制者主宰1点，后者分别恢复双方主宰1点。

两项均为必发：满血时不生成虚假恢复日志但正常完成；被无效时不恢复；圣女贞德按双方各自当前禁疗状态独立处理，不因一方无法恢复而阻断另一方。V2响应中恢复和旧Prompt拒绝已覆盖。全池形态守卫排除带抽牌、伤害、弃牌Cost或分支的恢复能力。功能提交`f6a8d50c3b6e11faedb2e459b0f3914146e714c1`通过专项/相邻120、Focused/Batch/Release规则3203与324卡原子审计，未部署。

## 2026-09-14 阵亡时单抽牌统一

无名的渗透者、墨子、贝奥武夫、布伦希尔德、哈特谢普苏特、井伊直虎与埃涅阿斯已归并到`L12SimpleDrawTriggerEffects`。卡牌规格同时拥有能力序号、条件、可/必发、抽牌接收者、空库原因、审计事件和结算展示文本，并据此生成细粒度原子程序与`single-trigger`场景；按钮、堆叠、结果、日志、动效和恢复不再各自猜卡文。

渗透者/墨子两个旧复合路由由新程序优先接管，旧路径仅为升级前在途状态保留。渗透者在对方战场阵亡仍由所有者抽牌；墨子的免死注释不再被解析为抽牌效果。五项选发在入栈前确认，布伦希尔德条件于触发时锁定；V2响应中恢复只结算一次，旧Prompt重复提交拒绝。

全池形态门禁固定只有这7项属于“阵亡时完整结果仅抽1张”；含治疗、弃牌、查看/加入或分支的能力继续进入各自组合协议。功能提交`1338dc8fe0d354a6b8efad0f11c278db2ae85bb9`通过专项/相邻24、Focused 3195、Batch/Release 3196与324卡原子审计，未部署。下一批继续按阵亡时单结果形态扫描士气/生命等资源操作，不按“阵亡时”一词机械合并。

## 2026-09-14 阵亡时手牌单军团登场统一

坂本龙马与亚瑟王已归并到`HandLegionSummonTriggerSpecs`。同一规格驱动当前候选、可/必发、最少选择数、手牌私密展示、空位声明、响应、逆序复核、登场状态及单段呈现。坂本的“最多1张”解释为必发效果内选择0至1张；亚瑟王的“可”解释为整项选发，二者不再共用错误的“不发动”按钮语义。

全卡池“阵亡时＋手牌＋登场”精确扫描只命中这两张；其他手牌登场分别属于登场时、进攻后、反击替代或主动效果，不混入本时点协议。功能提交`d61f9c26c476810b4396cfd3dc105f7b810ad08f`通过专项80、Focused/Batch/Release规则3184、平台116、UI321、连接23、卡图324+42与生产构建，未部署。下一批继续沿区域移动与离场时点扫描，不按卡名增加旁路。

## 2026-09-14 阵亡时墓地单军团登场统一

尼托克丽丝、血斧艾瑞克、陵墓圣武士已归并到`GraveLegionSummonTriggerSpecs`：三项均为必发，统一在入栈前人工选择墓地实体和我方空位，开放一次响应，并在逆序结算时重验区域、当前费用/有效特征及位置。无对象或无空位静默，目标/位置失效不改选、不覆盖；陵墓守卫Token可从墓地登场。

带冒号前费用的比约恩自复活、墓地主动的雷神之锤、外部监听的增殖甲虫和智慧法典多段后续不属于本单段协议。功能提交`f7b7f14788a87ccae7558c9c4e0838609b0e00ef`通过专项86、Focused/Batch/Release规则3173、平台116、UI321、连接23、卡图324+42与生产构建，未部署。下一批继续处理“阵亡时从手牌选择军团登场”及其最多数量语义。

## 2026-09-14 阵亡时墓地单卡回手统一

孙武、阿尔维达、忒修斯已从三个旧结算分支归并到结构化`GraveToHandTriggerSpecs`。同一规格驱动候选、可/必发、玩家选择、入栈前复核、响应、逆序结算复核、公开展示、入手事件、日志和单段场景；墓地选择统一展示全部牌并只允许合法对象。

阿尔维达按卡文无“可”处理为必发，唯一目标仍需点击；孙武、忒修斯可明确不发动，进入目标步骤后可取消整次尚未提交的事务。无合法对象静默跳过；目标在响应后离开墓地或失去当前合法性时失败且不改选；被无效不移动卡。功能提交`890967c510525f368622c59bc1eb199e52f44d68`通过专项68、Focused/Batch/Release规则3164、平台116、UI321、连接23、卡图与生产构建，未部署。下一批继续扫描“阵亡时从墓地选择军团登场”的位置声明与区域移动协议。

## 2026-09-14 抽牌后弃牌跨时点统一

“抽牌后弃牌”已从触发、登场和主动三个入口归并为同一组合协议。黑胡子蒂奇、传奇的拉格纳、奥拉夫二世、雷神之锤、赫拉克勒斯和洛基均先结算抽牌段；仅在该段成功后，以新形成的当前手牌建立私密、强制且不可取消的弃牌声明。六项整项只开放一次响应，被无效不抽不弃，抽牌失败不生成悬挂弹框。

本批同时修复主动复合后段的公共来源与上下文传递：主宰、场上卡或圣物不再因不处于`Resolving`区而中断，普通与免Cost复制保留相同能力身份。全池扫描排除先弃后抽且双方执行的〈神之天平〉，以及分属独立能力的山河社稷图、孟婆，避免只按关键词机械合并。

功能提交`5071f07ceba0c95819d1dfb01b7f4bd21926374d`与`6f2bca41c4aa9e4297e80dd51f91860bce7f13d6`通过定向22、Focused/Batch/Release规则3150、平台116、UI321、连接23、卡图324+42和生产构建，未部署。下一批继续按“区域移动的阵亡/离场效果”扫描单段与多段边界。

## 2026-09-14 公共触发取消语义

多步公开触发已把首步“选择不发动”与后续“取消整次发动”分开。后续步骤仍可安全退出，但不会把取消伪装成效果未发动；费用只有全部声明通过后才提交，取消不扣除资源也不建响应堆叠。卡文“最多1张”保留选择0张继续结算的独立语义。

功能提交`cee790d93a2678f0ed0ae0d71c3e7d218280e69b`通过规则3131、平台116、UI321、连接23、卡图及生产构建，未部署。该策略由公共步骤生成器统一提供，后续新增触发不再逐卡设置含混按钮。

## 2026-09-14 阵亡单段与击杀时点

阵亡类先按结算形态归并，而不是逐卡重写。本批确认哈拉尔击杀、奥德尔休整、哮天犬追加休整士气、阿塔兰忒翻转士气均是一个公开结算段，统一接入`single-trigger`。既有候选和最终提交复验不变，场上/士气对象即使唯一仍保留显式点击；选发能力的“不发动”与强制能力的无对象静默跳过继续区分。

疯狂的爱丽丝印刷为“击杀后”，原运行时却和普通进攻后共用`after-attack`并依赖数据补判。本批改为`after-kill`，候选只由权威击杀来源事件产生；场景绑定能力2，放弃、成功、无效、来源失效、once预留和重复提交均沿统一触发生命周期。

功能提交`323f181e126e4b33d3fb746a2424fa3cfec694c9`通过定向115、规则3130、平台116、UI321、连接23、全池248卡/577能力、卡图324+42和生产构建，未部署。下一批继续从阵亡池中区分单段区域移动与真正多段“抽取随后弃置”流程，后者不进入单段目录。

## 2026-09-13 事件触发语义键第二批

月读的“后排转前排”“前排转后排”“任意我方军团位移”分别拥有独立运行时点；同一位移触发两个效果时仍使用统一TriggerBatch排序，不再以两个`active`条目丢失语义。三项均绑定其印刷能力第1/1段，保持既有强制、选发、费用与目标协议。

孙悟空的离场替代已经完成返回主宰区后，才建立`master-legion-returned`条件士气分支；其场景文案只描述尚待结算的士气，不重复播放已经完成的返回。天廷士气卡补齐两段权威结构：主动支付2士气追加活跃士气使用`single-active`；返还后归零的可选恢复使用`morale-returned-to-zero`和`single-trigger`。按钮、声明、日志、动效、回放与恢复由相同能力段解析。

功能提交`9d48da58cc721ecddf1bf457bf6efd5260b698e8`通过定向109、规则3124、平台116、UI321、连接23、全池248卡/577能力、卡图324+42及前后端生产构建门禁，未部署。下一步继续扫描仍以通用时点或整段卡文驱动呈现的触发类入口，并按单段、复合、替代后续分别迁移。

## 2026-09-13 事件触发语义键第一批

统一生命周期不能只统一弹框和结果，还必须让运行时触发键表达真实时点。本批将主宰受伤、主宰因效果受伤、远程军团阵亡、圆桌骑士登场、战术效果成功结算五类入口从`active`迁出；只有玩家通过主动按钮发起的能力才继续使用`active`。

安德华拉诺特、阿尔忒弥斯与〈寻找圣杯之旅〉是单段可选触发，统一获得`declined/resolved/negated/failed`场景身份；玛格丽特是支付后两段复合触发，安格斯是强制试炼推进，保留各自段结构但统一发布`effect-trigger`。静态门禁将语义键列为契约，防止未来为了复用结算函数再次把事件时点写成主动效果。

功能提交`9a8c12be3a7a0556729e9aff87c42e5058d96019`通过规则3122、平台116、UI321、连接23、全池248卡/577能力及卡图/生产构建门禁，未部署。下一批迁移月读三种位移事件、孙悟空返回主宰区后事件和天廷返还士气后归零事件；这些入口会按单段、支付型和状态条件型分别归类，不机械共用一个协议。

## 2026-09-13 单段公开触发：驱魔道士陆瑛

公开触发不能复用主动按钮或反击响应的场景映射。本批新增`single-trigger`协议，显式登记来源卡、印刷能力序号与运行时触发键，只允许经审核的单段单结果能力进入。驱魔道士陆瑛能力1成为首项：对方战术结算后先由玩家选择是否发动，选择发动才进入通常响应窗口。

选择不发动不创建空堆叠，并以同一结构场景发布`declined`；进入堆叠后，正常返回手牌、被无效及来源在响应中离场分别形成`resolved/negated/failed`。声明、实际结果、动效、日志、回放和V2恢复都读取能力1第1/1段，旧响应Prompt不能重复提交。

专项8/8、Focused/Batch/Release规则3114/3114、平台116/116、UI契约321项、连接恢复23/23、卡图审计与生产构建及全池248卡/577能力审计通过，“after-opponent-tactic”旧整段唯一场景由1降至0。功能提交`edad5d003dda232095d2991d23fb8dfb5b706ee7`已同步GitHub main，未部署。下一步仍按时点和结算形态分组迁移，不机械处理117项登场或50项阵亡遗留。

## 2026-09-13 非堆叠规则动作：骑兵位移

齐格鲁德、源义经、珀尔修斯·晋升、赵云和莫德雷德的印刷位移段已从旧主动整段目录迁出。它们描述的是立即执行的骑兵规则动作：不支付卡效Cost、不进入效果堆叠、不开放响应窗；结构定义仍保留来源卡、能力序号、完整原文和呈现场景，供后台及玩家界面识别。

所有当前有效【骑兵】共享服务端规则动作投影。按钮资格、禁用原因与合法目的地从同一状态函数生成，`cavalryMove`提交再次调用该函数；前端不再自行猜测。由此收口〈腐秽大地〉只有后排空位时错误亮按钮、同时有空位时错误点亮后排的两类不一致。成功后只生成一个带场景身份的`move`事件，同时供战报、卡牌位移动画、规则动作条和回放读取；V2恢复后的本回合一次限制与重复提交也保持一致。

专项71/71、Focused/Batch/Release规则3109/3109、平台116/116、UI契约321项、连接恢复23/23、卡图审计与生产构建通过；全池248卡/577能力审计零问题卡。旧主动整段遗留由5降至0，后续主动效果迁移不再混入核心规则动作。功能提交`2dd4595c942d50eae95c27005801f5d89dba1e5e`已同步GitHub main，未部署。

## 2026-09-13 Cost支付后的墓地登场：荷鲁斯

荷鲁斯能力1现以`horus-revive`单段承载冒号后的实际效果。两种费用方式都必须在完整选择墓地军团和未来空位之后才提交，并在响应前支付；费用军团因此可以成为本次墓地目标，支付后被无效或结算失败不退士气、军团或次数。〈信仰狂热者〉只免除Cost与正常次数，仍进入同一个结果段。

冻结对象在逆序结算时必须仍处于墓地、仍是当前兵力不高于2000的军团、仍具有有效【太阳城】特征，冻结位置也必须保持为空。旧代码使用任意私区登场事务，导致对象被移至手牌仍可能登场；现在先执行严格区域重验，失败统一发布`failed`。同时按卡文是否含“原本”校正兵力口径：荷鲁斯和其他8张普通“兵力不高于2000”效果读当前兵力，只有明确写“原本兵力”的效果读印刷兵力。

本批把最后一个真实旧主动效果迁移完成，旧主动整段目录只剩5项骑兵位移动作。专项/相邻156/156、Focused/Batch/Release规则3100/3100，平台116/116、UI320、连接23、卡图审计与生产构建通过；功能提交`a737d585341cea01ce1bd7cee4c8bbea2f1ec167`已同步GitHub main，未部署。下一阶段建立统一动作呈现协议，而不是把动作伪装为可响应效果。

## 2026-09-13 持续监听不是主动按钮：无眠之夜

〈无眠之夜〉已从机械解析的3项能力校正为2项结构化定义：翻开天灾时的`disaster`触发，以及其位于天灾区期间的`continuous`监听。“当玩家使用主动休整时”描述的是被监听事件，不是〈无眠之夜〉自身可以由玩家发动的主动能力。后台、按钮、时点、卡文与运行入口现在读取相同边界。

监听仍通过公共效果入栈事件确认来源能力确属结构化主动休整；白起等主动休整会对使用者主宰造成1点中立非致命伤害，服部半藏翻正等普通主动不会触发。全池相同句型只另有监听圣物效果的〈虚构的圣杯〉，其目录已经正确保持单项持续能力。

本批使旧主动整段场景7降至6；剩余5项均为骑兵位移动作，另1项为荷鲁斯真实主动效果。专项75/75，Focused/Batch/Release规则3093/3093，平台116、UI320、连接23、卡图与生产构建及全池248卡/577能力审计通过；功能提交`6efa5df64ce7d22d8407f4f7d73ff534d6367b0b`已同步GitHub main，未部署。

## 2026-09-13 银臂努阿达：同一响应窗口内的独立结果段

银臂努阿达能力2现以两个结构段承载“最多2张士气转活跃”和“试炼+2”，但整项只开放一次响应。2符文是冒号前Cost并在入栈前支付；被无效会停止两段，避免把同一能力的“随后处理”误作新的可响应效果。

每段独立发布真实终态：预选0张士气时首段`skipped`，已选对象部分失效只处理其余对象，全部失效=`failed`；没有可推进试炼时次段`skipped`。支付符文引出的能力1触发和主效果延续顺序进入恢复测试，费用、触发、结算及旧Prompt不会在重连后重复。

本批将旧主动整段场景8降至7；其中5项确认是统一骑兵位移动作而非效果堆叠，后续建立动作呈现协议，剩余2项继续按组合/单段效果迁移。功能提交`bc036f16b3678a689868388d4d987466ce66e7c9`已通过完整验证并同步GitHub main，未部署。

## 2026-09-13 服部半藏：来源本身也是需要逆序重验的对象

服部半藏主动翻正已归入单段实际结算。该能力没有独立目标选择，但效果实际作用于来源自身，因此来源不能因堆叠保留快照就绕过权威状态重验。结算要求原实例仍在我方战场且仍为覆盖状态；离场或已经翻正都明确失败，不改写快照、不生成不存在的成功日志。

原子条件现在可以由结构定义声明失败终态与原因，区分普通条件不满足的静默处理和“发动后结算条件失效”。正常、无效、两种逆结算失效与V2恢复均已覆盖。旧主动整段场景9降至8；功能提交`a620dd5282c24960876c619c4c2f3a59bfb9e51d`已通过完整验证并同步GitHub main，未部署。

## 2026-09-13 奈芙蒂斯：无冒号的弃置是效果，不是预付Cost

奈芙蒂斯能力2已归入单一结构段。按当前统一句法边界，同一能力没有冒号时，不得把其中的“弃置”仅因动作名称而提前当作Cost；玩家只在发动时声明对象，军团到逆序结算时才按当前合法状态逐张弃置。这样响应无效会完整停止效果，上层效果也可以通过移走或改变对象状态令相应对象失效。

结算结果按实际处理记录：至少一张合法对象被弃置时该段`resolved`并按实际进入墓地数量形成折扣；部分失效不吞掉其余对象；全部失效=`failed`。取消、被无效、失败与成功不再共用旧整段文案，V2恢复与重复提交读取冻结声明。全池同句型扫描确认荷鲁斯等带冒号的战场弃置仍是Cost，不做错误机械迁移。

本批将旧主动整段场景10降至9；专项及相邻13/13、三层规则验证3078/3078、平台116、UI320、连接23、卡图与生产构建均通过。功能提交`0d12d9b5249bba5389524f95311d4bc5b3d2cc43`已同步GitHub main，未部署。下一批继续从剩余9项旧主动场景中迁移，并沿用冒号边界、声明冻结、响应后重验及标准终态协议。

## 2026-09-13 主宰临时军团形态：孙悟空

孙悟空能力1现以`wukong-transform-entry`单一结构段承载真实变身结果。卡文冒号前“返还2至8士气”是可变Cost，前排空位是效果公开对象；两步都保留统一取消入口，直到完整声明完成才实际返还士气、登记回合次数并进入响应窗口。支付日志只记录Cost，效果段只描述实际作为【斗士】军团登场，避免费用移动重复进入动效文案。

堆叠逆序结算时只使用冻结的士气数量和位置，并重新验证位置仍为我方前排空位。被响应占位时明确`failed`，不改选、不覆盖军团、不退士气或次数；被无效时不登场且Cost保留。V2恢复只按冻结数量生成一次军团并拒绝旧Prompt重复提交。既有当前类型隔离继续成立：主宰区孙悟空不是军团，实际登场后才是军团；其任何形式离场都替代为返回主宰区，之后条件满足才另行声明追加休整士气。

定向及离场相邻24/24、Focused、Batch与提交级Release规则均3071/3071，平台116/116、UI契约320项、连接恢复23/23、卡图40项/324张及生产构建通过；旧主动整段场景11→10。功能提交`914491398ba99f55f4103919e2caf24d0b1a724e`已推送`origin/main`并精确读回，本批未部署。剩余10项继续按同类型分组处理。

## 2026-09-13 固定支付选项与赋予能力分支：阿尔忒弥斯

阿尔忒弥斯主动效果现以固定决策弹框呈现两种冒号前Cost。神力或手牌不足时对应选项仍按统一布局显示但灰置，并携带服务端拒绝原因；选择弃牌后再从完整手牌中选择具体费用卡。玩家完成费用方式、费用对象、军团目标和强攻/震击分支前不改变状态，取消会释放整次声明。

支付提交后只建立一个可响应效果段。逆序结算重新判断冻结目标仍为我方公开军团、具有当前有效【奥林匹斯】特征且当前费用为3至6；任一条件变化均`failed`，不改选、不退Cost。强攻与震击读取同一段定义下的不同分支文案，实际日志、动效、回放与恢复一致。声明框架同时从前缀白名单改为按权威选项步骤验证稳定协议值，覆盖未来`pay:`/`buff:`等同型入口。专项及声明绑定相邻21/21、Focused、Batch与提交级Release规则均3065/3065，平台116/116、UI契约320项、连接恢复23/23、卡图40项/324张及生产构建通过；旧主动整段场景12→11。功能提交`a75122aa62532f5a158c2be9f0cbda60174eef49`已推送`origin/main`并精确读回，本批未部署。

## 2026-09-13 赋予“下一次击杀后转活跃”的共享协议

匠神锻造炉与莫瑞甘是全池仅有的两项`ReadyAfterNextKill`赋予入口。两者现在都在玩家完成目标选择后支付冒号前Cost并冻结目标；候选、提交、响应后结算统一读取目标当前所在区域、军团状态和有效阵营，锻造炉额外排除【晋升者】。失效明确为`failed`，不改选、不退主动休整/士气/符文；被无效不写入状态。后续一次性消费仍统一由击杀来源事件完成。

锻造炉的“下一次晋升神力-1”和“击杀后转活跃”分别成为印刷能力2下独立第1/1段，莫瑞甘主动项映射其印刷能力2。专项/相邻13/13、Focused、Batch与提交级Release规则均3058/3058，平台116/116、UI契约320项、连接恢复23/23、卡图40项/324张及生产构建通过；旧主动整段场景13→12。功能提交`eebc8eeed92e8c3d23add9b4f0efdb48b5d30113`已推送`origin/main`并精确读回，本批未部署。

## 2026-09-13 赋予位移与按位移历史赋予状态：八尺琼勾玉

八尺琼勾玉的两个运行模式现分别绑定独立第1/1段并映射回印刷能力2。主动休整是两者共同的发动Cost，只在玩家完整选择军团及（位移模式下）目的地后支付；支付前取消不改变状态，支付后被无效或对象失效不回退。

位移结算以冻结军团和冻结目的地为准，在响应逆结算后重新判断军团仍位于我方战场、公开、活跃且为军团，并判断目的地仍为空且未受天灾限制；任一条件失效均发布`failed`，不自动改选。免死结算重新判断军团仍在我方战场且本回合确实位移过，否则同样失败。正常位移继续不读写对象的`LastCavalryMoveTurn`，只更新公共位移历史并通知月读。

全池扫描确认上述两种赋予语义均只有八尺琼勾玉一个入口。专项/相邻14/14、Focused、Batch与提交级Release规则均3051/3051，平台116/116、UI契约320项、连接恢复23/23、卡图40项/324张及生产构建通过；旧主动整段场景14→13。功能提交`1175279b10f72bf35c62a41b62ffecc92f8c8386`已推送`origin/main`并精确读回，本批未部署。

## 2026-09-13 墓地到手牌对象：黄泉之门

黄泉之门主动回收绑定独立第1/1段。其主动休整在响应前支付；所选墓地卡在逆序结算时必须仍位于墓地、具有当前有效【高天原】特征且允许进入手牌，否则发布`failed`而非静默成功，不改选、不退Cost。万物统御之戒只改变有效特征，不改变衍生特殊卡区域限制。专项/相邻8/8、Focused、Batch与提交级Release规则均3043/3043，平台116/116、UI契约320项、连接恢复23/23、卡图40项/324张及生产构建通过；旧主动整段场景15→14。功能提交`c62669b3f163e2a60d9c3761dda734b61c9dd7d8`已推送`origin/main`并精确读回，本批未部署。

## 2026-09-13 墓地到底部Cost协议与神剑格拉墨

神剑格拉墨的主动休整现绑定`gram-nonlethal-damage`第1/1段并映射回印刷能力序号2。其冒号前全部属于Cost：来源主动休整，并将墓地中恰好可视为4张的当前有效【阿斯加德】军团按自选顺序返回牌库底部；冒号后只有对方主宰1点非致命伤害。所有Cost在响应前提交，被无效或断线恢复都不回退；实际结果段只叙述非致命伤害，不把费用移动重复显示为效果。

资格、选择和支付现共享同一判断：卡牌必须仍在墓地、是军团、具有当前有效【阿斯加德】特征、且允许进入手牌或牌库；〈渴求死亡的勇士〉按玩家逐张指定的1至3张值恰好凑4。万物统御之戒可以改变有效阵营，但不能改变衍生特殊卡的区域禁入状态。格拉墨不再自行删除墓地并添加牌库，而是使用统一到底部事务；因此取消、支付顺序、特殊卡替代规则和重连与罗洛等同类共享边界。

全池卡文扫描确认同类9项为蛇眼西格德、雷神之锤、步行者罗洛、勇士比约恩、奥拉夫二世、古斯塔夫一世、神剑格拉墨、英灵殿和西芙。其余入口已使用共享可进入过滤与墓地折算；本批额外修正奥拉夫、古斯塔夫初始资格仍统计特殊卡的问题。具名12/12、跨卡相邻66/66、Focused、Batch与提交级Release规则均3038/3038，平台116/116、UI契约320项、连接恢复23/23、卡图40项/324张及生产构建通过；直接墓地移除后添加牌库的旁路1→0，旧主动整段场景16→15。功能提交`adc6482e1cc9daf403d4ca7bcf03ea26a746f3cb`已推送`origin/main`并精确读回，本批未部署。

## 2026-09-13 冒号Cost复合主动：安卡神碑双运行模式

安卡神碑同一印刷主动效果包含两个可选运行模式，现分别注册`ankh-ready-guard`与`ankh-draw`第1/1段，并映射到同一原子能力序号2。结构段以实际选择为身份，避免把“弃手牌转活跃”和“陵墓守卫转休整后抽牌”写成同一条泛化动效或成功日志；按钮、Prompt、结果、回放与V2恢复均沿用该身份。

第一模式严格按冒号拆分：弃置1张手牌是Cost，所选休整〈陵墓守卫〉转为活跃是效果。初始资格同时要求可支付的手牌与合法目标，支付前可用标准取消退出；支付后响应逆序结算，冻结目标必须仍在场且保持休整，否则为`failed`并保留已支付弃牌及来源主动休整。第二模式把所选陵墓守卫转休整作为响应前Cost；被无效不抽牌，牌库在真正结算时为空则为`failed`并沿用败北，已支付的两项休整均不恢复。

本轮全池扫描44条带冒号的弃置、消耗、返还或转休整支付文本，并对已迁移能力复核“初始能否支付、支付前取消、支付后不回滚、逆序时对象/资源重验”。确认需要修补的同型漏口是安卡第一模式空手牌仍先选目标；其余已迁移入口继续由既有支付门禁覆盖。旧主动整段场景17→16。专项10/10、Focused、Batch与提交级Release规则均3030/3030，平台116/116、UI契约320项、连接恢复23/23、卡图40项/324张及生产构建通过。精确目录复核还确认遗留直接位移动作为齐格鲁德、源义经、珀尔修斯·晋升、赵云、莫德雷德共5项，纠正早期摘要的4项计数但不改变行为。功能提交`f1b99595ce70e776b7ed26110710dfb3a66b4b75`已推送`origin/main`并精确读回；本批未部署。

## 2026-09-13 单张牌库顶揭示与条件去向

阿麦金的主动休整现绑定独立第1/1段，并在一份定义中提供等待展示、加入手牌、返回顶部和返回底部四个公开结果分支。它与顶部3张强制选一不同：展示牌仅具有【彼界】单一有效特征时，“可加入手牌”仍允许玩家选择三种去向；否则只允许顶部/底部。资格统一读取有效特征集合：阵营和通用都是特征，圆桌骑士、晋升者、杨戬专属、哪吒专属等是额外特征，职介和试炼值不是；万物统御之戒转换不另写特例。

主动休整Cost在响应前完成，牌库身份只在结算时公开。响应使牌库为空、冻结实例在选择前消失、或加入手牌时有效特征已变化，均发布`failed`，不静默改成返回顶部、不移动其他卡、不回退Cost；被无效不展示。选择Prompt、四个实际分支、V2恢复和重复提交共用同一堆叠身份，私区移动使用共享权威区域事务。

全池单张顶部展示扫描命中李靖、李牧、平阳昭公主、乾坤·阴、冲田总司和阿麦金。前五张虽然共享“延迟读取牌库顶”，但分别属于登场付费替代、效果生成打出及随后抽牌、条件增兵、两段响应、进攻时效果生成打出，已由各自计划和回归约束；本批只抽取共同的隐藏信息与失效原则，不错误合并不同后句。旧主动整段场景18→17；下一批继续按剩余资源复合主动分组，而非逐卡添加旁路。专项14/14、Focused、Batch与提交级Release规则3023/3023，平台116/116、UI契约320项、连接恢复23/23、卡图40项/324张及生产构建通过。功能提交`495b7065a054cf14d3241e05fbecfb82db53bd27`已推送`origin/main`并精确读回；本批未部署。

## 2026-09-13 顶部牌库私区事务与必选对象

全池按“查看牌库顶部3张牌→选择符合条件的牌加入手牌→排列其余牌”扫描并逐入口核对，命中野外扎营、山河社稷图、花魁的馈赠、普罗米修斯和特勒马科斯。前三者分别处于手牌复合效果或同卡多主动分支，后两者为主宰/军团主动；不能以某一处理器已经修正代替同类完成。

统一事务边界为：冒号前Cost和回合次数在响应前提交；双方响应完成后才读取并向控制者展示顶部身份；全部展示牌进入同一行卡牌布局，合法集合独立决定可点击项。只要存在合法对象，“选择1张”就是强制步骤，唯一对象也必须点击且不得出现`skip`；没有合法对象时不创建空选择Prompt，直接进入全部展示牌的排序；结算时牌库为空为`skipped`。已选择对象在内部继续步骤失效为`failed`，但仍安全处理剩余牌并关闭堆叠，不回退Cost。

山河社稷图的`artifactDraw`与`artifactSearch`、普罗米修斯`prometheusTopThree`、特勒马科斯`telemachusTopThree`现各自拥有明确第1/1段；按钮、声明、冻结场景、结果、动效、日志与回放可按运行能力区分。山河抽牌空库沿用败北并发布`failed`，普罗米修斯被无效不揭示牌库。升级前冻结的无命中`skip`仍可恢复并排序；若当前冻结顶部仍存在合法对象，旧`skip`不能绕过必选语义。

新增精确目录门禁，直接列出所有尚未获得结构场景的主动能力卡号和能力序号，避免“同一原子能力有一个分支已结构化”掩盖另一个运行分支。当前未结构化主动能力从21项降至18项，其中5项是直接位移动作；下一批优先处理阿麦金这类单张展示/分支放置，以及资源复合主动。具名/同类22/22、跨模块369/369、Focused、Batch与提交级Release规则3017/3017，平台116/116、UI契约320项、连接恢复23/23、卡图324+42项及生产构建通过。功能提交`41a3cf813c9b751f35b1a2a7896fc9e01ad68436`已推送`origin/main`并精确读回；本批未部署。

## 2026-09-13 双公开对象效果的独立结算结果

全池卡文扫描“选择对方……选择我方/选择我方……选择对方”命中吉原的花魁与草薙剑：草薙剑是互斥选项，已经由分支协议覆盖；只有吉原的花魁属于同一次主动休整中预选敌我两个公开对象、随后可能分别失效的双对象效果。其运行现接入`starter-oiran-active`两段结构定义，主动休整仍是整项发动前的Cost且全效果只开放一次响应；敌军-1000与己军+1000各自取得独立的实际段、对象复验、结算结果、展示文本和日志文本。

逆结算后仅一侧对象失效时，该段发布`failed`，另一段继续并发布`resolved`；两侧均失效时发布两个`failed`；整项被无效时停止所有后续处理且不恢复已支付的主动休整。日志只叙述真正应用的兵力变化，不再在一侧或两侧失败时记录“两边均成功”。发动前缺少对方合法军团则明确拒绝，且不会先休整来源。

真实回归覆盖双段成功、敌方段失败/我方段成功、敌方段成功/我方段失败、双段失败、整项被无效、发动前无对象、V2检查点恢复、旧Prompt重复提交及升级前在途堆叠兼容；具名及目录8/8、组合段/ST全目录/呈现等相邻116/116，Focused、Batch与提交级Release规则均2997/2997，平台116/116、UI契约320项、连接恢复23/23、卡图40项/324张及生产构建通过。功能提交`31a808d4aec69032877238321dbb73e50851656f`已推送`origin/main`并精确读回。旧主动整段场景23→22；本批未部署。

## 2026-09-13 结算时士气翻转对象生命周期统一

奥林匹斯阵营能力`olympusMoraleFlip`及其同规则异画目录接入`single-active`第1/1段。支付1士气与冒号后的翻转对象保持两件事：多种支付结果时先由玩家选择实际支付资源，Cost提交后进入响应，响应结束才从仍为士气面的对象中选择1张翻转；被无效时不生成对象Prompt，已支付士气和回合次数不恢复。

公共`PromptS2FlipMorale`现在冻结“可选/必选、是否限休整士气”约束并在提交时复验。发动时有候选、响应逆结算后候选全部失效为`failed`；Prompt建立后所选对象离区、翻面、或不再满足休整条件同样为`failed`且不改选；发动时本来没有处理对象为`skipped`；可选效果明确跳过为`declined`。服务端事件、实际段结果、战报和前端日志统一认识`effect-declined=未发动`，不再与失败或无效混写。

真实回归覆盖支付/对象分离、正常、被无效、全部候选失效、冻结对象失效、可选跳过、V2检查点恢复和旧Prompt重复提交；具名6/6、同类及相邻奥林匹斯/诸神巅/呈现261/261，Focused、Batch与提交级Release规则均2990/2990，平台116/116、UI契约320项、连接恢复23/23、卡图40项/324张、前端效果契约和生产构建通过。功能提交`c1a1ada9b3824d2d0c1279f629c12c86f1c13cf1`已推送`origin/main`并精确读回。剩余旧主动整段场景25→23；下一批处理吉原的花魁这类同时修改敌我公开对象的双目标部分结算，避免一侧失效仍记录整段完整成功。本批未部署。

## 2026-09-13 无对象抽牌主动效果结果同源

奥林匹斯神力的`godPowerDraw`与西芙的`sifCycle`是剩余旧主动能力中仅有的两项“先提交非目标Cost、再以单一结果抽取1张牌”的运行能力；`S02-05C1A`只作为同规则异画目录同步挂接，不建立第二套运行逻辑。三项目录能力现统一为`single-active`第1/1段，按钮声明、实际抽牌、动效、日志、回放与检查点恢复使用同一能力身份。

两项Cost都在响应前提交：神力消耗并翻回休整士气，西芙依序把合计视为3张的阿斯加德墓地卡放回牌库底部。效果被无效时不抽牌、Cost不返还且回合次数不恢复；正常结算为`resolved`。若逆序结算使牌库在真正抽牌时为空，则发布`failed`并执行既有空牌库败北，不再把失败抽牌伪记为成功。

真实回归覆盖正常、无效、空牌库失败、V2检查点恢复、旧响应Prompt重复提交和本回合重复发动；同类与相邻奥林匹斯/ST03/呈现回归280/280，Focused、Batch与提交级Release规则均2985/2985通过。功能提交`487184e16191749214c2a4b7afaaf92886fe7987`已推送`origin/main`并精确读回。剩余`active + effect + Flow=null`旧场景由28降至25，其中5项是直接位移动作，继续保留在动作日志/回放协议；下一批处理奥林匹斯士气“支付1士气→结算时选择翻转对象”的同类目标状态能力。本批未部署。

## 2026-09-13 九月十日起跨源复核与入口级防回滚

本轮不再把“某个常规手牌打出用例通过”当作卡效完成。2026-09-10起的一方主线48个提交已按八层标准复核：所有运行入口、当前状态候选、冒号前Cost、响应与逆序重验、实际结算/持续状态、快照与重连、按钮/Prompt/动效/日志/回放，以及服务端目录/图鉴/搜索/生产包。全池当前态门禁覆盖248卡578效果段，0项待人工判断；324张卡的旧卡号分支、文本推断和无运行入口均为0。

嬴政暴露了入口级遗漏：正常手牌打出使用支付函数，但效果登场、GM或权威置入等通用路径可以直接推入击杀段。现在预支付规则放在所有登场触发必经的公共网关，候选与提交统一读取当前费用；候选在响应期间失效时重建权威Prompt，旧Prompt不能重复支付，来源离场或全部候选消失也有确定终态。以后所有冒号前Cost卡都必须先证明“每一种入场/发动入口都无法绕过事务”，而非只数支付函数的单点覆盖率。

天照第二能力按持续状态验收：结算建立本回合Aura、立即重算，后进入前排的【高天原】军团同样获得+1000，离开前排失去，返回前排恢复；数值和来源状态进入V2快照与检查点恢复，被无效不建立Aura，下一TurnSerial自动失效。孙悟空则固定卡文跨源规则：服务端目录、图鉴与搜索必须逐字一致，生产构建直接阻止单源更新。

本批Focused与Batch规则均2979/2979通过，UI契约320项、连接恢复23/23、卡图40项/324张和生产构建通过。下一阶段继续迁移剩余旧主动能力的分支与持续状态，但每一批同时应用本节的入口与跨源门禁；后台卡效工作台仍在效果一致性完成后建设，并复用统一设计系统和带图例的呈现样式选项。

## 2026-09-13 诸神巅多分支与可选“随后”段统一

诸神巅四项印刷能力已补齐结构化定义，运行能力不再因旧文本解析把“我方 回合1次”误归为静态；翻转士气、消耗2神力二选一、主动休整免晋升神力和开场追加士气分别保留自己的能力序号。原本挂在第3段的6000伤害场景已纠正到第2段，第1、3段单结果主动也接入准确的结算身份。

伤害分支声明的六份1000伤害在逆序结算时逐份复核：没有声明对象为`skipped`，所有对象失效为`failed`，仍有合法份数时只结算合法部分并记录失效份数，2神力Cost不返还。翻转士气虽然按现行规则在结算时选目标，但候选被上层响应清空后会明确`failed`，不再静默成功。

公共组合段新增显式“已预先选择不发动后续段”的终态声明。全计划扫描确认只有诸神巅与不朽之礼属于这一类，两者现在都会发布独立`declined`结果和对应“不登场”场景；李牧等`RequiredMode`仍是互斥分支，未选分支不生成虚假结果。专项8/8、同类兼容280/280，Focused与Batch规则均2970/2970通过。

## 2026-09-13 公开对象主动状态效果结算重验

黄金圣甲虫的最多两目标减兵与神农鼎的主宰能力次数重置现共享公开对象单段协议：对象在支付前声明，支付后进入响应，真正结算时重新读取权威状态。单目标不存在或不再满足条件时为`failed`；黄金圣甲虫允许主动选择0个对象，因此零目标为`skipped`，两个独立对象只失效一个时不改选，仍对另一个结算并发布`resolved`，同时留下部分失效审计日志。

被无效只停止效果部分：黄金圣甲虫的弃牌、神农鼎返还1士气和主动休整均不回退。神农鼎V2检查点恢复继续绑定原选择Prompt，旧Prompt重复提交拒绝；结算前目标能力已被其他响应重置时不再伪记成功。8个真实类别回归与呈现目录合计68/68，Focused与Batch均2962/2962通过。

旧主动整段场景由36降至34，卡号条件分支保持186。下一批继续处理选择分支和多段“随后”，其中诸神巅已由目录门禁确认必须走显式组合计划；直接位移维持动作协议。

## 2026-09-13 无目标主动状态写入结果同源

雷神索尔与《探寻天空之城》均属于“无需候选、提交费用或次数、进入响应、写入持续状态”的主动效果。两项现以运行能力ID绑定各自`single-active`第1/1段；按钮和声明不再与实际结果分离，正常写入为`resolved`，被无效为`negated`，已支付Cost不退。无目标是此类效果的固有结构，不会被误判为缺少合法目标。

真实回归固定雷神索尔正常生效、被无效后2士气仍已支付，以及天空之城跨V2检查点恢复后只结算一次、旧Prompt与重复发动均拒绝。目录防护同时证明诸神巅的免晋升神力能力存在多个旧场景，不能登记成单段；它继续留在组合计划。直接位移动作仍归动作日志/回放协议，不计入可响应效果。

专项与呈现回归63/63、Focused与Batch规则均2954/2954通过，原子审计324卡、卡号条件分支186。旧主动整段场景由38降至36；下一批优先处理带公开对象的单段状态变化，并明确部分目标失效与全部目标失效的终态。

## 2026-09-13 主动私区登场结果同源

旧主动场景已按真实运行协议重新盘点为42项；“每回合一次位移”属于直接动作，不建立响应堆叠，因此没有为了降低数字而伪造成效果段。本批选择已经共享私区对象、公开位置、费用提交和结算重验事务的四项：克利奥帕特拉七世复活陵墓守卫、黄金圣甲虫复活增殖甲虫、墓地雷神之锤自我登场、希波吕忒复活奥林匹斯军团。

四项接入各自第1/1段后，按钮/弹框声明、实际结算、动效、日志与回放可以使用同一能力身份；正常为`resolved`，响应后对象或位置失效为`failed`，被无效为`negated`。取消仍在支付前结束，已提交的士气、弃牌、墓地回底及主动休整不因后续失败或无效恢复。真实回归覆盖正例、无对象、取消、逆序失效、无效、恢复和重复提交，跨模块112/112通过。

旧主动整段场景由42降至38。下一批继续按多分支、隐藏牌库、资源复合和持续状态分组；直接位移动作另行核对动作日志/回放协议，不并入可响应效果段。Focused与Batch规则均2951/2951通过，卡号条件分支保持186。

## 2026-09-13 特洛伊木马跨回合生命周期结构化

旧复合响应清单最后一项特洛伊木马已完成迁移。对方进攻后的公开模式/对方空位声明连接第1/1段真实置入结果；轮到木马结算时会重新检查盖伏来源和声明格位，因响应使格位被占时为`failed`，不会覆盖或改选。战场上的对方全军-1000继续是持续状态层，只由真实区域状态决定。

所有者下个回合结束时不再直接改区域和抽牌，而是逐个建立不可响应的两段效果：第1段按冻结宿主/格位弃置木马，第2段随后抽1。组合计划现可显式声明后段依赖前段成功；弃置失败会产生`failed + skipped`，牌库为空则为`resolved + failed`。到期实例先预留再入栈，防止结束阶段恢复或多个实例导致重复排入。

同类扫描区分了跨回合持续期限、通用到期弃置和普通“随后抽牌”：全池只有木马同时具备跨战场状态、到期自弃及依赖抽牌，故新依赖为计划级显式选项，没有机械套用到其他卡。正常、位置失效、恢复、重复提交、前段失败和多实例均有回归；专项6/6、Focused与Batch规则均2950/2950通过，卡号条件分支保持186。旧复合响应体清单归零；下一批回到剩余旧主动能力，按私区、多分支、移动/状态及复合资源分组迁移，不能把这一结果扩大为全部主动效果已经同源。

## 2026-09-13 乾坤·阴与响应内同栈续段

乾坤·阴已迁入两段结构计划：牌库顶只在第1段合法结算时展示，符合条件则弃置并记录“符合条件”分支，不符合则置于牌库底部并结束；只有命中时才在第2段建立`post-hidden-reveal`我方军团目标选择。命中但没有军团时第2段`skipped`，被无效时牌库不被读取，检查点恢复保持同一目标弹框和分支场景。

本批同时修复一次响应能力的公共堆叠顺序。内部后续子句必须继续压在原被响应效果上方；若后句需要段首声明，下层先冻结，声明完成或形成`failed/declined`终态后才恢复逆序结算。无目标终态同样保留段号、动效与日志身份。后段不继承上一段`presentationFlow`，仅传递计划允许的声明与结果数值。

全量扫描覆盖七项`SingleResponseEffectPlans`与所有`DeclareAtSegmentStart`计划；真实命中、未命中、无目标、无效、失效、重复提交、重连和结算顺序均已固定。最终相关回归133/133、Batch规则2947/2947通过，生产卡号条件分支由187降至186。旧复合响应体只剩特洛伊木马，下一批处理其跨战场控制/所有权、盖伏身份、进攻后触发及所有者回合结束离场抽牌，不能把持续状态压成即时单段。

## 2026-09-13 逆序结算后的合法性重验

堆叠按后进先出结算。对象在发动/声明时合法只代表效果可以入栈；轮到该效果实际结算时，必须再次使用同一套对象谓词检查区域、卡牌状态、当前兵力、当前费用及其他限制。若响应效果先改变了对象，使其不再满足条件，该段`failed`且不改选；已经支付的Cost和此前已完成的效果段不回退。发动时原本没有合法对象的必发效果仍可发动并以`skipped`空处理，玩家明确不发动为`declined`，被无效为`negated`。

实现上，组合续段在前段/响应完成后统一重验，失效时建立保留原能力、段号和场景的失败结算项；历史结算器的`effect-cancelled`不再投影成玩家主动放弃，明确空目标改用`effect-noop`。神妙行军真实用例固定“选6000目标→先结算+1000→目标7000→击杀段失败且2士气不退”；莫德雷德旧入口固定2000→3000同样失败。这一规则同时覆盖离场、换区、费用阈值和休整/活跃等状态变化。

地主的胁迫已作为下一项旧响应体接入第1/1段结构场景，分为等待对方选择、额外弃牌、抵挡/支援无效；分支身份随检查点恢复，公开结果不携带私密弃牌实例，被无效时不会打开额外弃牌Prompt。聚焦136/136、完整Batch规则2943/2943零失败零跳过。旧复合响应体只剩乾坤·阴和特洛伊木马，需分别处理隐藏牌库展示/延迟目标与跨回合持续状态，不能压成同一模板。

## 2026-09-13 多段反击部分结算：不朽之礼与切腹仪式

不朽之礼现以同一印刷能力内的两段结算：第1段抽取1张牌，第2段按发动前声明选择不登场或将墓地陵墓守卫活跃登场。选择不发动后段明确记为`declined`；已声明的守卫、战场或位置在结算时失效则第2段`failed`，已经完成的抽牌不回退。切腹仪式同样拆为抽牌与费用-2两段：无合法对象不阻止必发抽牌并使后段`skipped`，有对象（即使唯一）仍须点击，已选目标结算前离场则减费段`failed`。

两项均属于“一项能力、一次响应”。首段被无效时整项停止；牌库不足使抽牌失败时首段记`failed`并停止“随后”处理。检查点冻结公开声明、段号和场景；不朽之礼的守卫实例仅留在权威私区声明数据，不进入公开场景定义。旧版本已经入栈的不朽之礼继续由旧整段处理体完成抽牌与登场，避免升级恢复时缺失后句。

本批同时发现并修复公共续段器的结果污染：上一段完成后写入的`effectResultPublished`、`effectResultStatus`和`presentationSceneId`原会复制到下一段，导致后段实际执行却没有独立结果日志，或错误沿用首段动效。现在每个后续段清除这三项段级状态，再按自己的Flow与公开分支解析场景。专项111/111、完整Batch规则2940/2940零失败零跳过。复合旧响应体由5个降为3个：地主的胁迫、乾坤·阴、特洛伊木马；下一批优先按“对手私区选择/独立后句/临时离场状态”分别迁移，禁止压成单段。

## 2026-09-13 私有手牌登场响应：摄政皇权与暗度陈仓

摄政皇权与暗度陈仓现各自拥有第1/1段稳定组合场景。卡文、声明事件、实际结算、动效、日志与回放读取同一段定义；私密手牌实例仍只保存在权威声明事务和StackItem中，不进入公开场景定义或分支条件。摄政皇权继续支持特殊军团允许的战场选择，暗度陈仓继续限定费用不高于4的【天廷】军团与我方空位。

两者都在入栈前一次性复验手牌对象、战场与位置；暗度陈仓还先复验士气，再返还1士气并入栈。入栈后对象离手、费用变化、阵营条件失效或位置被占用，现统一记为`failed`，不再让摄政皇权静默成功或把暗度陈仓误记为“选择不发动”；暗度陈仓已经支付的士气不退。选择`mode:none`则保持盖牌、士气和手牌不变，不入栈，重复提交旧Prompt被拒绝。

成功、结算期失效、选择不发动、重复提交与检查点恢复均由真实路径或冻结StackItem具名覆盖；聚焦84/84、完整Batch规则2930/2930零失败零跳过。复合旧响应体由7个降为5个：不朽之礼、切腹仪式、地主的胁迫、乾坤·阴、特洛伊木马；下一批优先不朽之礼与切腹仪式的多结果/部分结算边界。

## 2026-09-13 复合响应分支首批：绝对防御与拼死反抗

绝对防御现按玩家实际选择的当前堆叠项目冻结公开分支：直接响应`opponent-attack`进攻宣言时为“抵挡本次进攻”，响应进攻链或普通链上的效果时为“无效该效果”。判断不沿响应链回溯根时点，因此用绝对防御无效另一张反击战术不会误显示成抵挡。两种分支共用原弃置手牌Cost、响应资格和`response-negate`运行结算，但声明、实际结果、动效、日志与回放拥有不同稳定场景身份。

拼死反抗现把玩家在翻开前声明的单体/全体模式冻结到StackItem：单体分支为所选休整军团-2000，全体分支为结算时所有休整军团-1000。单体对象在结算前离场或转为活跃记为`failed`；全体分支结算时原本就已无休整军团记为`skipped`；响应自身被无效仍记`negated`。分支和结果状态随检查点恢复，费用/翻开状态不回退。

本批聚焦54/54、跨战斗/嵌套响应189/189、完整Batch规则2922/2922零失败零跳过。9个复合旧响应体已迁移2个，剩余7个：摄政皇权、不朽之礼、切腹仪式、地主的胁迫、乾坤·阴、特洛伊木马、暗度陈仓。下一批优先把摄政皇权和暗度陈仓的私有手牌选择/登场位置接入组合段，并保留声明失效不卡死、取消恢复响应优先权与重连复验。

## 2026-09-12 单段响应运行时映射首批

响应不能只按原子`trigger`名称统计：印刷能力保留“对方进攻我方主宰时/军团登场时”等精确时点，真实堆叠使用`response-block/response-negate/reaction/response-retarget-master`。本批建立显式`卡号+能力序号+运行时触发`映射，先接入四个没有公开分支或独立后续段的响应：佣兵部队抵挡、落穴陷阱无效登场效果、伏击选择我方军团加兵、戏法师的傀儡休整登场并改目标。

四项新增`single-response`第1/1段，声明、实际结果、动效、日志和回放共用同一印刷能力场景。佣兵响应目标已离栈或交战已结束、落穴目标已离栈、伏击已选对象结算前离场，以及傀儡卡牌/进攻对象/预选位置失效，当前均记为`failed`。响应自身被无效统一为`negated`，已支付的弃置、盖牌翻开等费用/状态不回退。

全响应来源盘点中，已有7个复合响应场景，本批新增4个单段响应场景；仍有9个旧响应体：绝对防御、拼死反抗、摄政皇权、不朽之礼、切腹仪式、地主的胁迫、乾坤·阴、特洛伊木马、暗度陈仓。它们分别涉及按对象分支、多个结算段、私有区选择、对手选择或延迟离场，下一批必须建立组合计划和部分结算语义，禁止登记为单段。

## 2026-09-12 单段主动效果实际结算首批

对全卡池原子目录和真实主动/响应入口完成一轮只读盘点：按原子时点统计，仍有44个主动能力与1个反应能力只有旧整段场景；另按响应运行时入口核对，旧响应体不能仅靠原子时点名直接绑定，需在下一批建立显式运行时响应映射，不能误套主动协议。本批先迁移其中8个已经确认是“单段、单一结算结果”的主动能力：白起追加士气、奥尔加减兵、宫廷魔术师封锁反击、伊姆何泰普减费、亚里士多德减费、彼界阿瓦隆减兵、绿洲舞女群体加兵与克里斯蒂娜战术费用替换。

这8项通过同一显式目录把运行能力ID、卡牌能力序号和后台场景绑定，整段场景升级为`single-active`的第1/1段。声明仍沿用原资格、候选、费用和响应事务；实际完成统一发布成功、被无效或跳过结果，按钮、日志、动效与回放读取相同场景身份。目录在构建时拒绝非active能力、多个旧场景或已存在分支/复合段的能力，避免为了显示结果把多段效果错误压成一段。

奥尔加真实结算补充“无目标”状态：Cost可支付时仍弃置自身并进入响应；发动时原本没有目标记录`skipped`，已声明目标在结算前失效记录`failed`，均不伪记成功减兵。具名回归覆盖白起正常结算、奥尔加无对象跳过，以及奥尔加选择检查点恢复、重复提交和被无效后费用不退。剩余36个旧主动能力保留在后续清单，按移动/状态、私有区域事务、多分支、多段和资源复合流程分别迁移；不把本批8项扩大为主动效果全部完成。

## 2026-09-12 主动/反击结算结果同源首批

已为进入结构化效果场景的堆叠项建立统一结果事件：发动声明保存精确场景、能力、效果段与公开分支，实际完成时只发布一次 `effect-result`，并明确区分成功、被无效、无合法处理对象跳过、失败与选择不发动。结果状态和场景ID保存在堆叠数据中，检查点恢复后沿用原对局冻结文案，不用新卡文重新解释旧事务；录像投影同步保留这些字段。

玩家动效不再播放结构化效果的声明事件，而从实际结果事件读取同一分支文案；对局记录也识别该结果类型。旧的整卡场景暂不伪装成已迁移，只对已有 `Flow` 的结构化主动、反击与复合段启用结果投影。回归覆盖成功、被无效、无目标跳过、失败、选择不发动、重复发布和重连恢复；下一批继续扩充未结构化的主动/反击场景，并核对“部分结算”是否需要独立终态，不能把局部空处理误记为整段失败。

## 2026-09-12 冒号费用/效果对象全入口续批

本批不以点名卡修补作为完成口径。先按“单个能力段冒号前为Cost、冒号后为效果”扫描登场公开声明、复合手牌战术、主动分支与离场替代入口；登场同类中吕布、武则天、墨子、夺命诗人埃吉尔、神剑格拉墨、克劳迪娅、井伊直虎、珀尔修斯、赫拉克勒斯·晋升统一为：只用Cost能力决定可否发动，效果对象存在时必须点击，不存在时省略对象步骤并在结算中空处理。扫描额外发现刘备、布伦希尔德与海拉的凝视也把后段对象错误并入发动门槛，已按同一协议修正。土方岁三两次击杀属于同一段效果，改为一个弹框最多选择2张：全部对象费用不高于2，选择2张时至少1张费用不高于1；只有一个合法对象时仍要求玩家点击。

孙悟空现对普通离场、效果离场、战斗离场和回合结束/进攻后返回共用同一完成函数；任何离场均回主宰区，返回后若我方士气少于对方且士气牌库非空，建立可选追加1张休整士气的独立触发。嬴政改用真实打出→登场→响应→结算测试固定无8费军团时只展示手牌；天照第二效果的按钮文本与既有完整结算同源；梅林无符文时仍展示两个灰置分支及原因，并保留标准底栏“不发动”。墓地选择统一在公共Prompt入口展示对应墓地全部卡牌，合法集合仍由服务端独立提供。

此批同时保留支付错误取消、临时军团状态与落穴响应类型的既有全链证据：罗洛代表值失效返回可取消重选；草薙剑/孙悟空只在权威战场军团状态下可被军团效果命中。尚未宣称所有主动/进攻/阵亡/试炼效果的结果动效和日志已经完成结构同源；下一批继续沿扫描清单迁移这些类型，不回到逐卡临时弹框。

特征判断已统一为有效阵营特征加附加特征：六个阵营和通用均是特征，圆桌骑士、晋升者、杨戬专属、哪吒专属及未来同名“专属”字段是附加特征，职介与试炼值不是特征。阿麦金与十字军共用该口径；“仅具有彼界单一特征”会排除任意附加特征。万物统御之戒只在主圣物或额外圣物区生效，并把通用有效特征替换为持有者阵营特征。槲寄生符咒固定3符文减至0费的真实打出链，自然馈赠进入0费筛选；费用、血量、兵力的所有玩家投影与回放最低为0。

## 2026-09-12 反击声明评估同源

四种公共反击计划（战斗至黎明、空城计、破败仪式、粮草掠夺）已提取无副作用声明评估：响应列表、隐藏卡池理论响应、声明步骤候选和最终入栈前复验共用同一份当前状态判断。评估只返回计划、权威时点、步骤候选与不可发动原因，不揭示盖牌/匿名手牌、不支付费用、不改变优先权。

破败仪式只有对方存在可盲选手牌或登场来源仍是权威场上军团时可用；粮草掠夺只有对方仍有手牌时可用；空城计必须存在可返还士气。声明期间条件失效时，卡牌保持盖伏、费用不支付、效果不入栈并恢复原响应优先权。四种计划均新增恢复后提交和重复提交回归，另覆盖无对象与声明中失效；本批仍不表示其结算结果文本、动效和日志已经完全同源。

## 2026-09-11 裁定覆盖：费用与效果对象分离

按用户明确裁定，以每个独立效果段的冒号为费用/效果边界：冒号前的费用必须可支付；冒号后的效果无合法对象不应被一概当作费用失败。费用不足时的后备处理按卡文（例如嬴政展示手牌）执行。不得从候选数量或按钮摘要反推费用。

效果对象即使只有一个也须玩家确认；无对象跳过与唯一对象确认分开。公共声明新增IsCostSelection显式记录费用身份，仅费用或内部控制标记可使用AutoSelectWhenExact；零对象跳过保留。原始卡文保持权威，当前是人工结构标注，不宣称已实现全卡文自动编译。

奥尔加的既有主动能力新增SkipEmptyEffect，允许无目标时支付弃置自身并入栈，结算减兵为空操作；不新增登场触发。此前“无候选一律禁用”的表述由本段覆盖；其他效果按段迁移，不能直接把所有候选校验删除。

进一步裁定同卡多能力必须先分段，再在各段内部判断冒号。奥尔加已作为首个完整样板：ABILITY 1为静态远程；ABILITY 2为仅在手牌适用的自伤Cost与登场减费效果；ABILITY 3为场上主动自弃Cost与减兵效果。原子API和后台现按能力输出/显示Cost与效果，三条实战入口读取对应结构段。其余自伤减费或多能力卡必须逐卡确认边界后迁移，不能用整张卡的第一个冒号批量切割。

2026-09-11续批：已按完整卡文把余下拉格纳、无情者哈拉尔、血斧艾瑞克、齐格鲁德和卡纽特大帝逐能力拆分，与奥尔加共同形成六张结构化手牌自伤减费卡。旧卡号集合已删除；资格、数值、提示、伤害、费用快照和日志共用该能力段，且自伤Cost先于打出/登场。触发条件中的冒号仅在当前段存在Cost原子时切分。哈拉尔与艾瑞克的必发对象效果已声明有对象必须点击、无对象跳过。相关138/138、完整Batch静态门禁及最终Release规则2858/2858零跳过通过；不表示其他多能力卡或所有结果动效/日志已经迁移。

2026-09-11支付/类型续批：打出前墓地、符文及人工士气选择统一获得显式取消，罗洛代表值按当前可支付费用生成并在复核失败时返回重选；非法、混合、重复和重连提交不消费费用或唯一Prompt。临时“作为军团”状态同时绑定实例和场上区域，草薙剑/孙悟空只有实际位于战场且处于军团状态时才进入军团候选/响应。界面复用既有支付控制条和Prompt底栏，新增UI契约禁止为单卡另设按钮位置。此批是费用生命周期和候选一致性的一部分，不表示全部卡牌支付入口已迁移。

2026-09-10。用户批准以对战可靠性和效果展示同源为优先；不新增新手教学。本文记录计划，不表示实现或验收完成。

## 第一批范围

复用 L12ActiveAbilities、L12PublicActiveEffectPlans、L12PublicResponseEffectPlans、L12RuleKernelIntegration 的现有事务链，抽取主动/反击的无副作用资格评估结果，供按钮、候选、提交拒绝原因共同消费。提交与结算仍基于当前权威状态复验，不信任旧按钮快照。

现有 BeginActiveAbility 仍分别调用 Starter、Public、S2、S1 等声明入口；PublicResponsePlans 仍独立组织候选和标签。这是需逐能力核对的迁移边界，不因存在卡号分支就认定规则错误。现有 AddPresentationEventById 分别接收日志 text 和冻结 EffectText，后续应由实际效果段/分支结果驱动两种投影，不能用卡文代替实际结果。

## 统一契约

- 静态定义：卡号、能力/段 ID、时点、必发/选发、费用、候选/目标条件、响应范围、分支、展示模板与公开等级。
- 执行状态：来源实例、控制者、事务 ID、当前步、声明目标、已付费用凭据、无效范围、实际结果、规则/展示版本。
- 终态区分：不能发动、选择不发动、被无效、无可执行对象、成功或部分结算。
- 私密候选仅在授权阶段生成；不可响应试炼、结算期选择、后续独立段以显式策略表达，不强制改成单一线性流程。
- 重复提交不得重复支付/结算/发事件；恢复保留支付与待处理段；跨版本恢复不能用新卡文解释旧事务。

## 类型与验收

全池按卡号+能力+段登记：登场、阵亡/离场、进攻/伤害、主动、反击、试炼/符文、费用/士气、目标替换/免死/击杀、天灾、多段分支。同卡允许多类型。

每类映射现有候选、支付、响应、结算及展示入口，记录具名测试：正例、无目标、被无效、重复提交、重连。不可响应类型验证其不能被无效，不伪造不适用场景。覆盖必须断言费用、栈、Prompt、区域和各视角事件；文件存在或卡号有入口不等于全生命周期通过。

顺序：主动/反击 → 多段分支 → 费用/普通及临时士气 → 登场/阵亡/离场 → 目标替换与无效 → 试炼/符文/天灾。逐批迁移同类并完成 Focused/Batch 门禁，不重复开发已验收功能。

## 首批实际进度

2026-09-10：新增 QuoteActiveMorale 作为公共主动士气报价。CommitActiveAbility 与已映射的正基础士气费用按钮共享基础费、主宰免耗和傲慢附加费计算，提交仍重新计算；荷鲁斯替代支付及未映射费用保留原有分支。4 个具名组合测试验证普通/傲慢×免耗/不免耗的按钮、缺费提示、实际扣费和重复命令拒绝，已通过。完整 Batch 规则2823/2823零跳过及全池静态门禁通过；反击、候选统一、段事件/动效/日志迁移尚未完成，不宣称全生命周期同源。

## 后续范围

第三批：余下四个旧单步调用已核对。奥尔加、太阳回牌、安卡抽牌已接公共候选；天照两步公共声明保留，旧单目标兜底删除。专项累计17/17通过，完整Batch2840/2840零跳过及全池静态门禁通过。当前PromptActiveTarget仅由公共开始函数调用，但其他多步或直接BeginPendingActivation仍需逐类迁移，不能把这个调用计数视为全部主动效果完成。

下一批先对四种 PublicResponsePlans 提取无副作用声明评估，让初选与提交复验共用候选/模式条件；公开提示按响应者投影，匿名手牌仍只能在授权时暴露匿名槽位。禁止把主动CanAct条件套到响应窗口。每种增加正常/失效/重复/恢复测试，再讨论结果事件同源。

第二批单步候选：主入口5项已共享候选计划（须佐两项、山河弃牌检索、草薙两项），10例异常/恢复专项通过，最终Batch2833/2833零跳过通过。首轮数据库清理占用失败已记录，独立与全量复跑均通过。

下一批方案：先核对 S1Faction 的4处 PromptActiveTarget 与其优先 PublicActiveDeclaration 之间的实际路由，避免把多步声明降为单步；随后把反击的响应对象/费用可行性投影接入同类评估结果。再给实际结算事件关联段 ID、分支 ID 与结果状态，让动效/日志从结果投影，保留冻结版本及隐私边界。当前不得宣称卡文/按钮/动效/日志已经全部同源。

排位仅离线比较当前公式、宽松成长、稳定竞争；区分七曜值、段位、赛季记录，审计有效对局和处置排除。需历史脱敏输入后才能给出模拟结论，不立即改线上分数。

仪表盘仅后台使用，展示样本、置信度、版本/时间范围与排除原因，不新增玩家评分、推荐卡组或自动强度结论。

## 效果一致性完成后的阶段：后台卡效工作台

状态：已纳入本次实施计划，作为效果生命周期、逐能力结构和展示同源完成后的正式阶段；当前不抢占对战可靠性与效果一致性优先级，不提前把尚未稳定的运行协议固化进编辑器。

目标是让维护者在后台基于现有原子化能力自行完成卡牌规则和呈现配置，不再依赖逐条对话修改代码。后台必须围绕同一份带版本的结构定义集纳：

- 卡牌基础资料、所属产品/收录产品选择、版本、印刷状态与生效范围；
- 按独立能力和效果段撰写的卡文，包括时点、必发/选发、冒号前Cost、候选、目标、响应、分支、结算、状态与公开等级；
- 勘误记录，包括原文、新文、原因、生效版本/时间、影响产品、关联能力段、历史修订与回退记录；
- 卡牌按钮、发动/支付/目标/响应弹框、成功/跳过/无效/失败日志，以及回放摘要所用的分支化文本模板；
- 一张卡或单个效果段的呈现样式，包括卡牌展示、来源强调、目标高亮、费用支付、响应、分支选择、结算与结果动效。

呈现样式不得让维护者面对无说明的内部枚举。每一个可选样式都必须在选择器内提供名称、适用场景、静态图例或可播放范例、桌面与移动端预览，以及公开信息说明；需要参数的样式同时展示默认值和当前效果的实际预览。范例素材随样式版本管理，删除或修改样式时不得破坏旧录像和进行中对局的冻结呈现。

后台采用“卡牌 → 能力 → 效果段 → 运行步骤/展示投影”的层级编辑，提供图鉴卡文、玩家按钮、各阶段弹框、响应方视角、动效、日志和回放的并排预览。界面只提供受约束的原子、条件、目标、费用、响应与样式选项，不允许录入任意服务端代码；高级规则无法表达时进入待开发状态，不能以自由文本冒充已接运行时。

发布流程固定为草稿 → 结构校验 → 场景预演 → 人工复核 → 生成差异 → 发布。校验至少覆盖能力分段、Cost可支付、候选与目标、公开/私密信息、响应范围、无对象策略、重复提交、恢复版本、文本变量和样式兼容性。已发布版本不可原地覆盖；进行中对局、录像和日志绑定冻结的规则/展示版本，并支持审计、回退和导出。

### 后台布局与视觉规范

- 先建立并复用后台设计系统：按钮层级、尺寸、间距、圆角、颜色、图标、表单、卡片、标签、弹框、空状态、错误状态和响应式断点统一；不得为单个功能临时拼接一套按钮样式。
- 主操作、次操作、危险操作和只读状态必须有稳定层级；同一区域的按钮高度、对齐、文案密度和点击区域一致。类似近期“改选派系”这种新增入口必须回归统一组件后才能验收，不以功能可点击代替视觉合格。
- 复杂编辑器使用清晰的分栏、折叠与固定预览区，避免一屏堆满字段；窄屏允许转为单列，但不得遮挡保存、校验、预览和返回入口。
- 每次新增后台功能必须同时提交桌面、窄桌面和平板/移动宽度的真实渲染证据；检查深色主题、长中文、空数据、错误提示、键盘焦点和触控区域。视觉验收与功能测试同为发布门禁。

### 分期与验收

1. 在效果一致性完成后冻结第一版规则/展示Schema及版本策略。
2. 建立只读卡效工作台，先汇总现有原子能力、卡文、勘误、产品、弹框、日志和呈现来源，并标记仍属硬编码或重复来源的字段。
3. 开放草稿编辑和全视角预览；先覆盖新增卡和高风险效果，不一次迁移全部旧卡。
4. 接入校验、测试场景生成、审核、差异、发布、回退和审计权限；后台发布仍须通过现有Focused/Batch/Release门禁。
5. 最后迁移存量卡，并持续禁止运行时按卡文猜测规则或由多个界面各自维护文案。

完成标准：维护者可以在后台选择产品、拆分能力、配置Cost/目标/响应/分支、撰写卡文与勘误、选择带图例的呈现样式，并同时预览按钮、弹框、动效、日志与回放；一次审核发布生成同一版本的运行定义和各界面投影。任一字段超出Schema、预览与运行结果不一致、缺少移动端/视觉证据或旧对局无法恢复时，均不得发布。
