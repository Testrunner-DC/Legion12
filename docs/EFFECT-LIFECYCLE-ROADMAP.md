# 效果一致性实施计划

> 本文件只回答四个问题：现在完成到哪里、质量如何、下一步做什么、什么条件下才算完成。
> 详细批次记录见 [任务台账](TASK-LEDGER.md)，逐项修复证据见 [Bug 修复记录](BUGFIX-REGISTRY.md)，最终验收定义见 [完成定义与验收矩阵](EFFECT-CONSISTENCY-COMPLETION-PLAN.md)。

## 一眼结论

| 项目 | 当前状态 |
| --- | --- |
| 总体结论 | **P0—P6 未完成。** 当前只有生命周期档案矩阵可以给出可复算百分比。 |
| 可复算进度 | **52 / 78 个生命周期档案完整退出（66.7%）；26 个未完成。** 此比例只表示档案矩阵闭环率，不等于代码量或卡牌数完成率。 |
| 权威基线 | 324 张卡、681 个能力段；403 段共享规则归属、194 段复合定义、84 段细定义、0 段未归属。 |
| 当前工作树 | 独立工作树 `effect-consistency-p0-p6`，基线 `be0d8b8`；本批未提交、未推送、未部署。 |
| 当前批次 | **台账零未归属及三项用户裁定收口。** 众神之乡的陵墓守卫场上兵力/费用光环、哮天犬与王者之剑的 Limit 1 衍生卡身份、阿尔忒弥斯最终勘误卡文及翻转士气语义已进入同一批验证；规则 Batch 4583/4583、全卡静态审计、UI 契约 332/332、台账 17/17、矩阵 5/5 已通过。 |
| 下一动作 | 清理本地生成物、隔离提交并执行提交级 Release（含独立前端依赖与生产构建）；随后按矩阵关闭 `composite:counter-deployment` 与 `continuous:printed-range` 的缺失项，再继续资源/主动/公开触发管线。 |
| 真实阻塞 | **无。** 当前没有待用户裁定、待授权或外部依赖阻塞；测试与批次边界不是停止条件。 |
| 一致性完成后 | **唯一后续路线：后台卡效工作台。** 把效果、勘误、产品和全部呈现来源纳入同一结构化维护流程。 |

## 当前进度

阶段百分比暂停人工估算；只使用自动矩阵的 **52/78（66.7%）** 作为可复算进度。每个 P 阶段仍按退出条件判定，不因单个档案完成而折算“已完成一部分 P”。

| 阶段 | 当前结论 | 退出前仍需完成 |
| --- | --- | --- |
| P0 卡池台账 | **未退出；未归属已清零** | 681 段已全部归入共享规则、复合定义或细定义；仍须把 278 个细定义/复合定义逐段映射到生命周期档案、运行入口和具名证据，并清除只靠卡号分支而未列档的入口。 |
| P1 共用生命周期 | **未退出** | 仍有 26 个档案未闭环；清理分散资格/支付/结算判定，统一拒绝、跳过、失败、无效和取消事实。 |
| P2 对象复验 | **未退出** | 按矩阵继续补齐区域/类型/条件复验，唯一对象仍选择，逆结算失效不换对象补位。 |
| P3 费用条件 | **未退出** | 完成主动休整、复合费用、唯一费用自动支付、失败取消兜底及已支付不退款的逐能力证据。 |
| P4 触发分支 | **未退出** | 继续登场、阵亡/离场、进攻/击杀、试炼、天灾和多段分支的响应边界与恢复矩阵。 |
| P5 展示同源 | **未退出** | 矩阵中所有 `presentation-consumers` 缺口清零；按钮、弹框、动效、日志、战报和回放按实际分支共用来源。 |
| P6 恢复发布 | **未退出** | 78 个档案全部补齐最低异常矩阵，再执行全量 Release、产物核对和线上验收。 |

## 质量审查

### 已达到的质量

- 自动台账覆盖 324 张卡和 681 个能力段，不允许无能力卡或同卡同触发能力静默消失。
- 能力使用稳定 ID 和结构哈希；共享档案绑定精确能力，不能因同卡、同触发或相似文字误继承。
- 台账明确区分“定义/归属证据”和“完整执行回执”，不会把文件存在或测试名称当成生命周期完成。
- 当前生命周期生成门禁通过：681 个能力段、78 个档案、52 个完整退出，且精确证据不得通过同卡或参数化测试误继承。
- 本批精确后端 82/82、规则 Batch 4583/4583、全卡静态审计、UI 契约 332/332、台账 17/17、矩阵 5/5 已通过。当前工作树缺少本地 `node_modules`，Batch 的前端生产构建在依赖解析阶段停止；由提交级 Release 在隔离目录执行 `npm ci` 后完成，尚不登记为可发布、可部署或 P6 完成。

### 仍需警惕的问题

- **台账归属不等于效果完成。** 403 个共享归属之外，278 个细定义/复合定义仍需按完整生命周期证明；未归属虽已清零，已有定义或运行入口仍不能直接视为验收通过。
- **测试总数不能表示矩阵覆盖率。** 当前自动矩阵已能逐档案计算正例、无目标、无效、目标失效、重复提交、重连和展示消费者；当前仍有 26 个档案不齐全。
- **结果语义仍未完全统一。** 服务端仍有大量历史 `effect-cancelled` 写法，其中部分实际表达目标失效或结算失败，需要逐项分类，不能批量改名。
- **展示仍存在多来源。** 卡文和部分范围已经结构化，但按钮、弹框、日志、动效和回放尚未全部从实际能力段/分支投影。
- **文档与发布状态必须分开。** “进入主线”“Release 通过”“已部署”“维护已解除”是四个不同状态，不再在同一批次描述中混用。

## 下一步执行队列

除规则裁定、新权限或真实外部阻塞外，以下队列连续执行；Focused、Batch、Release、提交和记录只是检查点，不是停止条件。

### 1. 按权威矩阵清零剩余 26 个档案

矩阵生成器已建立，现在的主任务是按缺失项逐族清零，不再重复搭建第二套清单。顺序为：

1. 结构化持续战斗与关键词；
2. 主动休整、士气/神力/符文及复合费用；
3. 公开/私有对象声明与逆结算复验；
4. 触发、响应、天灾和多段分支；
5. 展示消费者与最终恢复/发布验收。

每一族必须同批完成公共入口、精确证据、矩阵重生成和记录；不以单卡通过或累计测试数代替退出条件。

### 2. P1 结果语义统一

按真实语义审计旧结果出口：

- 不能发动：资格阶段拒绝，不建立效果声明；
- 选择不发动：`effect-declined`；
- 无对象或必发但无事可做：`effect-noop`；
- 已声明后对象/来源/条件失效：`effect-failed`；
- 响应成功无效：`effect-negated`；
- 仅真正的事务取消保留 `effect-cancelled`。

验收：同一结果只由一个公共出口产生；服务端事实、GM/审计和测试可区分；玩家战报可按展示策略隐藏技术结果，但不得改变底层事实。

### 3. P2 剩余对象族

依据完成矩阵，按“区域 + 对象协议”分族，不按单卡修补：

1. 私有区已声明对象：手牌、墓地、牌库；
2. 公开区类型变化：临时成为军团、主宰/圣物离开临时形态；
3. 单目标、独立多目标、固定数量和“至多”数量；
4. 目标替换、占位失败、来源失效和不得补位；
5. 唯一合法对象仍必须由玩家点击的入口。

每族必须同批完成代码、具名测试、台账和记录。

### 4. P3—P4 费用与触发族

- 费用：士气、临时士气、神力、符文、弃牌、自伤、返还、墓地代表值和复合费用。
- 触发：登场、阵亡/离场、进攻/击杀、主动、反击、试炼/符文、天灾、多段分支。
- 统一冒号边界、支付时点、响应范围、次数身份和“随后”依赖；卡文未写次数限制时不得擅自添加。

### 5. P5 展示同源

每个效果段提供同一份结构化来源：卡牌/实例、段 ID、时点、必发/选发、Cost、候选、目标、响应范围、分支、实际结果、公开等级和版本。

图鉴、按钮、选择弹框、响应弹框、动效、日志、战报和回放只做对应视角的投影，不各自维护规则或文案。

### 6. P6 最终验收与部署

- 每个生命周期档案通过最低异常矩阵；
- 全卡台账无未归属项，或保留项具有明确裁定、负责人和目标版本；
- 使用保存的状态/回放夹具完成最终回归；
- 完整规则、平台、UI、连接恢复、卡图、生产构建和发布包全部通过；
- 部署后核验 HTTP、WebSocket、版本、卡图、服务重启次数和维护状态。

## 当前需要裁定

**无。** 众神之乡已按 2026-09-24 最新补充实现为战场陵墓守卫兵力 +1000、当前费用 +1；费用修正只在场时存在，离场后清除。发现新的规则歧义时才在此登记，不以测试、提交、Release或批次完成充当停止理由。

## 最近批次

| 日期 | 批次 | 状态 | 关键结果 |
| --- | --- | --- | --- |
| 2026-09-24 | 台账零未归属＋众神之乡/衍生卡/阿尔忒弥斯 | 独立工作树进行中，未提交/未部署 | 权威矩阵 52/78（66.7%），未归属 1→0；众神之乡场上费用光环、哮天犬/王者之剑 Limit 1 衍生身份和阿尔忒弥斯最终勘误进入同批；规则 Batch 4583/4583、全卡静态审计、UI 332/332、台账17/17、矩阵5/5。前端完整生产构建待隔离 Release。 |
| 2026-09-23 | granted 战斗关键词 | 独立工作树进行中，未提交/未部署 | 权威矩阵 42/77；granted 挑衅、冲锋、必中、免死、贯穿全部闭环；修复限时必中已生效但战场不展示的分叉，定向及相邻回归 23/23。 |
| 2026-09-23 | P0—P6 矩阵闭环第一组 | 独立工作树进行中，未提交/未部署 | 权威矩阵阶段值 29/77；闭环持续/战斗关键词、附着、击杀授予、伊西斯开局、瓦尔基里抽牌阶段替代及安德华拉诺特首次伤害替代。 |
| 2026-09-23 | P0 长尾归属与天灾裁定 | 已进入 `origin/main`，未部署 | 归属 168→401，待核对 237→1；规则 4499/4499。 |
| 2026-09-22 | 五项响应/Cost/失败去向 | 已部署 | 绝对防御、戏法师的傀儡、芬尼亚传奇、锡瓦的卡巴、阿尔忒弥斯；线上版本 `fd0d602`。 |
| 2026-09-21 | 同刻触发与效果击杀续程 | 已进入主线 | 后序触发读取最新状态；致命替代完成后再恢复父效果。 |
| 2026-09-20—21 | 资源、主动休整、目标复验多族 | 已进入主线 | 神力/士气/临时士气、主动休整、公开/私有对象等共享出口持续收口。 |
| 2026-09-19—20 | 响应范围、S1 卡文与反击身份 | 已进入主线 | S1 新换行、305 张非天灾响应范围、反击身份和公共禁用结构化。 |

更早和更细的批次记录不再堆叠在本文件中：

- 实施与接收历史：[TASK-LEDGER.md](TASK-LEDGER.md)
- Bug、根因和验证证据：[BUGFIX-REGISTRY.md](BUGFIX-REGISTRY.md)
- 当前自动能力台账：[l12/EFFECT-ABILITY-INVENTORY.md](l12/EFFECT-ABILITY-INVENTORY.md)
- 完成定义：[EFFECT-CONSISTENCY-COMPLETION-PLAN.md](EFFECT-CONSISTENCY-COMPLETION-PLAN.md)

## 执行纪律

- 同族扫描、修复、具名测试、Batch、记录和提交必须同批完成，不为单个入口重复跑完整发布验证。
- 只在最终退出码和汇总核实后登记通过；进程启动或部分输出不能计作成功。
- 保留其他任务的工作区修改，不整库覆盖、重置或清理无关差异。
- 新卡或改卡必须先绑定既有生命周期档案；确实无法表达时新增共享协议，不为单卡复制一套流程。
- 卡效后台工作台安排在 P0—P6 完成后：届时再开放效果、勘误、产品、弹框/日志和带图例呈现样式的结构化编辑与发布。
- 本计划在 P0—P6 完成后只续接后台卡效工作台；排位、教学、玩家侧仪表盘及其他产品规划不属于本路线，也不会自动进入下一阶段。

## 完成标准

只有以下条件全部满足，才能标记“效果一致性完成”：

1. 324 张卡的全部能力段均有生命周期档案、运行入口和具名证据；
2. P0—P6 全部退出，未处理清单为零或具有明确产品裁定和目标版本；
3. 不能发动、拒绝、跳过、失败、无效和取消在服务端事实中可区分；
4. 按钮、弹框、动效、日志、战报和回放读取同一能力段及实际分支；
5. 每个生命周期档案通过最低异常矩阵；
6. 完整发布门禁和线上部署验收通过。

在此之前，只能声明某个阶段或某个生命周期族完成，不能笼统宣称整个效果一致性完成。

## 效果一致性完成后的唯一路线：后台卡效工作台

本节是 P0—P6 完成后的唯一续程。此前讨论过的其他产品方向不在本计划中保留；如未来需要推进，必须另行立项，不与效果一致性或后台卡效工作台混合验收。

目标：维护者能够在后台完成卡牌规则和呈现配置，不再依赖逐条对话修改代码。

统一维护的内容：

- 卡牌基础资料、所属产品、收录产品、版本和生效范围；
- 按“卡牌 → 能力 → 效果段”拆分的时点、必发/选发、冒号前 Cost、候选、目标、响应、分支和结算；
- 勘误原文、新文、原因、生效版本、影响产品、历史修订和回退记录；
- 按钮、发动/支付/选择/响应弹框、动效、日志、战报和回放文本；
- 每种呈现样式的名称、适用场景、公开等级、参数和桌面/移动端预览。

呈现样式选项必须附带静态图例或可播放范例，不能只显示内部枚举名称。后台复用统一设计系统，按钮、间距、弹框、表单、错误状态和响应式布局不得为单个功能临时拼接。

发布流程固定为：

`草稿 → 结构校验 → 场景预演 → 人工复核 → 差异确认 → 发布 → 审计/回退`

已发布版本不可原地覆盖；进行中对局、录像和日志绑定冻结的规则与展示版本。无法用现有结构表达的规则进入“待开发”，不得以自由文本冒充已接入运行时。

完成标准：维护者可在后台配置 Cost、目标、响应、分支、卡文、勘误、产品和带图例的呈现样式，并同时预览图鉴、按钮、弹框、动效、日志和回放；一次审核发布生成同一版本的运行定义与全部界面投影。
