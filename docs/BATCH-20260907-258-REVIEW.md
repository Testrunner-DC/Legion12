# BATCH-20260907-258 后台报告修复验收清单

本批从迁服核验暂停后恢复。正式服务仍为 `cb14e6b07d36fd8df1d1961b6a094d4f0b2577a9`；下述本地修复不能提前标记线上已解决。协作者另行实施的16:9布局独立验收，不覆盖或擅自发布其改动。

## 报告与验收对应

| 报告后缀（均为 BUG-20260906） | 本地修复结果 | 具名回归/证据 |
| --- | --- | --- |
| ed706387 | 自动入局同时对账路由与已确认权威快照；移除App重复入口，主动退出后同局重复快照/重连ack不回跳 | `check-l12-game-reentry.mjs` 6/6，含延迟路由、重复ack、导航取消重试、回放/离线/主动退出 |
| 292c4feb | 雷神索尔赋予冲锋覆盖效果登场，并按有效阵营判断；不误消费仅手牌打出适用的下一张军团加成 | `ThorChargeAppliesToAnAsgardLegionSummonedFromTheGraveyard` |
| 11adbd08、6d0f505d | 黑胡子登场整项只有一个可无效窗口，被绝对防御无效后不继续抽牌；正常结算仍先弃牌再按双方规定抽牌 | `AbsoluteDefenseNegatesBlackbeardsWholeEnterEffectIncludingTheDrawClause`、`BlackbeardStillDiscardsAndDrawsNormallyWhenItsEnterEffectResolves`；后者报告关联前者为同场重复 |
| 927ce3ae | 卡纽特大帝的待声明与完整效果链处理完毕后才继续翻天灾 | `DisasterWaitsForCanutesWholeEnterEffectDeclarationAndResolution`；已核对原录像公开事件顺序 |
| 93054472 | 安德华拉诺特（S02-0305）构筑上限1张，赛季配置不能将固有限制放宽为3张 | `LimitOneCardsAreRejectedAtTheSecondCopy`、`BloodRingPrintedLimitAppliesEvenWhenSeasonAllowsThree`；1张通过、2张拒绝 |
| a77fb6e2 | 为选中手牌动作、上浮和倾斜预留真实布局空间，避免常驻计时遮挡按钮；包含圣物/战术及GM对方手牌 | `verify-batch258-hand-actions.mjs`：6个尺寸/张数/卡类/GM组合，按钮命中、计时不交叠、场格不越界；协作者新布局须重新通过 |
| 7e27c502 | 乾坤·阳依据军团形态原本兵力判断，草薙剑的5000不会被当成0；同类型原本兵力条件同步修正 | `QianYangCannotSelectKusanagiWhoseLegionFormHasOriginalTroopsFiveThousand` |
| b130b777 | 步行者罗洛的候选、选择验证、费用提示一致使用有效阵营，魔戒赋予阿斯加德的通用牌可参与 | `RolloTreatsUniversalGraveCardsAsAsgardWhileTheRingIsActive` |
| 82e6141a | 荷鲁斯费用军团及阿尔伟达自身将腾出的格可用于后续登场；取消预声明不提前扣费 | `HorusCanChooseADeclaredCostSlotOnAnOtherwiseFullBattlefield`、`AlvidaCanChooseHerOwnCostVacatedSlotOnAnOtherwiseFullBattlefield`、`CancellingAlvidasProspectiveSlotReleasesTheWholeUncommittedDeclaration` |
| 17333d1f | 哮天犬衍生物阵亡消失后仍按死亡快照处理加入休整士气，不误入另一同卡号回场分支 | `VanishedXiaotianStillAddsTappedMoraleFromItsDeathSnapshot` |
| 3ae40480 | 悬停自动放大为此前明确拒绝的重复建议，不开发 | 发布收尾时关联旧拒绝记录并说明原因，不伪装代码修复 |

除重入/手牌脚本和构筑测试外，上述具名规则用例位于 `TwelveLegions.Tests/BackendReportRegressionTests.cs`。被纠正的旧黑胡子测试保留正常路径覆盖，不以删除失败测试代替修复。

## 证据边界

- 暂停前共享Batch结果：规则2406/2406、指定PlatformStore/ControlPlane74/74、UI276、卡图40项/324张、音乐行为、原子审计、Vue类型和生产构建通过，日志为仓库外 `D:\GPT\Legion12\artifacts\batch258-batch.log`。
- 恢复后的最终共享Batch已通过：规则2410/2410、指定PlatformStore/ControlPlane74/74、UI278、字号违规0、卡图40项/324张、音乐行为、重入6/6、原子审计、Vue类型和Vite241模块构建。日志为 `D:\GPT\Legion12\artifacts\batch258-resume-batch.log`。总UI契约已迁到单一重入协调器；新增卡纽特放弃后恢复天灾、腐秽大地后排排除、跨战场格位隔离、支付后格被占用不覆盖/不退款及雷神单次事件边界。
- 冻结后的手牌动作专项6/6通过，证据目录 `D:\GPT\Legion12\artifacts\batch258-resume-hand-actions`。开发服务捕获两次ResizeObserver循环提示，不宣称零浏览器提示；GM窄窗大量手牌左缘和GM面板覆盖右栏仍属独立UI批次未决项，不以按钮命中测试代替完整视觉验收。UI任务确认保留现状，待用户另行确认该边界优化。
- 部署脚本最后追加既有阻断标记测试后，主任务独立重跑 `test-l12-deploy-behavior.ps1 -FixtureBase D:\GPT\Legion12\artifacts\temp` 通过：deploy与dry-run均拒绝未对账标记，活动版本/runtime/标记保持不变，不停启服务或创建release暂存目录。本次未运行提交级Release。
- b130b777、82e6141a、17333d1f 的同场录像在正式库缺失，已通过实际数据库检查确认；具体原因未决。卡效采用确定性合成场景进行红绿验证，不声称已用缺失录像回归。迁服内部证据见 `MIGRATION-AUDIT-20260907.md`。
- 本地部署脚本的主机绑定、版本身份检查与失败保留数据保护属于独立运维子项；没有实际运行部署或回滚。
- 未达到部署版本核验前，不把上述报告写为resolved；重复、拒绝项目分别写明关联与理由，不一键全关。新机现有SSH权限不能等同本批生产发布授权。

## 面向玩家的更新日志草稿（待实际发布时使用）

- 卡牌修复：雷神索尔赋予的冲锋正确覆盖效果登场；黑胡子登场效果被无效后不再继续抽牌；卡纽特大帝效果完整处理后再翻天灾。
- 卡牌修复：乾坤·阳等“原本兵力”判断正确识别草薙剑军团形态；步行者罗洛识别魔戒赋予的阵营；哮天犬阵亡后正确加入休整士气。
- 支付与构筑：荷鲁斯、阿尔伟达可使用支付费用后腾出的合法登场格，取消预选不会提前扣费；安德华拉诺特构筑限制为1张。
- 连接与操作：修复权威快照已恢复却停留大厅的问题；主动返回后不会被同局重复快照拉回；手牌动作按钮和双方计时保留独立空间。
- UI和维护展示的实际上线内容，须在协作者对应批次验收与发布范围确认后另行加入；本草稿不证明任何功能已经上线。
