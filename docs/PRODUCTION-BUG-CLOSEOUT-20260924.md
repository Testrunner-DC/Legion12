# 正式服后台 Bug 清单收口（2026-09-24）

本文件是 2026-09-24 后续处理的版本化结论；首次拉取与用户逐项反馈仍保存在外部报告 `artifacts/reports/PRODUCTION-BUG-QUEUE-20260924.md`。

## 2026-09-26 发布状态

- 正式服已部署 `08cd7af644106ce8c723d24af5230c05c4db437a`；健康、首页、卡牌页、WebSocket 和玩家更新日志注入均匹配，维护按用户最新指令保持 `true`。
- 下列 25 项工程修复已经跨过“未部署”门槛，但维护期内尚未逐一重建全部原对局场景；按照关闭纪律，不以版本号和自动回归代替线上原场景证据，当前仍保持待复测。
- 正式后台账号 `Aimin` 的现有凭据返回 `authentication_failed`，因此未调用 Bug 状态写接口，也未绕过权限直接修改生产数据库。24、27、29 的裁定结论和30的性能归档关系均已记录，但后台状态仍需有效管理员授权后同步。

## 当前分组

- **工程修复已部署正式服、等待原场景复测后关闭（25项）**：01—09、11—15、17—21、23、25、26、28、31、32。
- **需要移动真机复测（3项）**：10、16、22。未取得真机证据前保持待复测，不以桌面模拟结果关闭。
- **无需改代码，可直接记录结论后关闭（3项）**：24、27、29。
- **转入性能治理专项（1项）**：30。它不再作为单点页面 Bug 重复修补，以资源版本推送、断线降级和轮询门禁统一验收。

## 重点顽疾证据

- 非被进攻位置错误亮起：根因是空格位缺失编号比较及普通布阵高亮未在战斗态隔离；主修提交 `a772582`，后续加固 `d37e59b`。验收要求为主宰抵挡、军团支援及四档比例下，只有真实被进攻对象显示金色战斗目标。
- 触发显示顺序、防御登场卡名、猎杀时刻取消语义、图特摩斯三世、多选构筑卡池、主宰榜排序和免死裁定均已有具名回归；仍须随包含这些提交的正式版本部署并在原场景复测。

## 关闭纪律

后台报告只有同时满足以下条件才改为 `resolved`：

1. 原场景有具名自动回归或确定的真机复测证据；
2. 修复提交已进入正式服实际版本；
3. 公网复测结果与版本号已记录；
4. 重复项已关联主要报告。

因此，本次是工程清单收口，不会提前批量关闭正式服后台记录。
