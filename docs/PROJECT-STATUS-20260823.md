# 十二军团 Web 项目状态（2026-08-23）

> 本文是项目级状态基线。每批功能、卡效迁移或基础设施工作完成后更新；具体 Bug 仍以 `BUGFIX-REGISTRY.md` 的追加记录为准。

## 2026-09-03 更新 105：牌库编辑器与牌库图额外区统一

- 牌库编辑器的当前主宰头像、名称、阵营和士气数已移至中间 Card Pool 大盒顶部并左对齐，左栏只显示当前选中卡牌详情；右下显式试炼与主宰自动额外卡和主牌列表复用同一横向卡图条目。
- 主宰自动额外卡映射已集中到 `decks.ts`；当前伊西斯自动携带〈复苏的奥西里斯〉，编辑器与牌库图导出读取同一公共函数。牌库图同时包含 `specialIds` 和自动额外卡，横版试炼按 `1752:1255` 正确绘制。
- UI契约新增4项并通过195/195，卡图架构28/28（324张）、Vue TypeScript和Vite生产构建通过，`git diff --check`无空白错误。本批仅保留本地改动，未提交、未推送或部署。

## 2026-09-03 更新 104：银臂努阿达能力勘误与符文事件粒度

- 〈银臂努阿达〉文本、玩家按钮和人工原子定义已统一为最新版本：消耗符文时按每枚符文分别产生一次可选兵力+1000触发，不再限定我方回合；主动能力消耗2符文后将最多2张士气转为活跃，并固定继续结算试炼+2。
- 公共 `SpendRunes` 现在按实际消耗数量登记符文消耗事件；一次支付2符文会形成两个独立同时时点触发，玩家可分别发动、分别选择目标，也可两次选择同一张彼界军团。扫描确认所有符文支付入口均复用该公共函数，事件消费者仅银臂努阿达，不会改变其他卡牌的结算内容。
- 回归覆盖对方回合触发、两枚符文形成两次触发、同一目标累计+2000、主动能力选择0/2张士气、试炼+2，以及卡表换行和原子定义。专项20/20、完整L12规则1159/1159、平台持久化与控制面60/60、ST目录3/3、原子审计324张零旧分支、UI契约191/191、卡图28/28（324张）及前端生产构建通过。本批按用户要求仅保留本地改动，未提交、推送或部署。

## 2026-09-03 更新 103：效果选项、附叠强攻与预览契约收口

- 全卡池效果选择保留内部 `mode:*` 协议，但玩家按钮统一显示卡面对应效果分句或准确费用动作；梅杰德不再出现“普通/强模式”，迦具土稳定按“消耗2士气、弃置1张手牌、不发动”横向排列，拒绝项固定最后，窄屏横向滚动。
- Prompt 预览改为显式呈现契约：效果决定和场面目标不再自动显示来源卡面；李靖、阿麦金、李牧、冲田等正在查看/处理的公开卡使用 `handled-card`，天灾公开信息确认使用 `information-card`。既有13处/7文件 `previewCardId` 已逐项分类。
- 〈王者之剑〉与〈侵略如火〉附叠强攻共用唯一有效关键词查询，战斗伤害、场面关键词和临时赋予读取同一来源；自身、临时及多个附叠来源并存仍只显示一次“强攻”，对主宰最多额外造成1点伤害。7个运行时附叠入口继续复用公共圆形附叠卡展示和详情。
- 静态门禁覆盖服务端所有选项标签、横向布局、拒绝项顺序、预览白名单及强攻统一查询；Focused与Batch规则均1159/1159，UI契约191/191、卡图架构28/28（324张）、原子审计324张零旧分支、Vue TypeScript和Vite生产构建通过。本批未提交、未推送、未部署。

## 2026-09-03 更新 102：卡牌选择 Prompt 全类型统一

- 服务端 `CreatePrompt` 改以真实卡实例和显式展示/预览卡号识别卡牌选择，统一补齐卡号、名称、图片、类型和区域；已扫描全部138个定义/调用行与31种字面 kind，排序、试炼排序和可选多卡已覆盖，非卡 `trigger-order` 不会误判。
- 前端新增共享卡牌候选组件，普通选择、非法灰置、回顶/回底排序、独立预览和起始调度共用卡图在上、卡名与区域在下的横向滚动视觉；仅有 `cardId` 时仍可解析真实卡图，混合 Prompt 的跳过/不发动入口继续可用。
- 隐藏对方手牌仍只显示匿名槽位、正式卡背和“匿名手牌”，真实身份只保存在服务端映射；真实快照测试覆盖顶3、弃置、墓地、检索、非法项、排序、预览与隐藏信息。
- 专项10/10，Focused与Batch规则均1157/1157，UI契约188/188、卡图架构28/28（324张）、原子审计324张零旧分支、Vue TypeScript和Vite生产构建通过；本批未提交、未推送、未部署。

## 2026-09-02 更新 93：ST新卡第三批A无目标费用与伤害后触发

- 刘季、维京狂战士、维京掠夺者完成真实实战入口：下一张军团减费按本回合可叠加状态登记、成功打出军团后消费并在回合结束清零；维京狂战士只在手牌且主宰血量不高于7时减费；维京掠夺者复用公共“对主宰造成伤害后”事件抽2张。
- 新增通用下一张军团减费字段并接入费用预览、实际支付、消费和回合清理；不按三张卡分别建立硬编码结算分支。
- 当前原子统计为324张、细原子72项/64张、待迁移127张、无实战入口18张；旧卡号case与文本推断保持0，卡号条件187、switch分支67未增长。
- 完整Release规则1076/1076、平台60/60、UI契约172、卡图契约24/324、Vue TypeScript/Vite生产构建和提交级发布包全部通过。

## 2026-09-02 更新 92：ST新卡第二批B与试炼值导入防回滚

- 安提诺乌斯、伊丽莎白一世、莫德雷德与布狄卡完成真实实战入口；主宰弃牌事实、条件减费、士气重置锁、骑兵位移、符文/冲锋二选一、当前兵力击杀和限时一次免死均由公共结构化规则执行。
- 同类型目标登场军团10张至此全部拥有已验证入口；新增能力继续复用 `PublicTrigger → PendingActivation → Stack → verified CompositeFlow`，不新增卡号旧分支或效果文本推断。
- 数据库导入器从权威卡面独立行写入试炼值，并锁定ST06-06/07/08分别为1/1/2，避免重新导入时只保留效果文字却丢失结构化数值。
- 当前原子统计为324张、细原子69项/61张、待迁移130张、无实战入口21张；旧卡号case与文本推断保持0，卡号条件187、switch分支67未增长。
- 完整Release规则1072/1072、平台60/60、UI契约172、卡图契约24/324、原子零旧分支、Vue TypeScript/Vite生产构建与提交级发布包全部通过；首次门禁发现新增内部选项缺公共自然语言标签，补齐“获得冲锋”后从头复验通过。

## 2026-09-02 更新 91：ST新卡第二批A带目标登场效果

- 萧何、胡夫、白沙瓦舞蛇人、乔泽与弗蕾迪斯五张新卡完成真实实战入口，统一使用目标声明、冒号前费用、隐藏区域保护、入栈响应和结算管线；胡夫反击保护、舞蛇人私密检索、弗蕾迪斯展示回手均有专项回归。
- ST同类型登场军团扫描命中10张；本批新完成5张，首批已有1张，剩余ST05-07、ST06-01、ST06-04、ST06-07列入第二批B。
- 当前原子统计更新为324张、细原子63项/57张、待迁移134张、无实战入口25张；旧卡号case、文本推断保持0，卡号条件/分支计数未增长。

## 2026-09-02 更新 90：效果选择取消语义与玩家文案公共收口

- `PendingActivation` 取消从默认布尔值升级为明确策略：已有“不发动”的步骤不再自动追加第二个同义选项；必须继续的后续段不可取消；确有“跳过本段/取消整次发动”差异时使用两个不同的自然语言按钮。
- Prompt、选择标签和拒绝消息在玩家可见边界去除“预先声明、公开区域、私密选择、独立段、结算模式”等工程术语，内部协议、元数据与回放仍保留原始结构。
- 代表卡与全局门禁通过；完整规则增至1055/1055，平台60/60，UI172、卡图24/324、原子零旧分支、TypeScript/Vite及发布包构建全部通过。
- 提交 `a8b2d7f` 已同步并部署正式服；主页、卡牌页、健康接口与WebSocket复核通过，部署前持久数据快照及上一版本均保留。

## 2026-09-02 更新 89：ST01–ST06 新产品接入与首批卡效

- 卡池由248张扩展为324张：从最新 `ST01-06-DataBase.xlsx` 导入70张产品卡，补齐六阵营士气，并在前后端、牌库、档案、沙盒、构筑校验、Prompt与部署审计中统一识别ST编号。蛇眼西格德、侵略如火、美杜莎及伊丽莎白一世采用数据库权威文本。
- 76张卡图完成原图校对、内容寻址派生、WebP/AVIF/缩略图及预加载清单，当前本地生产候选324/324、260,676,774字节；尚未上传R2或部署。
- 首批13项无歧义能力进入实战与后台共用结构化定义；原子目录可从数据库的ABILITY编号拆分，`无效果`保持零能力。ST全池远程判定改以“进攻距离+1，远程进攻无损”结构化能力为准，弓手、术师一视同仁，不以印刷职介名称硬判。
- 当前原子统计：324张、细原子57项/52张、复合路由179项/148张、待迁移139张、无实战入口30张。后续按目标效果、费用/替代、主宰/天灾/阵营机制分批写入，不把文本入库等同于卡效完成。
- 验证：L12规则1048/1048，前端UI契约172、卡图契约19/324、Vue TypeScript/Vite生产构建、原子审计与卡图324/324通过。GrandUMI共用套件有10项既有OP/EB失败，未混入本批修改。
- 发布：`a3d176c` 已部署正式服；主页、卡牌页、76张ST目录、324张健康计数、WebSocket及systemd均验证通过。优化卡图版本为 `8096eed784a24263c0d718a42f15636d02c8904dc57508d2a18e1a301eea4ae3`，切换前运行数据快照与上一版本均已保留。

## 2026-08-31 更新 35：首批复合预声明与类型化击杀来源

- 逐卡矩阵改为同时核验主动注册、时机集合、响应池、静态/派生规则、行动/状态、主宰/阵营/试炼/Token/特殊区等权威实战入口，并把测试证据独立列示。74张没有进入两个原子路由文件的卡全部具有权威实战入口，63张另有测试证据；当前无实战入口为0张，不能再把这74张称为“仅入清单/待迁移”。
- 7张复合卡进入公共预声明/事务/独立效果段：公开模式、目标和费用对象在基础支付前锁定，隐藏牌库内容只在对应段结算时选择，每个独立段分别入栈和响应；取消、无效和后续目标失效不部分支付、不悬挂父流程。
- 击杀来源改为类型化事件：直接战斗击杀继续触发印刷【击杀时】并保留FAQ19双方同归；震击等来源卡造成的状态击杀不触发印刷时点，但会满足锻造炉/莫瑞甘赋予的下一次击杀后转活跃，同一效果多杀只消费一次。
- 验证：新增专项5/5、相关278/278；Focused、Batch、Release规则均555/555；平台60/60；原子审计248/248、legacy0、文本推断0；UI契约166、卡图19/19（248张）、Vue TypeScript、Vite生产构建与发布包构建通过。主代理复核通过后按长期授权同步GitHub；本批不部署。

## 2026-08-31 更新 34：邮箱默认关闭与248张卡效审查基线

- 邮箱绑定、验证和找回改为显式开关：`L12_EMAIL_FEATURE_ENABLED` 默认关闭；关闭时前端隐藏所有入口，后端绑定、验证、忘记密码和令牌重置统一失败关闭，既有邮箱数据保持原样。未来只有在开关启用且 SMTP 配置完整后才开放。
- 建立可重复生成的248张逐卡审查矩阵，严格区分41张含细原子接管证据、148张含复合过渡路由和74张未进入这两个原子路由文件的卡；这74张均另有权威实战入口，其中63张有测试证据，0张无实战入口。15张同时含细原子与复合能力，不把局部迁移误报为整卡完成。
- 清除当前全部运行时 `EffectText.Contains` 卡效推断：可选自伤减费、带触发动画天灾、主动休整来源和派生远程/战斗标记改读结构化语义。原子门禁锁定文本推断0处、历史卡号 `case` 0处，并禁止复合路由与待迁移数量反向增长。
- 聚焦验证：邮箱存储与HTTP失败关闭9/9、结构化语义专项、UI契约和原子审计均通过；最终发布级门禁与后台已部署修复状态对账在提交前完成。本批不部署服务器。

## 2026-08-26 更新 33：自动模型路由与触发动画公共闭环

- 新增四档仓库级 Codex 代理：快速、标准、深度、关键；按真实负载将规则卡效默认抬升到标准档，未知根因、跨模块状态机进入深度档，网络一致性、安全及部署进入关键档。
- 根 `AGENTS.md` 固化最小只读分类、最高档优先、不确定向上、超档停止、单执行代理和主代理最终复核规则；增加任务台账、批次验证、回归夹具和防回滚流程。
- 触发动画公共根因修复：来源卡离开手牌/场上/墓地等常规检索区后仍保留触发卡快照；直接入栈的登场、晋升登场、进攻时与对主宰造成伤害时效果也发布统一动画。
- 触发动画下方只显示本次触发的能力原文；移除把整张卡多行效果拼成动画文本的兜底，并为 S01/S02 全卡池触发族建立自动扫描回归。
- 验证：四档代理注册、TOML 解析、严格配置入口和 AGENTS 上下文检查通过；触发专项20/20、完整规则411/411、UI契约93/93、前端生产构建通过，旧卡号分支保持0且 `EffectText` 推断未增长。

## 2026-08-25 更新 32：统一阵亡与离场双时点能力

- 修正〈陵墓构造体〉“阵亡时／离场时”同一能力被重复登记的问题。
- 保留阵亡属于离场的规则关系，同时区分其他卡牌来源的阵亡、离场触发，避免误删合法响应。
- 增加同源能力去重回归测试，后续同类双时点文本必须通过统一触发定义接入。

## 1. 产品范围

项目当前同时承担十二军团官方主页、资讯与规则/FAQ、卡牌档案、牌库编辑与牌库广场、在线对战、单人测试沙盒、对局记录与回放、赛事中心、玩家账号以及管理后台。

移动端专项适配和管理后台的大规模扩展暂不进入当前批次；当前优先保证桌面端规则内核、全卡效、沙盒复现、网络恢复和内容发布链路稳定。

## 2. 当前可用能力

| 模块 | 当前状态 | 备注 |
| --- | --- | --- |
| 官网信息架构 | 已搭建 | 左侧统一导航；进入对战区域后直接以大厅为主页，并统一承载赛事、牌库、排行榜、好友和个人对局记录 |
| 卡牌档案 | 已覆盖 S01/S02 | 共 248 张，支持赛季、类型、费用等筛选；横版卡牌按使用场景适配 |
| 牌库系统 | 可用并持续完善 | 本地/账号保存、编辑、牌库码、牌库广场和牌库图等能力已接入 |
| 在线房间与对战 | 可进行完整流程测试 | 包含房间、天灾准备、调度、阶段、Prompt、响应堆叠、战斗、记录与回放；同账号可在断线后认领原座位并恢复权威快照与 Prompt |
| 单人测试沙盒 | 已修复双方代行 | GM 可查看双方手牌，并以对应玩家身份处理双方 Prompt 和操作 |
| GM 面板 | 已接入 | 复用卡牌档案式选择器，可构造手牌、区域、生命、士气、击杀、横置及自定天灾场景 |
| 赛事中心 | 基础框架已搭建 | 完整赛事组织流程仍需继续验证与补齐 |
| 注册、登录与账号 | 已接入基础流程 | 牌库、好友关系和账号信息可持久化；好友支持在线状态、申请与接受后直接建房 |
| 管理后台 | 权限、发布与审计闭环已接入 | 管理员管理账号/Bug/审计；内容编辑可维护官网草稿并显式发布；卡效支持逐能力人工辅助、确认或退回，全部写入持久审计 |
| 部署体系 | 已建立 | Windows 发布脚本、Linux 原子切换、运行数据保留、健康检查和 WebSocket 探针可用 |

## 3. 规则与卡效状态

| 指标 | 当前值 | 解释 |
| --- | ---: | --- |
| 卡池 | 248 张 | S01 133 张 + S02 115 张 |
| 原子清单覆盖 | 248/248 | 仅表示后台可查看结构化定义，不等于实战完成 |
| 细粒度原子运行时能力 | 44 项，覆盖 41 张卡 | 后台与实战读取同一程序；仅证明对应能力已接管 |
| 结构化复合运行路由 | 179 条，覆盖 148 张卡 | 已脱离历史卡号 `switch`，但仍需按规则语义继续拆分为公共细粒度原子 |
| 未进入两个原子路由文件 | 74 张 | 74张均有权威实战入口；63张另有测试证据，0张无实战入口；逐卡证据见审查矩阵 |
| 历史卡号 `case` 分支 | 0 处（0 个卡号） | 由 `-RequireZero` 门禁锁定，禁止重新引入 |
| 运行时效果文本推断 | 0 处 | 不再以可变卡面文案决定权威规则 |
| 人工审查标记 | 奥林匹斯已扩展 | S02-0501～S02-0523 已提供拆分的卡、两位主宰及神力/士气均区分 `human-assisted` / `confirmed`；与迁移状态分离 |

已建立 `PendingActivation`、`TriggerBatch`、`LibraryOps`、`DerivedStats`、Prompt/Stack、条件战斗配置、派生职介和原子运行时等公共底座。历史卡号 `case` 与运行时效果文本推断均已清零；后续重点是把179条 `CompositeFlow` 结构化复合路由继续拆成条件、费用、目标、结算、持续时间和替代效果等公共原子，并补齐无效、区域移动、隐藏信息、日志和回放边界测试。

独立模型审计已确认的高价值缺口：

- 条件费用与前排挑衅首批已迁移：S02-0509、S02-0510、S02-0512、S02-0518 的神力条件费用由结构化原子驱动；全卡池8张前排印刷挑衅统一读取带排位参数的公共查询。
- 登场获得冲锋与登场回合可进攻军团已迁移；〈梅林〉、〈帕西瓦尔〉、〈高文〉、〈伊丽莎白·都铎〉四张“登场时获得1符文”已迁入公共原子，带可选、费用或后续目标的复合符文能力仍按独立批次迁移。
- “下一张战术无需费用”和无额外费用/目标的可选抽牌簇已迁移；带弃牌、伤害、额外目标或后续结算的复合抽牌仍按独立批次迁移。
- 战斗关键词与条件职介已清除运行时 `EffectText.Contains` 推断；剩余卡号身份与数据映射继续按逐卡矩阵分类，不能机械删除合法身份规则。

## 4. 最近验证基线

完成“沙盒固定视角与双方代行”“场面符文支付”“GM 棋盘选位”“有意义响应窗口”“条件费用与挑衅公共规则”“公开展示卡牌公共动画”、简单可选抽牌原子迁移以及 2026-08-24 三来源 Bug 合并审计后：

- 十二军团规则/实战测试：406/406 通过。
- 原子运行时专项：46/46 通过，历史卡号 `case` 分支由 `-RequireZero` 锁定为0。
- 后台持久化测试：8/8 通过，Bug 闭环、权限变更、官网草稿/发布、卡效人工审查、统一审计和重载均受保护。
- 前端 UI 契约：88/88 通过。
- Vue/TypeScript 与 Vite 生产构建通过。
- 沙盒固定以创建者视角显示：我方始终在下、对方始终在上，双方手牌正面可见，双方 Prompt 和规则动作均由沙盒操作者代行；弹框最小化时仅保留“展开”入口。`resource-payment` 已覆盖符文圆形点击，GM 军团从棋盘空位选取登场位置。
- 有合法反击/手牌响应时才打开响应窗口；无合法动作由服务端自动让过。
- 公开展示统一通过全局最上层、无蒙版、非阻塞动画呈现，卡图与“对手展示卡牌〈卡名〉”保留 3 秒后自动消失；日志携带可查询卡牌快照，不再要求对手确认。
- 触发式效果已接入同一动画层；普通触发批次与试炼完成触发均由服务端发布来源卡快照及对应效果原文，前端不再按卡号或日志概括猜测。
- 三来源审计已把腾讯文档 46 项、群截图 4 项和线上后台 4 项按根因合并为 51 个唯一问题；特殊起始军团规则、普通军团印刷数值完整性、黄金圣甲虫登场链和天灾公开信息双确认均已增加发布门禁，人工验收入口见 `BUG-CROSS-SOURCE-CHECKLIST-20260824.md`。
- GrandUMI 旧测试工程仍因缺少其历史 `卡牌数据` 目录而失败；该工程不是十二军团发布门禁，问题保持显式记录。

## 5. GitHub 与线上状态

- 当前 GitHub 同步基线：以 `main` 最新提交为准；每个功能在测试、远端检查后独立提交。
- GitHub `main` 已包含该提交。
- 线上主页、卡牌页、健康接口均返回 200，公网 WebSocket 探针通过，服务状态为 `active`。
- 后续批次按“完成一项 → 检查远端 main → 整合协作者提交 → 中文备注提交并推送”执行。
- 部署只在用户明确下达部署指令时执行；GitHub 同步不自动触发线上部署。

## 6. 当前风险

1. 历史卡号 `case` 与运行时文本推断已清零，但179条 `CompositeFlow` 仍是过渡层；非历史的身份条件和数据映射要逐项分类，不能把合法身份规则误删，也不能让复合路由长期替代细粒度原子。
2. 原子清单覆盖率高于细粒度实战迁移率，必须持续防止把“已展示”或“已路由”误报为“已完成”。
3. CDN 生产工具链已完成，图片仍以仓库资源和 Steam 来源为主；R2 Bucket、`cards.legion12.grand-umi.com` 绑定与全量迁移尚未在 Cloudflare 控制台执行。
4. 网络已完成原座位认领、权威状态/Prompt 恢复、旧连接隔离和同一对局 revision 防倒退；断线遮罩、超时判负、多端登录策略仍需后续完善。
5. 管理操作已接入统一审计模型；仍需增加账号停用、内容定时发布、审计导出和更细的角色能力矩阵。
6. 发布前必须保持运行数据库、账号、Bug、官网内容与对局记录，任何验证失败都必须原子回滚。

## 7. 下一阶段顺序

1. 细粒度原子化：以179条 `CompositeFlow` 及74张“有权威实战入口但尚未进入两个原子路由文件”的卡为审查基线，按逐卡矩阵继续拆解公共原子；不得以数字下降替代语义正确性，也不得恢复卡号 `case` 或运行时文本推断。
2. 在 Cloudflare 控制台建立 R2 Standard + `cards.legion12.grand-umi.com`，使用已完成的多尺寸/内容哈希/预加载工具做全量迁移；Steam 仅作为迁移来源。
3. 网络基础恢复已迁移；后续增加断线遮罩、超时判负和多端登录策略。
4. 后台权限、审计日志、内容草稿/发布和卡效人工确认已完成基础闭环；后续补账号停用、定时发布和审计导出。
5. 在每批迁移后由第二模型做只读独立验收；最终裁定仍以规则书、FAQ 和自动测试为准。

## 8. 2026-08-24 最新批次

- 十二军团规则/实战测试：316/316 通过。
- 后台持久化测试：7/7 通过。
- 前端 UI 契约：82/82 通过，Vue/TypeScript 与 Vite 生产构建通过。
- 沙盒防御操作区统一使用当前代操作玩家，单人可提交对手的主宰抵挡和军团支援，创建者视角保持固定。
- GM 面板新增场上卡返回所有者手牌与按规则进入下一阶段；〈草薙剑〉进入战场后圣物区为空。
- 原子运行时新增可暂停/恢复的 `Optional` 执行语义；玩家拒绝时终止该能力，确认后从下一原子继续，不重复执行条件或前置步骤。
- 〈源博雅〉、〈宫本武藏〉、〈源义经〉、〈荆轲〉、〈贝奥武夫〉、〈布伦希尔德〉、〈神农鼎〉、〈哈特谢普苏特〉、〈井伊直虎〉、〈埃涅阿斯〉及〈阿塔兰忒·晋升〉的两种登场时机已迁移到同一可选抽牌模板。
- 该批次当时将旧卡号分支收紧到 195 处、161 个卡号，原子运行时已验证程序增至 28 项；最新基线见更新46。

## 9. 2026-08-25 符文支付与反馈复核批次

- 固定数量、同质符文费用改为在模式/目标声明完成后自动支付；只有〈高文〉这类支付数量本身可变且影响效果的场景继续让玩家点击符文圆形。
- `PendingActivation` 增加完整效果选项标签与取消入口；取消不会支付费用、登记次数或进入堆叠。彼界阵营效果、梅林及同类入口不再暴露编程内部值。
- 修复〈亚瑟王〉叠放〈王者之剑〉错误消耗士气，现严格消耗 1 符文，并由回归锁定士气不发生二次变化。
- 规则/实战测试更新为 319/319；前端 UI 契约 82/82，Vue/TypeScript 与 Vite 生产构建通过。
- 本地测试组审计表仍是 2026-08-21 的 46 项旧基线，仅含“审计总览”“反馈审计”；腾讯文档新“Bug修复核对”子页尚未能通过当前浏览链路读取，后续不得用旧表冒充线上复核结果。

## 10. 2026-08-25 资源支付选择收敛

- 普通费用支付改为按“是否存在真实决策”决定交互：所有可用资源都必须支付或只有一种资源类型时自动完成；普通士气、神力、〈黑色莲花〉、陵墓守卫之间存在可保留选择时才要求玩家点选场面资源。
- 打出军团、覆盖反击战术与通常移动的自动分支统一允许使用当前规则合法的陵墓守卫；0士气且仅有1张活跃陵墓守卫时不再卡在支付弹框。
- 完整规则测试319/319、UI契约82/82、Vue/TypeScript与Vite生产构建通过。
- Cloudflare R2 卡图生产工具链已列入上线队列；客户端正式切换前仍需完成R2 Bucket、`cards.legion12.grand-umi.com`自定义域名、对象上传与缓存/回退验收。

## 11. 2026-08-25 条件战斗配置公共化

- 远程、条件职介、挑衅、主宰保护、设定兵力、进攻无损和无法被远程进攻统一读取结构化战斗原子；合法目标预览、正式进攻、派生职介和后台原子清单使用同一份定义。
- 全卡池37张远程卡按常驻、仅前排、仅后排逐张建立回归；〈阿塔兰忒·晋升〉后排真实派生为【弓手】且本次进攻兵力视为3000，〈源义经〉后排本次进攻兵力视为2000。
- 8张前排挑衅和2张前排主宰保护不再依赖卡号分支或“挑衅/挑畔”文本写法；一次性及限时进攻无损进入交战快照并按次消耗。
- 旧卡号分支计数本批不增加；战斗规则由散落运行时特判迁入声明式配置，后续新增同类能力必须通过公共原子注册。

## 12. 2026-08-25 返还士气选择公共化

- 返还士气与普通费用支付分离为独立的 `resource-return` 协议；当活跃/休整、普通/神力或〈黑色莲花〉之间存在真实结果差异时，由玩家直接点击场面士气并确认返还。
- 所有候选都必须返还，或候选返还结果完全等价时自动完成，不增加无意义操作。
- 主动效果和堆叠内返还士气均可暂停并从原效果继续；目标、位置、来源状态在支付前二次校验，避免失效效果先扣资源。
- 〈黑色莲花〉不再拥有单独优先级设置；只有玩家实际选中返还时才进入墓地，其他士气返回各自士气牌库。

## 13. 2026-08-25 带目标阵亡时触发声明迁移

- `TriggerBatch` 在玩家完成同一时点排序后，逐个处理目标、模式、费用与位置声明，再按既定顺序压入堆叠；等待声明期间不会越过当前候选。
- 首批覆盖花木兰、孙武、荆轲、上杉谦信、坂本龙马、图坦卡蒙、尼托克丽丝、无情者哈拉尔与神箭奥德尔；结算只使用候选携带的已声明目标。
- 取消声明会移除当前触发候选且不支付、不入栈；荆轲返还士气在目标声明完成后、入栈前完成。
- 完整规则/实战测试更新为 325/325；旧卡号分支没有增加，后续继续迁移复合阵亡时及其他带目标触发簇。

## 14. 2026-08-25 卡图 CDN 生产准备

- 已在 D 盘建立完整源图归档：S01 远程迁移 133 张、S02 本地复制 115 张，共 248 张、约 835.47 MiB。
- 已完成 `20260825` 内容寻址构建：每张卡生成原始 WebP、240/480/960 WebP 与 960 AVIF，共 1,242 个文件、约 189.89 MiB；清单缺图 0 张，方向统计为竖版 228、横版 20。
- 新增可断点续传的归档脚本，下载包含逐卡进度、连接/总时限、重试与临时文件清理；输入输出均拒绝 C 盘。
- Cloudflare 账户侧尚缺 R2 Bucket、S3 访问密钥、自定义域名绑定与上传客户端；在这些外部配置完成前，不切换线上 `imageUrl`，Steam 仍只作为迁移来源保留。

## 15. 2026-08-25 常驻战斗能力映射复核

- 修正〈吕布〉、〈疯狂的爱丽丝〉、〈墨子〉与〈驱魔道士 陆瑛〉的常驻进攻无损/无法被远程进攻映射；〈阿喀琉斯·晋升〉继续由人工确认结构化定义提供进攻无损。
- 新增全卡池反向契约：从印刷能力行推导常驻规则并逐张对比实战 `CombatProfile`，防止后续卡号集合再次错位。

## 16. 2026-08-25 复合资源费用事务化

- 返还士气与〈傲慢之罪〉附加支付改为同一声明/校验事务：所有选择完成前不改变场面，已声明返还对象不会再次成为支付候选。
- 自动返还同样预留将离场对象；组合费用不足时直接拒绝且不改变士气、神力、返还统计或效果使用次数。

## 17. 2026-08-25 触发声明与攻击目标同源

- 目标预声明失效时会原子清理对应触发候选并继续同一时点的后续批次，不再留下不可操作的等待状态。
- 合法攻击目标高亮与正式进攻统一读取 `TryValidateAttackTarget`；召唤失调例外、天灾、静态保护、距离、远程限制、挑衅和主宰保护只维护一份规则。
- 新增三项精确回归，完整规则/实战测试更新为 331/331。

## 18. 2026-08-25 连锁响应时机同源

- S01/S02 反击战术、佣兵部队与戏法师的傀儡统一沿响应链读取最初的进攻、登场、打出、主动效果或权威事件时机。
- 对手响应后，优先权回到原玩家时会重新计算其合法响应；仍以“当前顶部属于对手”为约束，不允许同一玩家连续自响应。
- S02 反击战术的合法性与结算共同定位原始权威事件，不再因中间存在另一张反击战术而丢失 defense/non-hand-entry/effect-hand-add/effect-ready 上下文。
- 完整规则/实战测试更新为 332/332。

## 19. 2026-08-25 复合阵亡触发预声明迁移

- 在首批单目标阵亡效果基础上，继续迁移〈陵墓构造体〉、〈萨拉丁〉、〈勇士比约恩〉、〈阿尔维达〉、〈血斧艾瑞克〉、〈忒修斯〉和〈亚瑟王〉。
- 卡牌、墓地顺序、登场/位移位置及费用均在进入堆叠前完成；响应方不再面对尚未公开目标的效果。
- 公共声明层新增动态未占用战场位置步骤，批量登场不会重复占用同一格；取消可选目标时整项效果不支付、不入栈。
- 完整规则/实战测试更新为 334/334。

## 20. 2026-08-25 进攻时强攻快照同源

- 将所有卡效赋予【强攻】的直接字段写入收口为公共入口；卡牌状态与当前主宰进攻伤害快照由同一逻辑更新。
- 覆盖奥拉夫二世、韩信、鲍斯、阿尔忒弥斯、赫拉克勒斯·晋升、草薙剑、卡诺匹斯罐 一、美尼斯、金发哈拉尔等同类来源，移除单卡补丁式伤害累加。
- 新增奥拉夫二世精确回归，确保进攻时获得强攻立即作用于本次进攻且不会重复叠加。

## 21. 2026-08-25 限时必中交战快照修复

- 〈无畏的刺杀〉等“仅进攻军团时”的限时【必中】正式进入交战快照；对军团生效时同时禁止抵挡与支援，对主宰不越界生效。
- 永久与限时必中共用同一防御规则入口，不再由具体防御动作按卡号判断。

## 22. 2026-08-25 彼界战术与登场触发同源

- 〈安格斯·麦·奥格〉按战术效果成功结算监听主动战术、S01反击战术与S02反击战术，并按真实回合序列执行“回合1次”。
- 完成〈寻找圣杯之旅〉后的【圆桌骑士】登场触发覆盖正常登场、晋升登场、效果登场和GM触发效果登场。
- 触发批次在已有堆叠或响应窗口时进入等待队列，防止登场时效果与同一时点触发错序。

## 23. 2026-08-25 第二季反击战术公共响应修复

- S02-0015 至 S02-0018 已恢复为可从手牌正常覆盖到后排的反击战术；不再被前端误走主动战术打出路径。
- 第二季反击战术沿响应链读取原始权威事件的控制者和类型；同一玩家可在双方依次获得优先权后继续发动第二张合法反击战术。
- 同一来源效果产生的多个转活跃权威事件带有共同来源身份；〈毒药发作〉会无效该次效果的整批转活跃，不会只拦截第一张。
- 〈粮草掠夺〉选择对方手牌时仅显示卡背和位置占位，不泄露卡名、卡图或效果。
- 完整规则/实战测试更新为 341/341；前端 UI 契约 82/82，Vue/TypeScript 与 Vite 生产构建通过。

## 24. 2026-08-25 特殊衍生卡区域限制与墓地完整候选

- 主牌库外衍生卡建立统一身份识别，〈哮天犬·稚〉和〈符文〉不再被通用回手、回牌库或墓地回收效果带入普通区域。
- 场上回牌库效果在目标预览、主动提交与结算三层使用同一限制；陵墓守卫仍保留“以任何形式离场进入所有者墓地”的替代规则。
- 〈复仇血鹰〉不再预先截断墓地前两张卡，玩家可从全部其他合法【阿斯加德】卡中选择2张。
- 同类扫描识别出英灵殿与瓦尔基里仍有旧式墓地选择分支，已登记到复合主动效果预声明迁移批次，不在本次局部改写中遗漏。
- 完整规则/实战测试更新为343/343。

## 25. 2026-08-25 英灵殿与瓦尔基里复合预声明迁移

- `PendingActivation` 支持从此前声明的卡牌中继续指定去向，以及排除已声明场上对象后继续选择另一个目标。
- 〈英灵殿〉完整声明墓地2张牌和两个不同的合法击杀对象后，才休整并把墓地牌作为费用返回牌库底部；响应结束后仅结算已声明目标。
- 〈瓦尔基里〉完整声明墓地2张牌及各自去向后，才消耗士气并对主宰造成伤害；旧式“读取墓地前2张”分支已经删除。
- 复合预声明精确回归2/2、完整规则/实战测试345/345通过。

## 26. 2026-08-25 圣物区手牌出牌限制同源

- 〈黄金圣甲虫〉与〈安德华拉诺特〉的静态出牌限制统一由规则内核计算，覆盖主圣物和叠放/额外圣物区。
- 手牌快照携带权威禁用原因，前端不再仅按费用猜测是否可打出；同名的另一张〈黄金圣甲虫〉也属于“其他圣物”。
- 同类回归覆盖两种限制来源、同名/不同名圣物、快照与服务端提交双层门禁。
- 完整规则/实战测试347/347、前端 UI 契约82/82、Vue/TypeScript 与 Vite 生产构建全部通过。

## 27. 2026-08-25 雷神之锤墓地主动效果

- 墓地卡牌开始使用动态能力快照和统一能力按钮；不可发动时显示灰置原因，不再为单卡另造墓地交互。
- 〈雷神之锤〉在支付前依次声明3张其他墓地卡牌及登场位置，费用按玩家所选顺序返回牌库底部，随后通过公共非手牌登场路径活跃登场。
- 〈雷神索尔〉开局选择将〈雷神之锤〉加入起始手牌时，使用统一展示事件向对手公开卡图和可查询卡名。
- 完整规则/实战测试348/348、前端UI契约82/82、Vue/TypeScript与Vite生产构建通过。

## 28. 2026-08-25 普罗米修斯真实主宰入口回归

- 〈普罗米修斯〉主宰快照会提供查看牌库顶部3张的效果按钮，真实主宰实例可进入统一主动效果链。
- “消耗1神力”只将活跃神力转为休整神力，不改变其神力面；“消耗并翻转神力”继续使用独立资源原子。
- 新增真实主宰端到端回归，防止仅用战场同卡号替身测试造成入口漏测。

## 29. 2026-08-25 效果抽牌日志同源

- 堆叠结算中的效果抽牌由公共 `Draw` 入口统一写入对局记录，覆盖旧分支和原子结算。
- 公开日志只显示效果来源、玩家和数量，不泄露抽到的私密卡名；已有定制文案的效果避免重复记录。
- 〈西施〉精确回归锁定“选择 0 张军团仍付费并抽牌”以及不公开所抽卡名的契约。

## 30. 2026-08-25 测试组登场回合进攻权限复核

- 〈彭忒西勒亚〉、〈珀洛特埃〉的普通登场效果，以及〈阿喀琉斯·晋升〉的晋升登场效果，均已直接验证服务端快照中的合法军团进攻目标，对局界面会据此显示进攻按钮。
- 〈草薙剑〉从圣物区被须佐之男置入前排时保留原本作为圣物的登场回合：同回合不会获得进攻权，后续回合正常进攻。
- 上述三项进攻权限与草薙剑时点均使用公共 `LegalAttackTargets` 契约，不增加前端单卡按钮特判。

## 31. 2026-08-25 万物统御之戒可选流程

- 〈万物统御之戒〉登场时效果在弃置手牌之前先询问是否发动，“不发动”不会改变手牌或留下未结算选择。
- 选择发动后仍按“弃置1张手牌 → 选择1张【通用】卡牌展示并加入手牌 → 洗牌”的既有流程结算。

## 32. 2026-08-25 阵亡/离场共用时点去重

- 〈陵墓构造体〉“阵亡时/离场时”的同一项能力不再同时进入两个候选集合；阵亡作为离场只生成一次能力，不会重复发动。
- 其他来源于不同卡牌的合法“我方军团阵亡/离场”触发继续分别进入同一触发批次，不因本次去重而丢失。

## 33. 2026-08-25 效果选项展示文本统一

- 所有效果模式按钮优先使用服务端随当前 Prompt 下发的完整效果原文，通用“是/否”与协议键值仅作无专用文案时的兜底。
- 前排/后排、普通/晋升登场、符文/试炼、取消等公共选择补齐中文协议标签，〈杜阿特之门〉与同类型效果不再分别维护前端单卡映射。
- 不可发动及仅在触发时点发动的能力继续保留完整文本供查看，但按钮灰置且不可点击；由后端能力视图与前端公共按钮样式共同约束。

## 34. 2026-08-25 天灾公开展示与触发流程分离

- 只有持续效果、没有“触发”能力的天灾会向双方播放三秒公开卡图动画；动画位于全局最上层、无蒙版且不阻塞场面。
- 含触发能力的天灾翻开后直接执行天灾效果，不再增加双方确认步骤。
- 天灾仍不能被响应或无效化，天灾导致的阵亡/离场仍不触发相关卡牌效果。

## 35. 2026-08-25 公共掷骰事件动画

- 对局中的普通 `dice` 事件开始统一播放全局最上层、无蒙版且非阻塞的骰面动画，权威结果仍由服务端决定并保留在对局记录。
- 覆盖〈雷霆天怒〉、〈黯陨晨星〉和〈魔龙降世〉等现有掷骰入口；先后攻掷骰继续使用包含双方主宰头像的准备阶段动画。
- 前端 UI 契约更新为86项，Vue/TypeScript 与 Vite 生产构建通过。

## 36. 2026-08-25 伊姆何泰普登场回收回归

- 通过真实手牌打出、响应让过、登场触发、墓地选择与回收链验证〈伊姆何泰普〉会展示全部合法的高费用【太阳城】军团，并同时保留“不发动”。
- 当前规则分支无需改写；新增回归用于识别旧部署或入口回滚，完整规则/实战测试更新为354/354。

## 37. 2026-08-25 下载版腾讯文档复合回顾

- 已直接读取《L12web-Bug与建议登记-0825.xlsx》的“测试组Bug反馈”61行和“Bug修复核对”74行，不再使用旧的 0821 双工作表副本推断线上状态。
- 将本轮 BUG-20260825-90 至 110 共21项公共修复逐项写入跨来源人工核对清单，并映射腾讯文档、群截图和后台重复来源。
- BUG-20260825-112 已修正〈落穴陷阱〉仅凭共享 `enter` 时点而错误响应圣物登场的问题；响应候选现在同时校验原始来源为军团，并保留军团登场嵌套响应回归。
- 复合核对确认：雷霆天怒骰子动画、效果选项文本、天灾展示、雷神之锤墓地主动、万物统御之戒可选发动、伊姆何泰普真实回收等下载表新增备注均已有独立修复或回归证据。
- 仍保留响应信息隐藏与无意义窗口自动跳过之间的产品取舍、Cloudflare R2账户侧配置，以及未按原牌库人工复测的条目；这些项目不会被错误标记为“已通过”。
- 线上管理后台已有16条与本轮公共修复直接对应的反馈更新为“已解决”并附审计备注；其余5条设计建议或缺少精确证据的反馈保持“新反馈”。

## 38. 2026-08-25 登场回合反击保护结构化

- 〈图特摩斯三世〉与〈拉美西斯二世〉的“登场回合不受反击战术效果影响”由结构化规则统一提供，响应窗口不再匹配中文效果文本。
- 全卡池扫描确认当前只有这两张军团拥有该规则；天灾不可响应仍由天灾公共入口处理。
- 两张卡均增加真实手牌付费登场、对方已盖伏〈伏击〉的参数化回归，防止卡面数据格式变化或单卡分支调整导致保护失效。

## 39. 2026-08-25 下载表四项真实流程补证

- 〈符文之力〉已通过完整实战链验证：正常打出获得符文、选择额外支付士气、查看牌库顶部3张、展示并加入合法【彼界】卡牌，其余卡牌按玩家顺序放回牌库底部。
- 〈罗宾汉〉选择的〈侍从骑士〉现在明确标注来自手牌、牌库或墓地；选择后复用公共排队登场流程，由玩家直接在战场选择位置并活跃登场。
- 〈坂本龙马〉选择两张休整军团时，可把另一张占据的阵地作为目标并以一次原子交换完成互换，不产生非法中间空位。
- 登场效果响应链已验证：对方〈伏击〉响应后，原玩家重新获得优先权并可用〈绝对防御〉继续响应；与进攻响应链使用同一公共时机追溯。
- 新增真实流程回归4项并全部通过；对应下载表 T31、T32、T43、T45 已写入跨来源人工核对清单，但在测试组以原牌库复测前不标记为人工通过。

## 40. 2026-08-25 下载表未编号反馈稳定化与猎杀时刻费用前置

- 为下载表末尾13条未编号反馈建立U01至U13稳定编号，并与既有公共根因、自动回归和人工复测状态合并，后续不再按空编号重复开发。
- 全卡池扫描确认〈猎杀时刻〉是当前唯一把固定数量墓地卡牌作为手牌战术发动费用的卡；旧实现在结算阶段才检查数量，会在墓地不足4张时支付士气后空结算。
- 手牌快照与权威出牌入口现在共同前置校验该费用：不足时灰置且不能提交，不支付士气、不移出手牌、不建立堆叠；满足后继续进入4张卡牌自选顺序与击杀流程。

## 41. 2026-08-25 〈黯陨晨星〉主动战术免费范围

- 〈黯陨晨星〉单数结果的免费机会现只作用于主动战术；反击战术继续显示并支付印刷费用，不会错误消费天灾给予的机会。
- 主动战术成功打出后会消费 `ds01-free-tactic`，费用预览、权威支付和反击覆盖读取同一类别约束。
- 全卡池扫描保留卡诺匹斯罐、孙武、奥德修斯与阿瓦隆等原文未限定“主动”的 `FreeTacticCount` 行为，避免把单张天灾的类别限制错误扩散到其他卡。

## 42. 2026-08-25 未印刷费用与数值0分层

- 卡牌实例新增 `HasPrintedCost`，由卡牌定义的可空费用字段生成；支付计算仍可把未印刷费用安全视作0，但卡面不再据此虚构0费角标。
- 〈哮天犬·稚〉以及全卡池其他 `cost: null` 的额外牌、试炼、符文和特殊卡统一复用该字段；普通印刷0费卡仍正常显示0。

## 43. 2026-08-25 限1构筑上限数据化

- 卡牌定义新增 `DeckLimit`，前端目录、编辑器加牌、前端保存校验与服务端权威校验统一读取卡牌数据，不再各自假定所有卡最多3张。
- 当前按测试组反馈登记卡诺匹斯箱、卡诺匹斯罐一至四、〈万物统御之戒〉与〈雷神之锤〉共7张限1卡；全卡池集合测试会阻止漏登、误登及编辑器/服务端规则分叉。
- 普通卡继续使用默认上限3；未来新增其他构筑上限时只需更新卡牌数据，不增加卡号特判。

## 44. 2026-08-25 第一季天灾等级补充

- 登记〈无情者哈拉尔〉2，以及〈勇士比约恩〉、〈奥拉夫二世〉、〈阿尔维达〉、〈血斧艾瑞克〉、〈土方岁三〉各1点天灾等级。
- 服务端权威卡表和前端卡牌目录同步更新；第一季全部27张已知天灾等级军团由集合回归锁定，避免只验证本次六张而遗漏旧数据。

## 45. 2026-08-26 攻城投石车扩展范围真实流程补证

- 〈攻城投石车〉在后排发动能力、支付2张活跃士气并完成响应结算后，权威合法目标集合会同时包含对方后排军团与主宰，不会要求玩家预先二选一。
- 新回归从实际发动入口贯穿支付、入栈与结算，不直接改写临时范围字段；〈伪造密令〉继续共用同一扩展范围字段与进攻目标生成器。

## 46. 2026-08-26 线上基线复核、彼界登场获得符文原子化与 R2 本地验收

- 已将提交 `612768c` 通过独立发布检出与原子切换脚本部署到服务器，避免覆盖 `D:\GPT\L12-web` 中尚未提交的协作者改动；线上主页、卡牌页、健康接口均返回200，公网 WebSocket 探针成功。
- U02〈切腹仪式〉卡面为强制式“对方进攻后”，当前继续按必须发动实现；若规则意图为可选，需等待正式裁定后再修改。
- T02 当前继续遵循“只有存在合法响应时才开启窗口”的既定交互，因此可能被对手据窗口是否出现推断响应资源；在未决定固定伪窗口或强制双方让过前，不以单卡方式掩盖该产品取舍。
- 新增 `operation.gain-rune` 原子及实战运行入口，迁移〈梅林〉、〈帕西瓦尔〉、〈高文〉、〈伊丽莎白·都铎〉四张无目标、固定获得1符文的登场时能力，并删除四个旧卡号分支。
- 同类型扫描确认〈阿麦金〉为可选获得符文，其他获得/消耗符文能力还包含费用、目标或后续结算，不与本批固定强制获得符文错误合并。
- 原子运行时已验证能力增至32项；旧卡号分支降至192处、157个卡号。专项测试9/9、完整十二军团规则/实战测试381/381、平台发布门禁8/8、前端UI契约88/88及生产构建全部通过。
- R2 本地资产归档已核验：248张原图共876,054,538字节；CDN产物1,242个、199,115,076字节，其中248张卡共1,240个不可变变体，竖版228张、横版20张，清单缺失0、文件缺失0。
- 本地生产清单已指向 `https://cards.legion12.grand-umi.com`，并包含WebP/AVIF、多尺寸、内容哈希与预加载清单。
- Cloudflare账户侧尚未上线：本机未配置 `CLOUDFLARE_ACCOUNT_ID`、`R2_BUCKET`、AWS访问密钥，也未安装AWS CLI；完成Bucket、API Token、自定义域名绑定后方可执行上传与正式切流，当前Steam来源仍保留为迁移回退。

## 47. 2026-08-26 彼界可选符文与冲锋登场原子化

- 〈阿麦金〉的“登场时 可获得1符文”迁入统一 `Optional → GainRune` 程序；选择不发动会直接关闭能力，选择发动才从公共 continuation 继续获得符文。
- 〈斯卡哈〉的“登场时 获得冲锋”迁入公共 `Keyword(charge)` 程序，与既有冲锋卡共享同一实战解释器。
- 全卡池符文扫描确认：〈寻找圣杯之旅〉、莫瑞甘、安格斯·麦·奥格、彼界阵营效果等仍分别包含回合一次、特定触发、试炼完成或费用，不与阿麦金的单纯可选登场能力机械合并。
- 删除两张卡的登场旧分支及阿麦金专用 continuation；验证原子由32项增至34项，旧卡号分支由192处/157卡降至190处/155卡，并将审计棘轮同步收紧为190。
- 专项回归12/12、完整十二军团规则/实战测试385/385、平台持久化发布门禁8/8、前端UI契约88/88及生产构建通过。

## 48. 2026-08-26 弓手职介规则与主宰交互原子化

- 将【弓手】建立为权威职介能力来源：任何印刷或派生职介“视为【弓手】”都会统一获得“进攻距离+1，远程进攻无损”，不再要求赋予职介的单卡重复登记战斗参数或依赖卡号特判。
- 〈雷神之锤〉登场回合可进攻对方主宰迁入 `SetState(source.canAttackMasterOnSummonUntilTurn)` 公共原子；该权限不等同于冲锋，也不会错误允许其进攻军团。
- 〈步行者罗洛〉登场恢复1点主宰血量与〈金发哈拉尔〉阵亡恢复1点主宰血量迁入 `HealMaster` 公共原子，并继续经过军团效果恢复限制的统一入口。
- 删除对应3处旧分支；验证原子由34项增至37项，旧卡号分支由190处/155卡降至187处/154卡，审计棘轮同步收紧为187。
- 专项回归7/7、完整十二军团规则/实战测试390/390、平台持久化发布门禁8/8、前端UI契约88/88及生产构建全部通过。

## 49. 2026-08-26 规则书关键词基线与条件触发原子化

- 新增 `docs/l12/RULEBOOK-KEYWORDS.md`，以规则书2.0结构化文本为权威来源，整理规则效果、持续效果、主动式效果、触发式效果、反应式效果五类执行模型，以及【强攻】【免死】【必中】【挑衅】【冲锋】【隐匿】【震击】【贯穿】八个正式词条。
- 将词条、职介能力和核心规则动作分层：已明确【弓手】会获得“进攻距离+1，远程进攻无损”；远程文本本身不反推弓手，弃置不等于阵亡，位移1格不等于1次位移。
- 新增 `AdvanceTrial` 公共原子并迁移〈侍从骑士〉阵亡推进试炼；〈圣女贞德〉阵亡使双方主宰恢复1点、〈金发哈拉尔〉低血进攻获得强攻、〈传奇的拉格纳〉低血登场获得冲锋、〈无情者哈拉尔〉血量条件下可选伤害均迁入验证原子程序。
- `HasImmediateEffect` 先查询已验证原子程序，避免删除旧卡号清单后新程序没有进入堆叠；上述五项旧卡号分支已删除。
- 继续依关键词基线迁移宫本武藏“我方前排没有其他军团时获得冲锋”与赫拉克勒斯·晋升“进攻时本回合获得强攻”，两者均进入公共 `Keyword` 运行时。
- 验证原子由37项增至44项；旧卡号 `case` 分支由187处/154卡降至181处/151卡，审计棘轮同步收紧为181。赫拉克勒斯·晋升原实现为 `if` 而非 `case`，已删除但不计入该棘轮数值。
- 本批最终验证：完整十二军团规则/实战405/405，平台持久化发布门禁8/8，前端UI契约88/88及生产构建通过。

## 50. 2026-08-26 历史卡号分支全部清零

- 将剩余 181 个 `case "Sxx-..."` 迁入 178 条结构化运行路由；3 条已完成试炼映射改按试炼名称处理，历史旧卡号分支由 181 处/151 卡降至 0 处/0 卡。
- 新增 `operation.composite-flow` 过渡原子，使原子后台、实战入口和语义流程处理器共用同一份路由定义；原处理器改按稳定流程名分派，不再按卡号分派。
- 原子审计门禁升级为 `-RequireZero`：完整扫描服务端卡效源文件并锁定卡号 `case` 必须为0；同时另报卡号身份条件、数据映射和文本推断，避免把合法数据引用混充为旧分支，也避免通过缩小扫描范围伪造清零。
- 增加迁移脚本、零分支清单、路由身份与无 legacy 回退测试；178 条路由必须具有唯一可执行复合原子，且运行时按卡号+时机解析到同一对象。
- `CompositeFlow` 只是复合效果继续细拆的过渡边界；后续原子化工作不再受逐卡 `switch` 阻碍，但仍需按照规则书、FAQ和逐能力回归把复合流程拆成细粒度公共原子。
- 最终门禁：规则/实战406/406、原子专项46/46、平台持久化8/8、UI契约88/88及前端生产构建全部通过。

## 51. 2026-08-26 P1 公共棋盘常驻布局安全轨道

- 当前棋盘统一改为“对方战场 / 中央阶段安全轨道 / 我方战场”三段网格，中央阶段栏与天灾不再覆盖双方战场的军团和士气等常驻信息。
- 士气条脱离战场纵向文档流并固定在主宰侧通道，避免资源条挤压前后排；正式对战、沙盒、观战与回放因共用 `GameBoard.vue` 同时生效。
- 新增两项防回滚契约，UI契约由88项增至90项；前端生产构建通过。本地后端未连接，真实满场对局的像素边界复核留待下一次明确部署后执行。

## 52. 2026-08-26 战场生效关键词与主宰军团化兵力统一

- 场面快照新增权威 `ActiveKeywords`，按当前位置和当前持续状态汇总【强攻】【免死】【必中】【挑衅】【冲锋】【震击】【贯穿】；隐匿覆盖继续遵守隐藏信息，不向对手泄露。
- 公共卡牌组件删除底部单字状态角标，改为从费用下方向下逐项显示完整关键词文字框；多个关键词不换行、不占用兵力和叠放卡图区域。
- 前端接入 `IsMasterLegion`，孙悟空作为军团时显示当前兵力；新增 `DisplayBaseTroops` 将设定兵力与增减兵力分层，避免孙悟空的士气设定兵力错误显示为增益。
- 同类型审查覆盖草薙剑军团化、晋升叠放、隐匿和覆盖战术；规则军团身份继续由后端 `IsFieldLegion` 掌握，UI不新增孙悟空卡号分支。
- 新增后端快照回归与两项前端防回滚契约；专项后端回归、UI契约92/92和前端生产构建通过。

## 53. 2026-08-26 衍生卡离场规则与无印刷费用统一

- 按规则书通则建立衍生卡公共离场替代：衍生卡离开战场时直接消灭，不进入墓地、手牌、牌库或移出游戏区；阵亡时/离场时效果仍使用最后已知信息正常建立。
- 〈哮天犬·稚〉不再在效果选择弹框或历史回放中显示不存在的0费；服务端弹框协议、实时场面和回放转换统一携带或恢复 `HasPrintedCost`。
- 全卡池扫描确认当前主牌库外衍生卡为〈哮天犬·稚〉与〈符文〉，试炼、天灾、晋升基底及普通叠放卡不纳入该身份。
- 新增回手、击杀及随叠放主体离场回归，锁定衍生卡不会落入任何普通区域；圣物区与GM旁路也已接入同一替代规则，并保留费用显示和离场规则的前后端防回滚契约。

## 54. 2026-08-27 隐藏手牌与墓地主动交互等六项收口

- 〈粮草掠夺〉、〈破败仪式〉改用服务端随机匿名手牌槽位；未写“查看”的效果不再泄露卡名、卡图、实例号或对手抽牌顺序，公开日志也只记录盲选动作。
- 〈雷神之锤〉从墓地点击卡牌本身进入“是否发动”；确认后才选择三张其他墓地卡，支付后自动关闭墓地并回到棋盘选择登场位置，拒绝发动不再要求选费用。
- 自动阶段与GM下一阶段均锁定先攻玩家首回合不抽牌；【免死】只替代阵亡，保留致命伤瞬间状态，持续修正会重新计算，战斗只有目标实际离场才成立击杀。
- Prompt、主宰、阵营和单卡效果最小化后仅保留展开入口，统一避开右下 Bug 反馈；沙盒按当前代操作玩家开放双方主宰效果，同时继续复用正式规则校验。
- 本批增加匿名隐藏信息、墓地主动四段流程、先攻抽牌、免死多路径、沙盒对方主宰和UI防回滚覆盖；发布级规则测试两轮434/434、平台持久化两轮8/8、UI契约98/98、零旧分支审计、生产与隔离构建及发布包生成全部通过。

## 55. 2026-08-28 展示动画与公开牌库闭环

- 公开展示和触发动画改为只向非发动方播放；天灾仍向双方播放。动画只呈现本次触发/展示的精确文本与实际公开卡牌，不再展示来源卡或完整卡牌效果；〈李牧〉已按“单张展示卡＋本次登场展示说明”锁定回归。
- 非构筑计数牌按卡牌规则标记统一识别，牌库编辑器、房间、牌库页和牌库图统一以 `40(3)` 显示计入构筑与额外数量，不再维护〈陵墓守卫〉单卡号特判。
- 公开牌库由账号服务端持久化，支持公开、更新、查看、点赞、复制和作者编辑/删除；服务端复核牌库合法性及资源所有权，旧账号数据可向后兼容加载。
- 防误覆盖：新建、另存为或切换其他本地牌库会清除当前公开版本身份；只有从自己的公开牌库进入编辑器时才保留该身份并执行更新。
- 发布门禁：L12规则434/434、平台持久化9/9、原子旧卡号分支0、UI契约101/101、生产构建和空白字符检查通过。

## 56. 2026-08-28 GrandUMI 后台对照与 L12 控制平面设计

- 完成 L12 现有后台、GM 沙盒、赛事、账号权限、内容发布、卡效审计、Bug 管理、对局数据和发布脚本的静态盘点，并以 GrandUMI 管理能力为参照形成逐项差距矩阵。
- 明确现状：Bug 闭环、内容草稿/发布、账号管理和卡效人工确认已有真实服务端能力；赛事仍是浏览器本地原型，运维页仍是说明占位，管理操作与审计记录尚未统一进入一个命令与权限边界。
- 形成控制平面方案：以 `AdminCommandBus`、细粒度 RBAC、幂等键、版本前置条件、追加式审计/事件发件箱为核心，覆盖 Bug、内容、赛事和发布四条工作流，并与实战数据平面隔离。
- 给出五阶段迁移顺序：先补权限矩阵、会话撤销、审计与关联 ID，再将现有管理动作命令化，随后拆分 `platform.json`，最后建设服务端赛事与受控发布入口。
- 本批仅新增设计与审计文档，不修改运行时代码、外部系统或线上数据；未提交、推送或部署。

## 57. 2026-08-28 后台控制底座第一阶段

- 后台认证从散落的角色字符串判断迁移到集中权限矩阵，当前覆盖玩家本人会话、账号、后台会话、Bug、内容、卡效审查和审计读取；账号接口同步返回实际权限，前端按权限而非角色名显示管理入口。
- 新增账号本人会话列表、当前/指定/全部会话撤销，以及管理员按账号查询和撤销会话；撤销立即使令牌失效并主动中断对应 WebSocket。修改密码保留当前会话并撤销其他会话。
- 所有 HTTP 请求接收或生成 `X-Correlation-ID`；后台写入和认证/授权拒绝记录关联 ID、权限、结果、理由、请求方法与路径。管理页面可直接检索安全拒绝与关联 ID。
- 新增最小 `AdminCommandBus`：角色变更和管理员会话撤销已具备命令 ID、幂等键、平台版本前置条件、干运行和理由字段；同键同命令可安全重放，同键异命令和陈旧版本会明确拒绝。
- `platform.json` 采用向后兼容补全：旧账号、旧会话和旧审计记录缺失新字段时会自动归一化，不使既有令牌失效；本阶段没有迁移或删除线上数据。
- 验证：完整规则测试434/434、平台权限与持久化16/16、UI契约106/106、Vue/TypeScript与生产构建、零旧卡号分支审计、Codex严格配置检查和 `git diff --check` 全部通过。统一 Batch 包装器在 Windows PowerShell 受限语言模式中无法点载路由脚本，已用 PowerShell 7 独立验证该门禁并逐项执行其余门禁。
- 当前限制：幂等缓存仍为单进程内存；审计仍内嵌于 `platform.json` 且有5000条上限；版本前置条件使用整个平台文件版本；现有其他后台写接口尚未全部迁入命令总线。未提交、未推送、未部署。

## 58. 2026-08-28 后台控制平面第二阶段

- Bug 处置、官网内容草稿、卡效人工审查与高风险角色变更均进入 `AdminCommandBus`；旧 `/api/admin/*` 写路由保留兼容，但不再直接绕过命令边界，新增 `/api/admin/v1/*` 等价入口。
- 命令信封、幂等签名与最终结果、失败原因、审批状态和审批人均持久化到向后兼容的 `platform.json`。同一操作者与幂等键在进程重启后仍重放同一结果；同键异载荷继续返回冲突。
- 角色变更、正式内容批量发布和回滚为高风险命令：首次请求只建立 `requested` 命令，不修改业务数据；申请人不能自审，批准时重新校验申请人的原始权限，并使用持久化的原始 payload 与 `expectedVersion` 执行。
- 平台文件新增独立业务版本与存储修订号，记录命令、审批或拒绝审计不会使待审批命令自己的 `If-Match` 失效；管理事务以快照回退和一次文件替换完成业务变更与命令结果持久化。
- 官网内容使用显式白名单、条目版本、发布版本、批次与回滚指针；前端可先预览/dry-run，再一次提交全部键。批次在完整预校验后单次保存，任一键冲突不会留下部分发布；回滚只允许目标批次仍是各键当前已发布版本时执行。
- 后台新增命令列表/详情、失败原因、待审批批准/拒绝、审计结果/操作者/命令/关联ID筛选，以及内容发布预览、批次历史和回滚入口。发布按钮不再循环调用逐键发布接口。
- 兼容与验证：旧 `platform.json` 缺少业务版本、命令、审批、内容版本与批次字段时可直接加载并在下次写入补齐；完整规则434/434、平台控制面22/22、第二阶段专项6/6、UI契约110/110、Release后端构建和Vue/TypeScript/Vite生产构建通过。GrandUMI 全套新增后为175/185，10项失败仍是既有 NewAtomicOps/EB/OP16 卡效路径，与本批控制面无文件交集。
- 当前边界：仍使用单进程锁与单体 `platform.json`，业务版本仍是平台级而不是逐资源版本，审计仍有5000条上限且未独立归档；新安装环境只有一个根管理员时需要通过受控离线引导建立第二审批人。赛事、生产发布、外部系统、实战卡效与数据库迁移未进入本阶段；未提交、未推送、未部署。

## 59. 2026-08-28 后台控制平面第三阶段

- 平台权威存储迁入 `platform.db`：SQLite 事务保存整个平台快照、存储修订、业务版本和 SHA-256；命令、审批、领域变更与同批审计在一个事务内提交，提交前异常恢复最后持久快照。
- 旧 `platform.json` 首次导入前原样备份为 `.pre-sqlite.bak`，之后继续生成原子兼容镜像。数据库完整性或快照校验失败时只读加载已验证镜像，写入失败关闭，避免损坏数据库被误初始化为空平台。
- 管理审计独立追加到 `admin_audit_events`，不再受 JSON 兼容窗口5000条限制；查询核对事件校验和。恢复演练通过 SQLite 在线备份生成隔离副本，校验 quick-check、快照、版本和审计数量，不覆盖 live 数据。
- 聚焦验证：Phase3专项5/5；第一至第三阶段平台27/27；Release后端构建通过。无网络安装、无提交、推送、部署或外部系统变更。
- 仍有边界：业务数据目前是 SQLite 中的事务快照，不是逐表规范化模型；平台级版本和单进程内存聚合尚不支持多实例写。独立审计仍与业务同库，归档、告警、恢复替换授权和不可篡改保留留到安全治理阶段。

## 60. 2026-08-28 后台控制平面第四阶段

- 赛事中心从浏览器本地原型迁入服务端权威聚合：赛事、主办者/裁判账号 ID、规则与牌库可见性快照、报名、签到、轮次、配对、房间码、暂停/恢复、补时、结构化裁决、归档与正式对局引用均随平台事务快照持久化。
- 赛事写操作全部经 `AdminCommandBus`，使用幂等键与目标赛事 `expectedVersion`；无关平台写不再令配对或赛果命令冲突。工作人员变更、开赛、建轮/开轮、赛果/处罚、对局引用与结束归档为不可自审高风险命令，审批人还必须属于目标赛事作用域。
- 牌库快照由服务端按查看者裁剪：本人和工作人员可见，公开策略分别支持全程公开、赛后公开或始终私密；赛事开始后锁定牌库。正式对局只在已经结束且双方账号名与桌次一致时保存引用，不允许赛事后台接管实战。
- 旧 `localStorage` 赛事不再参与页面权威状态。页面只检测旧副本，用户必须先 dry-run 预览再确认同一摘要；服务端只接受已存在账号映射，旧昵称不能获得裁判/主办权限，成功导入后才清理本机副本。
- 验证：Phase4专项6/6、第一至第四阶段平台聚焦33/33、UI契约113/113、Release后端构建、Vue/TypeScript/Vite生产构建和空白字符检查通过。无网络安装、提交、推送、部署或外部系统变更。
- 当前边界：配对是最小轮转而非完整瑞士积分/破同分；尚无申诉、排行榜结算、赛事媒体、规范化逐表存储或多实例并发。下一阶段只建设受控发布编排与只读运行态，本阶段没有执行生产发布。

## 61. 2026-08-28 后台控制平面第五阶段

- 新增受控发布领域边界：后台不接收工件路径、脚本、URL、凭据或客户端自报验证状态，只能选择受信 `IL12ReleaseControlAdapter` 提供并带 commit、SHA-256、验证门禁与环境白名单的工件 ID；默认适配器禁用且不提供任何工件。
- 发布/回滚写入全部经过 `AdminCommandBus`，以 `staging/production` 环境级版本校验并发。dry-run 只生成计划、不调用执行适配器；真实请求首次只进入待审批，禁止申请人自审，批准时复用持久化原始载荷与版本并再次复核工件 hash。
- SQLite 事务快照持久化环境状态、发布运行、激活、健康检查、WebSocket 冒烟、失败和回滚记录。相同幂等键跨重启返回持久化最终结果且不会重复执行；同键异载荷、审批时工件变化与环境版本冲突均被拒绝并进入统一审计。
- 后台新增“发布控制”页签，展示已验证工件、显式适配器只读运行态、健康/WS结果和运行历史；支持发布/回滚 dry-run 与双人审批提交。运行态与结果码均经过白名单清洗，不返回适配器内部异常或敏感细节。
- 验证：Phase5专项7/7、Phase1–5平台聚焦40/40、UI契约116/116、Debug/Release后端构建及Vue/TypeScript/Vite生产构建通过。未联网安装、未提交、未推送、未部署或调用外部系统。
- 当前边界：没有连接真实工件库、生产发布适配器或密钥系统，故该阶段建立的是可安全迁移的编排/持久化/审批边界而不是线上部署能力。生产适配器仍需 durable outbox/状态机、多实例租约、超时/中断恢复与外部幂等保证；当前执行调用仍同步处于单进程事务锁内。

## 62. 2026-08-28 后台控制平面第六阶段

- 账号支持持久 `Disabled` 状态。禁用/启用使用带幂等键、平台版本和理由的高风险命令，首次请求不执行且申请人不能自审；批准禁用会原子保存状态、撤销全部活动会话并立即中断对应 WebSocket。根 Admin 与操作者自身受保护，重新启用不会恢复旧令牌。
- 登录失败按“规范化账号 hash + 服务端连接地址 hash”双桶持久限流，窗口为10分钟内5次失败、锁定15分钟；返回401/429和 `Retry-After`，并区分成功、失败、达到锁定与锁定期拒绝审计。状态写入事务快照，重启、用户名大小写和单侧换键不能绕过。
- 增加受控离线第二审批人引导：只有服务器 CLI、显式环境开关和至少32字符一次性环境凭据同时具备，且系统尚无第二审批人时才能把目标账号调整为 `release-manager`；无 Web 路由，不回显凭据，同命令跨重启只重放结果。
- 高风险和离线引导命令在独立审计不可用时失败关闭；审批执行前再次检查，避免审计在等待期间失效后仍写业务。安全快照集中显示审批人、禁用账号、登录锁定、审批积压、发布失败、审计/归档与部署前置告警。
- 审计归档采用服务端派生目录、逐事件校验和、整段 SHA-256、SQLite 元数据和非破坏源事件保留；支持 dry-run、双人审批、跨重启列表与逐段恢复演练，文件或事件被篡改时演练失败。客户端不能提供路径或删除源事件。
- MFA 未伪报完成：仅提供可注入凭据保护器的迁移边界，默认 `unavailable` 且注册关闭；没有持久化 TOTP secret 或恢复码。后台与个人页明确显示未启用及完整启用前置条件。
- 验证：Phase6专项7/7、Phase1–6平台聚焦47/47、UI契约120/120、Debug/Release后端测试项目构建及Vue/TypeScript/Vite生产构建通过。服务端全量200/210，10项失败仍为本批未改动的 `NewAtomicOps`、`EB_M6`、`OP16` 与 `OP16_045` 对局效果既有基线。专项包含真实 WebSocket 失效、429/`Retry-After`、双桶/重启绕过、bootstrap负向与幂等重放、审计首次提交/审批双重失败关闭、归档dry-run/审批/恢复/篡改及旧 JSON 加载。
- 当前边界：MFA 仍未实现；审计归档是主机校验文件，不是外部 WORM 或签名链，告警未连接通知渠道；登录限流不是分布式网关，审计与业务仍共用 SQLite 文件，平台仍为单进程事务聚合。未提交、未推送、未部署或调用外部系统。

## 63. 2026-08-29 后台产品模型与运营配置收口

- 本节取代第62节中关于长期 `release-manager`、赛事长期工作人员角色及账号/赛事双人审批的设计；内容正式发布、生产发布与审计归档的高风险复核边界不变。
- 长期身份从通用企业角色收敛为玩家/管理员；旧 support、editor、referee、organizer、release-manager 在加载时归一为玩家。账号身份和禁用/启用由管理员直接执行并审计，不再要求另一人审批；根 Admin、操作者自身和最后一名有效管理员继续受保护。
- 主办者不再是账号角色：玩家创建赛事即获得该场主办者作用域；主办者只能从已接受好友中指定裁判，裁判权限只在该赛事内有效并随赛事结束失效。开赛、轮次、暂停、补时、判罚、结果与归档按赛事作用域直接执行，不再进入后台待审批。
- 新增真实运营配置工作台，覆盖赛季、赛季天灾池、禁限卡、默认预组、对战模式、功能开关和维护公告，支持服务端校验、dry-run预览、版本前置、应用、历史与回滚；所有变更记录操作者、理由和审计。
- 新增只读运行状态：服务版本、在线账号/WS、房间、进行中对局、卡牌数量与CDN探针均来自后端权威来源；无法权威判断时明确显示不可用。
- 后台导航按日常运营、游戏内容、系统治理分组，账号、Bug、官网内容、卡效、运营配置、发布、命令、审计与安全各有唯一入口，赛事工作人员只在赛事中心管理。
- 验证：完整控制面54/54、十二军团规则与实战491/491、前端UI契约125/125、卡图校验19/19（覆盖248张）、原子审计248/248且旧卡号分支为0，以及Vue/TypeScript/Vite生产构建通过；`git diff --check`通过。发布门禁已能在 Windows PowerShell 下正确识别中文服务端路径并实际执行后端测试；NU1900仅为NuGet漏洞元数据网络提示。
- 边界：内容正式发布、生产发布与审计归档仍保留复核；运营配置尚未全量热接入既有实战和构筑消费者，因此当前完成的是权威配置管理与版本闭环，不是所有配置项的即时运行时切换。2026-08-29 已获用户授权同步并部署，发布仍须以最终干净提交通过 Release 门禁。

## 64. 2026-08-29 运营配置运行时接入

- 后台运营配置已按赛季与天灾、构筑规则、对战与房间、功能开关、维护与公告、版本与状态重新分层，赛季天灾池、禁限卡、模式开关、默认房间配置和维护状态均有真实字段、校验、历史和回滚入口。
- 新账号默认预组由配置的官方预组主宰决定；牌库保存、发布、导入、房间与沙盒选择以及开战前复核均读取同一禁限卡策略，避免只在编辑器提示而服务端仍可绕过。
- 新房间读取默认模式、观战、手牌公开及天灾配置；维护或停用模式会阻止创建、加入、准备和开始新对局，但不会中断已经开始的对局与重连。
- 房间创建时固定运营策略版本，禁限卡、模式与赛季天灾池不会被后台后续修改中途改变。赛季天灾池进入对战时洗混，〈堙灭〉仍固定为最后一张。
- 公开牌库与赛事功能开关已接入前端入口及服务端拒绝；其他任意开关只有在增加明确消费者后才会生效。
- 验证：运营配置/安全聚焦 16/16、十二军团完整规则 491/491、前端 UI 契约 125/125、卡图架构 19/19（248 张）及生产构建通过。平台全量仍有 10 项与本批无关的既有 OP 测试失败，已作为真实基线保留。
- 当前边界：赛事尚未自动编排并创建正式 L12 对局房间；本批完成代码与文档收口但未部署服务器。

## 65. 2026-08-29 傲慢资源、天灾高清与精确回合开始期限

- 〈傲慢之罪〉的主宰附加费用已进入公共普通资源选择：士气、神力和符合条件的活跃〈陵墓守卫〉均可支付；伊西斯等效果的内部费用对象先保留，避免同一守卫双重支付及失败后的部分场面修改。
- 天灾禁选、随机公开、触发确认与公开历史均请求详情级卡图，随机公开不再把240像素缩略图放大。
- “直到下个我方回合开始前”改为按真实回合开始事件失效，覆盖挑衅、免死、主宰不可被进攻及反击封锁，额外回合不再造成错误延长；同池补齐〈库丘林〉前排免死入口。
- 〈阿喀琉斯·晋升〉只有在本次战斗权威记录为“进攻者存活且实际击杀被进攻军团”时才获得状态；佣兵抵挡、支援、目标存活与同归均不触发。状态持续期间只有其当前位于前排才实际具有挑衅。
- 〈天照大神〉两项能力已验证为独立次数与独立费用：第一项1士气，第二项仅弃1手牌；只有〈傲慢之罪〉会给任一主宰效果追加1资源。
- 验证：阿喀琉斯聚焦8/8、完整规则串行517/517、平台22/22、UI147/147、卡图19/19（248张）、生产构建通过；原子审计248/248、legacy case 0、`cardConditional=212`，较既有213继续下降。
- 发布边界：本批按完成流程同步GitHub，但未获得部署授权，不部署服务器。

## 66. 2026-08-29 观战退出与房间生命周期收口

- 观战者点击“返回大厅”会先向服务端发送权威离房请求；客户端在收到确认前进入 `leavingRoom` 状态，屏蔽迟到的房间/对局快照和自动跳回对局。
- 若退出请求期间网络重连，客户端在会话恢复后优先重发离房请求；服务端清除房间码、观战标记、玩家索引与自定义牌库，后续恢复不再返回旧观战快照。
- 房主关闭已经结束的房间时，服务端在同一关闭流程中快照并清空观战列表，释放所有玩家和观战者，并向在线观战者发送房间关闭通知；观战加入和关闭通过同一锁防止竞态。
- 同类型扫描覆盖正式观战返回大厅、沙盒退出、普通玩家赛后返回房间、玩家投降后离房、观战重连恢复、房主关闭房间和对局广播枚举。正式玩家赛后重备语义保持不变。
- 验证：新增三条观战生命周期回归并与既有玩家离房回归合计4/4；完整后端串行520/520；UI契约148/148、卡图架构19/19（248张）、Vue/TypeScript/Vite生产构建通过；原子审计248/248、legacy case 0、`cardConditional=212`。
- 发布边界：按仓库完成流程同步GitHub；本批没有部署授权，不部署服务器。

## 67. 2026-08-30 后台 BQ-20260830-01 规则与状态收口

- 〈拉美西斯二世〉的候选改为排除全部同名实例；由其委托发动的军团【登场时】效果携带结构化来源保护，不再因换成被选军团作为堆叠来源而错误开放反击战术。
- 〈信仰狂热者〉继续按物理实例各自每回合1次；免费主宰发动与正常主宰次数双向独立，修复“正常发动后免费机会被前置次数门禁拒绝”。
- 场上已有〈王者之剑〉时，第二个〈亚瑟王〉发动前显示设计者确认的精确警告；继续会支付1符文但不生成、叠放或转移剑，取消不支付且不产生结果。
- 验证：新增红测首轮3失败/2通过，修复后聚焦13/13；完整规则串行525/525；平台持久化22/22；原子审计248/248、legacy case 0、`cardConditional=212`；UI契约148/148、卡图架构19/19（248张）和生产构建通过。GrandUMI历史全套10项既有OP16/EB_M6失败不属于L12发布门禁且与本批无文件交集。
- 发布边界：按完成流程同步GitHub并逐条回写后台修正情况；生产环境仍停留在既有版本，本批没有部署授权。

## 68. 2026-08-30 费用资源与牌库摘要统一

- 对战双方费用资源使用同一稳定排序投影：普通阵营为活跃士气、休整士气；奥林匹斯为活跃神力、活跃士气、休整神力、休整士气。
- 排序只改变显示位置，不改变权威数组和物理实例 ID；支付、返还、〈黑色莲花〉等身份相关处理继续命中玩家实际点击的资源。
- 神力 Logo 改为淡黄色，并以金色描边和奥林匹斯蓝底与普通士气区分；活跃/休整状态继续以亮度和饱和度区分。
- 新增统一 `DeckProfile`，牌库编辑器、我的/公开牌库、牌库详情、新旧友谊战房间和沙盒全部使用主宰 Profile，不再混用整卡裁切、多卡拼条与“库”占位图。
- 验证：UI契约152/152、卡图架构19/19（248张）、Vue/TypeScript/Vite生产构建通过；用户已授权本批同步GitHub并部署。

## 69. 2026-08-30 GitHub 自动同步授权边界

- 验证通过且无未解决远端冲突的完成批次，已获得长期提交并推送 `origin/main` 的授权，不再重复请求每批 GitHub 推送确认。
- 服务器部署、生产版本切换、服务重启与其他外部系统不包含在长期授权中，仍须逐批获得明确授权。
- 根 `AGENTS.md` 与快速、标准、深度、关键四档代理配置已统一该边界，避免审批规则互相冲突。

## 70. 2026-08-30 好友房规则与对战动画呈现收口

- 好友邀请接受后立即关闭邀请界面，双方由权威 `friendRoomCreated/roomState` 自动进入整备室；发起方作为房主，可在开战前修改禁限卡、天灾、观战和手牌公开规则。服务端拒绝访客或开战后的更新，保存规则会重置双方准备状态。
- 符文、卡诺匹斯等主宰附近圆形标识按双方场面方向镜像，试炼卡背使用正式横版素材比例，不再拉伸。
- 动作条只保留回合开始、主要阶段、回合结束。卡牌打出、登场、盖伏、位移、入墓、返回和检索改为读取 DOM 更新前后的真实区域或卡牌锚点，首尾与场面实体连续衔接。
- 奥西里斯特殊胜利使用约7秒金黄色双方可见序列：关闭墓地，五张完整卡诺匹斯罐沿倒五角星绘制顺序出现，奥西里斯在中央出现并闪光停留后才进入胜利界面。
- 验证：完整规则525/525、平台持久化与控制面60/60、UI契约153/153、卡图架构19/19（248张）、原子审计248/248且旧卡号分支0、生产构建通过；本批只授权发布至测试域 `testrun.legion-12.com`。

## 71. 2026-09-01 第六批6D最后已知来源与权威区域事务

- 触发候选统一携带最后已知来源快照，内部权威查询补齐全部实战区域且与客户端可见查询隔离；区域转移按所有者和实例ID原子执行，缺失或重复失败关闭，禁止把快照插入区域。
- 桂小五郎的返回与士气转活跃成为先后独立触发；陵墓构造体的阵亡/离场候选保留附属守卫并按真实所有者处理；草薙剑可从任一权威区域把唯一真实实例返回所有者牌库顶部。
- 红基线0/13；最终专项14/14、草薙剑历史兼容1/1，Focused/Batch/Release规则均689/689、平台60/60、UI171/171、卡图与生产构建、提交级无部署发布包全部通过；原子248/248，旧分支与文本推断均为0，`cardConditional`降至198。按长期授权同步GitHub，本批不部署，严格不混6E及后续范围。

## 72. 2026-09-01 第六批6E公开位置结算重验与原子登场事务

- 9张焦点卡的公开登场位置继续在触发声明或主动提交时锁定；公共 `TrySummonFromAnyPrivateZone` 在结算时先重验目的战场、空位与唯一真实来源实例，全部成功后才从私区移除并落位。失败不覆盖、不改选、不复制，也不吞其他对象或恢复费用。
- 勇士比约恩的主宰1伤与4张墓地牌回牌库底均前移为冒号前原子费用；黄金圣甲虫主动位置失效仍保留休整与每回合一次。无名渗透者跨玩家战场、特洛伊木马战场转移及晋升/圣物/反击覆盖继续走各自合法事务。
- 红基线1/14绿、13/14红，修复后专项14/14；S1/S2、6D/6E兼容191/191。Focused、Batch、Release规则均703/703，Release平台60/60；私区登场门禁、原子248/248、legacy/text inference均0、`cardConditional=198`、UI契约171/171、卡图19项/248张、优化卡图248/248、Vue TypeScript/Vite生产构建及提交级无部署发布包全部通过；本批不部署。

## 73. 2026-09-01 第六批6F试炼推进公共事件

- 通常行动的8张 TrialValue 军团现统一生成可响应的 `TrialAdvanceEvent`；休整费用在入栈前支付，效果被无效不返还。推进只把当前未完成试炼的进度提高至最多8，不会自动标记完成或发布6B完成事件。
- 兰斯洛特、加拉哈德、芬恩、康斯坦丝的登场/击杀公开模式改由 TriggerBatch 的 `PendingActivation` 预声明；芬恩消耗符文转活跃是推进成功后的独立候选，来源失效只取消转活跃且不返还符文。安格斯战术成功触发改为强制且once在被无效后仍消费；阿瓦隆回合开始的推进与得符文作为同一可响应段，并在普通及GM阶段流结算后再恢复重置。
- 红基线1/20绿、19/20红；最终专项20/20、6F+6B 28/28、6B/Latest/S2兼容230/230。Focused、Batch、Release规则均723/723，Release平台60/60；公共触发门禁、原子248/248、legacy/text inference均0、`cardConditional=198`、UI契约171/171、卡图19项/248张、优化卡图248/248、Vue TypeScript/Vite生产构建及提交级无部署发布包全部通过。首次Release准确拦截新增`mode:rune`缺少玩家自然语言标签，补为“获得1枚符文”后完整复验通过。本批无新增规则裁定，不混6G，不部署。

## 74. 2026-09-01 第六批6G-A可选触发声明与独立段

- 统一玛格丽特、安德华拉诺特、阿尔忒弥斯、莫瑞甘、李牧和寻找圣杯之旅的七个可选触发入口：是否发动、公开士气目标与玛格丽特休整费用均在入栈前声明并提交。
- 回合一次触发改为 pending→final 预留；拒绝会释放，提交后即使效果被无效或目标结算时失效也不恢复次数。玛格丽特的回血与回血封锁拆成两个独立堆叠段。
- 全量历史回归同时发现并修复：拒绝最后触发后天灾检查未恢复、天廷士气归零提示重复创建，以及阿尔忒弥斯把休整士气错误变为活跃神力。
- 红基线0/14，最终专项14/14、历史兼容8/8；Focused、Batch、Release规则均737/737，Release平台60/60；原子审计248/248、legacy/text inference均0，`cardConditional=196`。UI契约171/171、卡图19项/248张、优化卡图248/248、Vue TypeScript/Vite生产构建、服务端发布包与提交级无部署验证全部通过。

## 75. 2026-09-01 第六批6G-B李牧隐藏展示延迟与独立抽牌段

- 李牧登场候选改为先公开声明是否展示牌库顶与是否执行随后抽牌，声明期不读取或泄露牌库顶身份；展示段合法开始后才读取顶部，并在符合条件时延迟询问免费打出或放底。
- 展示与抽牌使用公共复合计划作为两个独立响应段。展示拒绝、被无效或来源离场不吞抽牌；只选抽牌时直接从第二段入栈，两项都不选时不产生空栈。免费打出继续复用6A/6C手牌声明与PendingActivation。
- 红基线0/8，修复后专项8/8、相关兼容51/51；Focused、Batch规则均745/745，原子审计248/248、legacy/text inference均0、`cardConditional=196`。按用户要求，Release与提交级无部署验证并入全部后续批次完成后的最终统一收口；本批不提交、不推送、不部署。

## 76. 2026-09-01 第六批6I-A原子可选触发公开声明

- 全量13张卡14项Verified Atomic Optional统一在TriggerCandidate建立时判断公开前置条件，并在任何堆叠项出现前通过PendingActivation声明是否发动；条件不满足或拒绝发动均不产生空响应栈。
- 原子结算器只消费不可变`declared:mode`，删除旧`verified-atomic-optional` Prompt/continuation；已经成立的触发条件在响应期间不重验。宫本武藏与6H进攻声明组合为单一候选，阿塔兰忒普通登场与晋升登场保持两次独立声明。
- 红基线5/52绿、47/52红；修复后专项52/52、迁移精确兼容66/66、S2Faction与6H组合221/221，六项历史夹具迁移定向6/6。Focused、Batch规则均797/797，静态门禁与原子审计248/248通过，legacy/text inference均0，`cardConditional=196`、`cardSwitchArm=67`、无权威入口0。完整Release按用户要求并入全部后续批次最终统一验证。本批不提交、不推送、不部署。

## 77. 2026-09-01 第六批6I-B直接阵亡/进攻后触发公开声明

- 18项直接legacy阵亡/进攻后入口统一迁入公共TriggerCandidate与PendingActivation；可选发动、公开墓地/战场目标、公开登场位置、私密手牌身份和荆轲冒号前返还士气均在响应前声明，结算只消费不可变声明并重验目标或位置。
- 疯狂爱丽丝使用pending→final回合一次保留；拒绝释放，提交后无效或来源离场不恢复。黑胡子与雷神之锤等抽牌后弃牌的具体手牌继续在抽牌合法完成后私密选择；上杉谦信、坂本龙马与亚瑟王不向对手快照泄露手牌身份。来源离场统一读取最后已知快照，忒修斯展示后加入手牌，格温莉安仅处理效果导致阵亡并修正日志卡名。
- 红基线0/47，修复后专项47/47；旧亚瑟/忒修斯、雷神与GM/衍生离场夹具完成精确迁移。Focused、Batch规则均844/844，公共声明门禁及原子248/248通过，legacy/text inference均0、`cardConditional=185`、无权威入口0；Release与Git同步按用户要求并入后续批次统一收口。本批不提交、不推送、不部署。

## 78. 2026-09-01 第六批6J-A登场与晋升登场公开声明

- 扫描非Verified Atomic的S1/S2登场及晋升登场resolver，将44张卡45个时点统一接入数据驱动`L12EnterPublicTriggerPlans`。公开模式、费用、墓地/战场目标及位置经TriggerBatch与PendingActivation入栈前提交，结算只读`declared:*`并重验目标/位置；已付费用不因无效或失效恢复。
- 李靖、万物统御之戒、武田信玄、八尺琼勾玉、罗宾汉等只预声明公开模式/位置，牌库顶、检索与私密卡牌身份保留到合法效果开始后。诸葛亮展示/天灾调整、卡诺匹斯强化/弃置及武田检索/真田登场均为独立响应段；不发动或无合法首段时直接从首个真实后段开始，不产生空响应栈。
- 新增固定种子专项58项并迁移相关历史夹具，定向迁移13/13；公共声明静态门禁锁定44张卡、公共入口、隐藏Prompt白名单与独立段。Focused、Batch规则均902/902；原子审计248/248，`cardCase=0`、`cardConditional=185`、`cardSwitchArm=67`、`effectTextInference=0`、无权威入口0。Release、提交与Git同步按用户要求留到全部批次统一收口，本批不部署。

## 79. 2026-09-01 第六批6J-B主动/响应/结算期Prompt收口

- 野外扎营、前线侦查及7项旧触发共9张卡9项统一接入公共HandPlay Composite或TriggerCandidate/PendingActivation。公开模式、公开士气目标与普通资源、4士气、有序墓地2张等费用均在入栈前提交；无效、来源离场或目标失效不退款、不改选。
- 牌库顶部、排序、检索与受影响玩家自己的手牌选择继续在对应段合法开始后私密处理；祷告仪式被拒后的私密查看作为独立候选，不在公开段或声明期读取天灾身份。同一候选已有声明时不再重复建立PendingActivation。
- 全池扫描139个`CreatePrompt(`静态令牌（138个调用点）、63个PendingActivation入口和48个PushEffect入口，保留Prompt按七类登记并设139只降不升门禁。红基线0/9，最终专项16/16、定向兼容12/12、历史顺序迁移3/3；Focused/Batch规则均918/918，原子248/248、旧case与文本推断0、`cardConditional=185`。Release及Git同步留最终统一收口，本批不提交、不推送、不部署。

## 80. 2026-09-01 第六批6J-C效果生成免费打出与私密区域事务

- 全池扫描效果生成打出、再次发动、直接加入手牌与牌库移区。生产`PushEffect(...,"play")`由4处收敛为3处：公共复合免费打出、公共效果生成打出及待裁定托勒密重复主动；李牧和冲田不再各自直接Push。直接`Hand.Add`稳定为7处合法底层原语，直接`Library.Remove`降为41处合法结算期移区，分别设只降不升门禁。
- 新`L12EffectGeneratedPlay`统一已展示牌库卡的免费打出：李牧普通/复合主动战术共用同一事务；冲田军团公开位置进入PendingActivation，可覆盖己方后排反击，提交时重验位置，失效不覆盖、不改选且不错误回手。军团复用私区登场权威事件、天灾等级与登场触发；圣物复用替换、持续与登场触发；战术移入Resolving后才压入统一play栈，被无效不返回牌库。
- 效果加入手牌继续统一经`AddCardToHandByEffect/effect-hand-add`，未展示身份不会进入对手快照或公开日志；明确展示的冲田顶部卡保留公开reveal。红基线3/6红，扩充专项12/12，李牧兼容11/11、冲田兼容4/4，组合兼容244/244；Focused/Batch规则均930/930，原子248/248、`cardCase=0`、`cardConditional=186`、`effectTextInference=0`、无权威入口0。Release、提交与Git同步继续留全部批次最终统一执行，本批不部署。

## 81. 2026-09-01 第六批6K-A S01通用与天廷逐卡逐能力语义审查

- 逐项验收22张S01通用基础卡、24张天廷卡和10张通用天灾，共56张94项能力；独立审计表逐卡保留规则断言、运行时和测试证据，唯一状态为46通过、8修复、2疑点、未实现0、缺测0。
- 修复黑胡子、战斗至黎明、刘备、西施、空城计、凌霄宝殿、杨戬的独立效果段与公开声明，并纠正摄政皇权强制触发：合法时不能拒绝，手牌身份私密、位置公开，置伏来源真实入栈前翻开。李靖依赖“随后”和雷霆天怒平局只登记待裁定。
- 红基线0/9及摄政补充0/1；最终专项10/10、相关兼容123/123、Focused/Batch规则940/940、原子248/248、静态门禁与矩阵导出通过。完整Release和Git同步按用户要求留最终统一收口，本批不提交、不推送、不部署。

## 83. 2026-09-01 第六批6K-C S01高天原逐卡逐能力语义审查

- 逐项验收24张高天原卡的55项原子目录能力，独立审计表逐卡保留规则断言、运行时与测试证据；唯一状态为15通过、9修复、0疑点、未实现0、缺测0。
- 本多忠胜的全体减费/随后击杀、天照大神两项主动及天诛公开目标进入公共PendingActivation/Composite，公开目标和费用入栈前提交、独立段各自响应。`DeferredEffectStack`改为逐项压入，不再让底层后段跳过响应窗口。
- 服部半藏隐匿后按规则不属于普通军团目标，但仍被天灾专用口径影响；高天原公开军团查询与私密区域检索统一通过`HasFaction`处理万物统御之戒，全池同根因裸分支一并清理。
- 首轮1/8绿、7/8红及天诛精确0/1；最终专项11/11、相关高天原/S1/S2兼容600/600、Focused/Batch规则均967/967、原子248/248、24卡/55项静态门禁与矩阵导出通过。无新裁定，Release、Git同步与提交继续留全部批次最终统一收口，本批不部署。

## 82. 2026-09-01 第六批6K-B S01太阳城与阿斯加德逐卡逐能力语义审查

- 逐项验收太阳城29张69项与阿斯加德24张55项，共53张124项能力；独立审计表逐卡保留规则断言、运行时与测试证据，唯一状态为43通过、8修复、2疑点、未实现0、缺测0。
- 图特摩斯、伊瓦尔、卡诺匹斯箱/罐二/罐三、众神之乡与英灵殿统一完成公开声明、隐藏信息延迟和语义独立段；复合公共框架不再把智慧法典仅属于已完成StackItem的一次性奖励标记复制给后续独立段。
- 初始红基线1/12绿、11/12红；最终专项16/16、智慧法典精确17/17、相关兼容404/404，Focused/Batch规则956/956，原子248/248、静态门禁与矩阵导出通过。霍列姆赫布致命替代和托勒密重复主动沿用既有OPEN，不猜裁定；完整Release与Git同步留最终统一收口，本批不提交、不推送、不部署。

## 84. 2026-09-01 第六批6L-A S02通用与天廷逐卡逐能力语义审查

- 逐项验收18张S02通用卡与8张天廷卡，共26张51项能力；独立审计表逐卡保留规则断言、运行时与测试证据，唯一状态为23通过、2修复、1沿用既有疑点、未实现0、缺测0。
- 孙悟空在支付2至8张士气的同一PendingActivation内公开声明前排格，结算重验失效不覆盖、不改选、不返费；始皇帝击杀与返还士气/建立限制成为两个独立响应段，本回合非阵营来源的士气增加统一受公共入口限制。
- 红基线1/6绿、5/6红；最终专项6/6、定向10/10、S2通用与人工原子42/42，Focused/Batch规则均973/973；26卡/51项静态门禁、矩阵导出和原子248/248通过。信仰狂热者声明层级继续保留既有OPEN，不猜裁定；Release、Git同步与提交留最终统一收口，本批不部署。

## 85. 2026-09-01 第六批6L-B S02太阳城与阿斯加德逐卡逐能力语义审查

- 逐项验收太阳城8张21项与阿斯加德8张23项，共16张44项能力；独立审计表逐卡保留规则断言、运行时与测试证据，唯一状态为12通过、4修复、0疑点、未实现0、缺测0。
- 陵墓圣武士离场减费改由真实公共离场事务按控制者登记并于回合末清零；沙漠君临在入栈前原子弃置场上军团费用并以公共Try事务重验声明格；雷神之锤主动按钮直接进入墓地费用/位置声明；陵墓守卫资源只按当前控制者己方回合判断。
- 红基线2/6绿、4/6红；最终专项6/6、精确兼容8/8与7/7、相关组合251/251，显式串行Focused与Batch规则均979/979；16卡/44项门禁、Prompt棘轮135、矩阵导出和原子248/248通过。首次Focused的CLR内部Regex瞬态崩溃已由串行全绿复验排除生产回归；Release、Git同步与提交留最终统一收口，本批不部署。

## 86. 2026-09-01 第六批6L-C S02高天原与奥林匹斯逐卡逐能力语义审查

- 逐项验收高天原7张22项与奥林匹斯28张79项，共35张101项能力；独立审计表逐卡保留规则断言、运行时与测试证据，唯一状态为20通过、14修复、1沿用既有疑点、未实现0、缺测0。
- 月读后→前每次移动各建独立可响应触发；武运在天与诸神巅使用公共Composite拆分独立段；晋升底座状态由公共事务完整迁移但不启用底座印刷能力。奥林匹斯效果选择与重验统一按`HasFaction`识别万物统御之戒，隐藏牌库身份仍延迟到合法结算。
- 红基线1/8绿、7/8红；最终专项13/13、精确兼容6/6，Focused/Batch规则均992/992；35卡/101项静态门禁、矩阵导出和原子248/248通过。海伦致命替代继续保留既有OPEN，无新裁定；Release、Git同步与提交留最终统一收口，本批不部署。

## 87. 2026-09-01 第六批6L-D S02彼界与天灾逐卡逐能力语义审查

- 逐项验收彼界32张与S02天灾6张，共38张83项能力；独立审计表逐卡保留规则断言、运行时与测试证据，唯一状态为30通过、8修复、0疑点、未实现0、缺测0。七份独立审计至此覆盖S1+S2全248张、552项能力。
- 梅林/圣杯隐藏牌库信息延迟到合法结算；彼界有效阵营统一识别万物统御之戒并保留“只有彼界”印刷边界；十字军三模式共享once，加拉哈德费用预付。S02天灾仍不可响应并保持真实牌库反转、中立非致命等既有语义。
- 红基线1/9绿、8/9红；最终专项11/11、相关兼容249/249，Focused/Batch规则均1003/1003；38卡/83项及全池248张门禁、矩阵导出和原子248/248通过。无新增裁定；Release、Git同步与提交留最终统一收口，本批不部署。

## 88. 2026-09-01 第六批6M全卡池最终交叉审查与Release

- 七份逐卡表、矩阵与OPEN完成交叉校验：248张/552项唯一覆盖，188通过、54修复、6张疑点卡对应5项裁定，缺测与未实现均为0。
- 最终diff审阅修复戒指声明期隐藏命中泄露、有效阵营声明过滤和3个内部模式标签；新增红绿回归与只降不升静态门禁，无新增裁定。
- Release规则1006/1006、平台60/60、原子248/248、UI171、卡图19/248、优化卡图248/248、Vue TypeScript/Vite与服务端publish通过。仅有既有NU1900离线元数据警告；默认产物目录ACL改用隔离`.runtime`后完成无部署验证。本轮不提交、不推送、不部署，交主代理唯一同步。

## 89. 2026-09-02 ST卡图线上降级链修复

- 线上可见复现证明ST卡图清单已是324张，但未上线的清单CDN被优先请求，重复WebP错误事件又跳过可用同源图，所以没有Steam备份的76张ST卡最终全部显示XII占位。
- 客户端改为只有显式构建变量才能优先CDN，否则先走已经验证200的同源内容寻址图；错误事件与激活URL绑定，避免跨级。发布门禁改为必须从公网清单读取并GET `ST01-01` 图片，不再只抽查第一张旧卡。
- 本地通过卡图契约23/23、UI172/172、Vue TypeScript与Vite生产构建；修复随release `00edf6b`上线，正式浏览器筛选ST01后12张结果全部显示真实卡面。
- 发布过程同时定位并修复原子目录每卡动态编译Regex造成的.NET 10并行崩溃；修复后保持默认并行并在37秒内通过Release规则1049/1049，无需以全串行牺牲发布速度。
- 首次重发由新增公网门禁发现ST01-ST06没有命中Nginx内容寻址长缓存规则并自动恢复上一版本；正则统一覆盖Sxx/STxx后，二次发布通过ST01-01真实GET、WebP类型与一年immutable缓存门禁。

## 90. 2026-09-02 ST第三批B剩余18张卡效

- ST-DS02、ST01-M1、ST02-07、ST02-10、ST02-M1、ST03-08、ST03-10、ST03-M1、ST04-02、ST04-04、ST04-05、ST04-10、ST04-M1、ST05-01、ST05-10、ST05-M1、ST06-M1、ST06-S1已全部进入结构化能力目录和权威运行路径，ST卡无实战入口清零。
- 费用与公开信息统一在入栈前声明并支付，隐藏牌库身份延迟；句号和“随后”定义的独立段分别开放响应。持续效果、回合末区域事务、攻击双方时点、致命替代、叠放强攻、墓地多重计数、资源消费触发、试炼完成与一次性减费均复用公共入口。
- 固定种子20401至20423专项13/13；主代理完整Release规则1089/1089、平台60/60、UI契约172、卡图契约24/324、Vue TypeScript/Vite生产构建及发布包通过。原子324张通过，`cardCase=0`、`cardConditional=187`、`cardSwitchArm=67`、文本推断0、`noRuntimeEntranceCards=0`；新增模式自然语言标签缺口被Release门禁捕获并补齐后从头复验通过。
- 无新增规则裁定项。功能提交`9f55cdd`已推送GitHub main并部署正式服；服务器活动版本`9f55cdde8cdca7a2044f0f3b00e518da047fb279`，主页、卡牌页与健康接口200，WebSocket建连通过，服务active。

## 91. 2026-09-02 效果对象选择顺序与奥林匹斯资源事务

- 高天原阵营效果按卡面顺序改为“支付2士气→抽1张→按结算时场面选择活跃军团→选择相邻空位”，后半段没有合法目标时只跳过位移，不回滚已经结算的抽牌。
- 奥林匹斯阵营效果将“支付哪张资源”和“把哪张士气翻为神力”拆成两个决定；存在不同支付结果时先让玩家点击费用对象，避免系统自动消耗准备翻成活跃神力的士气。支付完成并经过响应后，才选择翻转目标。
- `S02-0513`、`S02-0518`、`S02-0520`的可选登场翻面只显示一次目标选择，目标框内提供“不发动”，不再出现“是否发动→选择对象→再次取消”的重复流程。
- 六张ST阵营士气统一映射到既有阵营能力，ST04高天原与ST05奥林匹斯自动继承相同顺序；玩家文案门禁新增“公开对象”禁词。完整Release规则1097/1097、平台60/60、UI契约172、卡图契约24/324、原子零旧分支、Vue TypeScript/Vite生产构建及提交级发布包全部通过；本批不部署。
- 全池逐卡状态随本批证据更新为191张通过、57张明确错误已修复、疑点0；旧审查批次当时的历史统计保留，不回写成伪造的历史结果。

## 2026-09-03 更新 94：ST主宰Profile与天灾圆形官方物料

- 新增6张ST01–ST06初始主宰Profile与19张S01/S02/ST天灾圆形卡图，全部使用逻辑卡号命名并经共用资源入口消费。
- 左上角本局天灾和沙盒自定天灾共用官方圆图；未知天灾仍使用圆形卡背，禁选、详情、触发展示和中央活跃天灾仍使用完整高清卡面。
- 防回滚契约为UI 173项、卡图架构27项/324张；Release规则1097/1097、平台60/60、TypeScript/Vite生产构建与发布包全部通过。

## 2026-09-03 更新 95：公开匹配、排位与七曜

- 对战大厅的排位与休闲匹配已由服务端权威队列接管：排位使用赛季天灾和禁限卡，休闲使用全部天灾且无禁限卡；同账号不能互配，等待扩圈会每5秒重算并保留最初排队时间。
- 三派系为秩序、混沌、命运。每赛季先选派系、完成5场定级，之后进入恰好5个段位；改选派系会归档原派系本赛季数据并重新定级，但保留隐藏匹配分。派系、段位名、颜色、图标和前五/第一称号均可由后台编辑。
- 七曜值、派系总量、个人胜负结算、段位保底、实力差、连胜/连败与终结连胜均已持久化；胜负页只展示本人变化，重要事件在对战子页与对战界面无蒙版滚动广播。
- 排行榜只显示本赛季排位数据且不公开内部账号ID；个人对局记录排除沙盒，沙盒不创建正式SQLite记录、不进入排行榜和排位结算。
- 验证：排位/匹配/记录/沙盒专项29/29，完整Release后端1104/1104、平台60/60、UI契约173/173、卡图架构27/27（324张卡）、原子审计324张零旧分支、TypeScript/Vite生产构建及提交级发布包通过。本批不部署。

## 2026-09-03 更新 96：卡效选择、协防与内容界面统一

- 可选效果统一为来源名称、本次效果文本、发动/不发动和确认；来源卡图不再占据中央。弃牌、查看多张、选择部分共用横向卡图，非法对象只灰置；唯一合法资源支付自动完成，多种支付结果仍由玩家自主选择。
- 阿喀琉斯·晋升只在确认击杀后获得挑衅，持续至下个我方回合结束。ST“协防”进入结构化关键词和复数支援协议，可跨列并与普通同列支援者联合；迦具土触发不会跳过后续支援。
- 官网内容改为批量载入后原子呈现，避免先闪默认文案；后台加入实际资讯编辑器。牌库编辑器将详情移到左侧原筛选位置，筛选移到主牌库上方，详情和已保存牌库分盒且后者独立纵向滚动。
- 当前已通过Release规则1107/1107、平台持久化与控制面60/60、UI177项、卡图27项/324张、Vue TypeScript/Vite生产构建和浏览器布局检查；ST 324张卡继续保持无实战入口0、旧卡号执行分支0。提交、GitHub同步和正式服部署在本批统一完成。

## 2026-09-03 更新 97：试炼与 ST 卡效真实闭环

- 修复试炼军团在玩家快照中丢失“试炼”按钮；通常试炼现在休整并立即推进，不开放响应，仍保留活跃、非登场回合、未完成试炼和卡效锁定等合法性条件。
- 完成珀涅罗珀的神力费用/手牌军团/登场位置链与特勒马科斯的顶3全展示、合法选择、展示加手和余牌整体回顶/回底链。
- 把 ST 全目录从“有入口”审计升级为76张逐卡端到端审计，补全赵云、诸葛连弩兵、聂隐娘、王昭君、暗度陈仓、绿洲的舞女、阿肯那顿、克里斯蒂娜、送葬者凯恩、吉原的花魁、美狄亚、美杜莎、光之剑和两张天灾的真实运行缺口。
- 聚焦回归12/12，ST目录审计76张、69张效果卡、7张白板、121条人工能力边界通过；完整 Focused 规则/实战1125/1125与直接 Prompt、私密区域事务棘轮通过。本批未同步或部署。

## 2026-09-03 更新 98：主效果后可选额外段空询问收口

- 阿斯加德阵营效果固定先支付2士气并抽牌，再根据结算时血量与剩余资源决定是否提供“支付1士气回复1点血量”；条件或费用不满足时不再弹出无意义询问。
- 新增通用结算后可选付费段，并让阵营、主宰、军团、圣物、战术及复合计划统一跳过只剩单一否定项的空决定；费用唯一时自动支付，多种资源仍由玩家点击选择，非费用目标继续在结算过程中选择。
- 全卡池扫描并补齐旧直接入口的合法目标守卫，保留包含查看牌库或排序操作的有意义空检索界面。专项29/29通过；完整Focused门禁被当前工作区既有S02审计PowerShell脚本解析错误阻断。本批未同步或部署。

## 2026-09-03 更新 99：阵营士气基准身份与命名统一

- 新增前后端共用的权威士气身份表：天廷、太阳城、阿斯加德、高天原、奥林匹斯、彼界分别固定使用最早的基础士气；奥林匹斯普通面为 `S02-05C1A`、神力面为 `S02-05C1`。异画和ST士气仅作为版本别名，不再成为运行时默认资源。
- 服务端卡表、预组、牌库验证、建局、阵营效果、资源点击和特殊区域，以及前端账号牌库、编辑器、档案和沙盒选择器均从同一映射解析。所有玩家可见名称统一为“士气·阵营”，奥林匹斯神力统一为“神力·奥林匹斯”。
- 奥林匹斯活跃士气、休整士气、活跃神力、休整神力四态未被合并；构筑中误存的ST或神力面会在保存/载入/建局边界规范到普通基准面，战局中的真实神力状态仍保留神力卡面。
- 验证通过：士气身份专项20/20，牌库/扩展效果/ST士气及玩家文案相关组合95/95，UI契约180/180、卡图资源契约28/28（324张）以及Vue TypeScript/Vite生产构建。完整Release仍有同一工作区前序批次的7项既有失败，已向主代理逐项交接；本批未同步或部署。

## 2026-09-03 更新 100：卡牌档案同卡版本聚合

- 卡牌档案由物理版本列表改为逻辑卡列表；324张权威卡加5张仅供档案展示的S1异画合计329个版本，聚合为318张逻辑卡，网格、筛选结果和总数均不再重复计算版本。
- 默认卡面稳定选取最早产品中最低已知罕贵度，未知及缺失罕贵度有固定后置顺序；搜索任一版本的卡名、卡号、产品、罕贵度、效果或标签均会命中该逻辑卡。
- 详情卡面提供左右透明三角循环版本，编号、产品、图片、数值、效果、标签和预组收录随当前版本同步；横版卡仍沿用既有缩略图旋转与8:5详情规则。
- 士气版本复用权威canonical别名；奥林匹斯普通士气与不同规则的神力反面保持分离。UI契约184/184、卡图架构28/28（324张）、Vue TypeScript与Vite生产构建通过；本批未同步或部署。

## 2026-09-03 更新 101：触发批次与进攻时间线恢复

- 修复可选进攻时效果因条件、资源或目标不足而自动跳过后，战斗停在进攻方效果时点的问题；触发批次无 Prompt、声明、候选或堆叠项后，统一恢复持久化进攻状态机。
- 阿喀琉斯·晋升的两条战斗回归重新进入真实伤害结算：近战不会获得额外伤害，受到远程进攻时进攻方造成伤害-1000；战斗结束断言防止今后再次以“数值没变”掩盖时间线未执行。
- 同步清理观星、花魁的馈赠、安格斯、震击及珀洛特埃入口测试中对无意义弹框或非法攻击目标的旧假设。关联聚焦30/30、完整Release规则1148/1148通过；本批未同步或部署。

## 2026-09-03 更新 102：生产后台19条新Bug公共根因收口

- 只读拉取`status=new`共19条并建立`BQ-20260903-01.01`至`.19`唯一映射；按Match、文本、历史台账和当前工作树去重为五个公共根因、五条确定性复核、两组已修待发布及一条FAQ非Bug。未在本机输出或下载管理员凭据，真实对局私密数据未写入测试夹具。
- 对方回合前排+1000改为9张卡的结构化连续规则；全部符文支付统一经`SpendRunes`并消除兰斯洛特旁路；全部晋升者由服务端发布实例级合法基底，补齐埃涅阿斯；31个主动休整能力与印刷回合一次分离；月读另一目标支持双方战场，拒绝不消费次数、成功后同回合不再询问。
- 近藤支援、花魁顺序、陵墓构造体三守卫、阿麦金圆桌骑士及临时士气支付均以确定性真实流程证明当前行为正确，没有为报告文本另加单卡分支。罗宾汉/侍从骑士/都铎命中BUG-20260903-206，银臂多符文命中BUG-20260903-214；兰斯洛特防守击杀按FAQ 19不改。
- 同批补齐大厅内换牌库闭环：排位、休闲、好友房及沙盒双方入口共用已保存牌库选择器，不再跳转编辑器；选择采用模式隔离的事务式草稿，取消无副作用、确认才持久化并在好友房同步服务器。排位按当前运营禁限/赛季校验，休闲不套排位规则，好友按房间开关，沙盒只做基础构筑校验；非法牌库灰置并说明，无可用牌库明确提示。
- 活动入口扫描覆盖`/battle`与`/sandbox`；旧`/lobby`仅重定向、OnePiece组件未挂载，明确的`/decks`管理入口保留但不承接“更换牌库”。大厅策略专项1/1、BQ聚焦63/63、Focused、Batch与主代理Release规则均1223/1223，平台60/60，UI契约201/201、卡图28/28（324张）、原子旧执行分支0、TypeScript/Vite及提交级发布包通过；待远端复核/发布。本执行任务未提交、推送、部署或修改生产后台，最终一次性同步、正式服部署与19条后台回填由主代理完成。
