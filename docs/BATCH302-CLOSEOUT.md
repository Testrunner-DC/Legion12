# BATCH302 本地收口与发布日志草稿

日期：2026-09-09。未提交、推送、部署或回填线上 Bug。保留此前批次全部差异。

## 已完成的可见结果

1. 预约维护提示与提前广播在同一时间窗口出现，不再刚预约便显示停机提示。
2. 单卡分析先选主宰（可全部），复用 GM 卡牌选择器；时间为全部、近7日、近30日、本赛季。详情增加指标与样本说明；独立卡牌清单支持排序与分页，先取得当前筛选完整列表再排序，避免仅排序当前页。
3. 后台账号、Bug、发布、审计、文章、媒体、对局及治理等增长列表改用分页；固定配置字段和已有服务端分页不重复改造。分页不删除数据、不扩大接口查询范围。
4. 网页更新日志仅展示最新10份，历史记录不删除。
5. 改选派系显著提示七曜值及本赛季战绩重置；支持返回、二次确认及取消，确认前不修改原派系。
6. 结算页双方独立退出，先退出者立即可开启下一局；不会因另一方停留而受限，也不会因旧房间关闭影响新对局。双方离开后回收，最迟30分钟回收；赛果持久化未完成则保留现场重试，不丢弃结果。终局沙盒不走旧5分钟断线清理路径。
7. 牌库编辑器额外区与主牌库采用一致列表结构，横卡背景不再挤乱名称、数量和操作按钮。
8. 共用卡牌详情横卡保持比例，图片及边框受父容器宽度约束，不再溢出编辑器侧栏。

以上可见结果为下一次实际发布的日志草稿，不能标为已上线。

## 证据

- 前端最终完整 build/typecheck/UI313及既有专项通过（session98737，版本0.0.0-batch302-local）。新增维护边界、分页、派系确认、分析查询守卫接入正常构建。
- 浏览器交互：分析页1440/390，派系及分页1280/390，额外区与横卡详情1600/1280/768通过。使用合成数据，不含真实用户私人信息。
- 图片证据：`D:/GPT/Legion12/artifacts/batch302-analytics`、`batch302-controls`、`batch302-deck-extra`。
- 完整规则此前2766/2766通过（session58867）；加强先退出玩家实际开启新局的专项8/8通过。最终沙盒保护修改后完整规则复验见 HANDOFF 最新记录。
- diff --check通过。没有运行无关 GrandUMI 全量测试；本批未改共享平台持久化逻辑。

## 实现边界与回滚守卫

- 结算退出使用保留席位信息的虚拟占位，不清除仍在读结果的对方；清理成员前核对当前房间，不能清掉已加入新房间的玩家。
- 服务端拒绝在终局直接重新准备清空结果；正常新房间开局及恢复仍保留测试。
- 不新增资源清理调度器，复用现有房间时钟；不改变正在运行的真人对局。
- 分析保持排位及合格数据范围，隐私权限不变；旧筛选异步结果不得覆盖新筛选。
- 横卡使用共享详情组件约束，不复制另一套详情，不改变图鉴产品及勘误开关。
